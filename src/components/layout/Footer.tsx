'use client';

import styles from './Footer.module.css';

export default function Footer() {
  return (
    <footer className={styles.footer}>
      <p>AXPT 2025 — All Rights Reserved — connect@axpt.io</p>
    </footer>
  );
}