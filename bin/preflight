#!/bin/bash

# === AXPT | Preflight Shortcut ===
# This wraps the full ultra preflight deploy ritual
# Usage: bin/preflight

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$SCRIPT_DIR/../app/scripts"

bash "$PROJECT_ROOT/ultraPreflightDeploy.sh"
