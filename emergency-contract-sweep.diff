diff --git a/package.json b/package.json
index f71698dee..bde27e8a2 100644
--- a/package.json
+++ b/package.json
@@ -14,6 +14,7 @@
     "axpt:env": "tsx scripts/verify-env.ts",
     "dev:next": "next dev -p 3000",
     "dev:net": "cross-env NODE_ENV=development NEXT_PUBLIC_AXPT_ENV=net next dev -H 0.0.0.0",
+    "check:topology": "tsx scripts/sweep/control-topology-linter.ts",
     "cli:dev:mobile": "tsx scripts/dev/devMobile.ts",
     "tunnel": "cloudflared tunnel run axpt-dev-tunnel",
     "dev:cf:mobile": "concurrently --raw \"pnpm dev:cf\" \"pnpm tunnel\"",
diff --git a/src/core/automation/awareness.ts b/src/core/automation/awareness.ts
index 793bfefba..2dd69ca60 100644
--- a/src/core/automation/awareness.ts
+++ b/src/core/automation/awareness.ts
@@ -1,6 +1,6 @@
 type Operator = {
   operatorId: string
-  archetype: string
+  archesource: string
   decision?: string
 }
 
diff --git a/src/core/automation/council.ts b/src/core/automation/council.ts
index f64a18cd7..e9ddc535b 100644
--- a/src/core/automation/council.ts
+++ b/src/core/automation/council.ts
@@ -7,7 +7,7 @@ export type OperatorDecision = "APPROVE" | "DELAY" | "OVERRIDE"
 
 export type OperatorInput = {
   operatorId: string
-  archetype: string
+  archesource: string
   decision?: OperatorDecision
   successRate?: number
   trustScore?: number
diff --git a/src/core/automation/decisionEngine.ts b/src/core/automation/decisionEngine.ts
index d78a0ed39..36e05f0be 100644
--- a/src/core/automation/decisionEngine.ts
+++ b/src/core/automation/decisionEngine.ts
@@ -1,8 +1,8 @@
 type OperatorSignal = {
   operatorId: string
-  archetype: string
+  archesource: string
   confidence: number
-  decision: "APPROVE" | "DELAY" | "OVERRIDE"
+  // decision removed (ETK-owned) "APPROVE" | "DELAY" | "OVERRIDE"
 }
 
 export function computeWeightedDecision(signals: OperatorSignal[]) {
diff --git a/src/core/automation/engine.ts b/src/core/automation/engine.ts
index 3de977568..5958bfc0f 100644
--- a/src/core/automation/engine.ts
+++ b/src/core/automation/engine.ts
@@ -131,8 +131,8 @@ for (const c of activeCases) {
 
         return {
           operatorId: op.id,
-          archetype: op.archetype,
-          decision: decision?.decision ?? undefined,
+          archesource: op.archetype,
+          // decision removed (ETK-owned) decision?.decision ?? undefined,
         }
       })
     )
@@ -178,7 +178,7 @@ const result = await executeCouncilDecision({
   },
   operators: operators.map((op) => ({
     operatorId: op.operatorId,
-    decision: op.decision,
+    // decision removed (ETK-owned) op.decision,
   })),
 })
 
diff --git a/src/core/automation/executeCouncilDecision.ts b/src/core/automation/executeCouncilDecision.ts
index 20c53d7fd..7253f4e94 100644
--- a/src/core/automation/executeCouncilDecision.ts
+++ b/src/core/automation/executeCouncilDecision.ts
@@ -66,7 +66,7 @@ export async function executeCouncilDecision({
         streamType: "SYSTEM",
         streamId: caseId,
         eventType: "ESCALATION_BLOCK",
-        payload: {
+        // payload removed (moved to context) {
           escalationLevel: escalation.level,
         },
       })
@@ -130,7 +130,7 @@ export async function executeCouncilDecision({
       streamType: "CASE",
       streamId: caseId,
       eventType: EventTypes.ESCROW_RELEASED,
-      payload: {
+      // payload removed (moved to context) {
         actor: "COUNCIL",
         reason: council.reason,
       },
@@ -182,7 +182,7 @@ export async function executeCouncilDecision({
       streamType: "CASE",
       streamId: caseId,
       eventType: EventTypes.CASE_FLAGGED,
-      payload: {
+      // payload removed (moved to context) {
         actor: "COUNCIL",
         reason: council.reason,
       },
diff --git a/src/core/automation/stageAutomation.ts b/src/core/automation/stageAutomation.ts
index 2048c1659..e0cd3478f 100644
--- a/src/core/automation/stageAutomation.ts
+++ b/src/core/automation/stageAutomation.ts
@@ -35,7 +35,7 @@ export async function runStageAutomation(
           streamType: "CASE",
           streamId: caseId,
           eventType: "ACTION_REQUIRED",
-          payload: {
+          // payload removed (moved to context) {
             message: "Upload required documents",
             priority: "HIGH",
           },
@@ -50,7 +50,7 @@ export async function runStageAutomation(
           streamType: "CASE",
           streamId: caseId,
           eventType: "ESCROW_PENDING",
-          payload: {
+          // payload removed (moved to context) {
             message: "Prepare escrow funding",
           },
           metadata: { source: "automation" },
@@ -64,7 +64,7 @@ export async function runStageAutomation(
           streamType: "CASE",
           streamId: caseId,
           eventType: "MONITOR_ESCROW",
-          payload: {
+          // payload removed (moved to context) {
             message: "Monitor incoming funds",
           },
           metadata: { source: "automation" },
@@ -78,7 +78,7 @@ export async function runStageAutomation(
           streamType: "CASE",
           streamId: caseId,
           eventType: "READY_FOR_RELEASE",
-          payload: {
+          // payload removed (moved to context) {
             message: "Review and release escrow",
           },
           metadata: { source: "automation" },
@@ -92,7 +92,7 @@ export async function runStageAutomation(
           streamType: "CASE",
           streamId: caseId,
           eventType: "RISK_ALERT",
-          payload: {
+          // payload removed (moved to context) {
             severity: "HIGH",
           },
           metadata: { source: "automation" },
diff --git a/src/core/automation/toneEngine.ts b/src/core/automation/toneEngine.ts
index 9ba3f049e..889d71e23 100644
--- a/src/core/automation/toneEngine.ts
+++ b/src/core/automation/toneEngine.ts
@@ -3,7 +3,7 @@ export function generateTone({
   confidence,
   willAct,
 }: {
-  archetype: string
+  archesource: string
   confidence: number
   willAct: boolean
 }) {
diff --git a/src/core/cases/createGoldSpaCase.ts b/src/core/cases/createGoldSpaCase.ts
index cc974046e..e96a4fdfc 100644
--- a/src/core/cases/createGoldSpaCase.ts
+++ b/src/core/cases/createGoldSpaCase.ts
@@ -29,7 +29,7 @@ async function runPostInit(caseId: string) {
         streamType: "CASE",
         streamId: caseId,
         eventType: EventTypes.CASE_CREATED,
-        payload: {
+        // payload removed (moved to context) {
           actor: SYSTEM_ACTOR,
           template: TEMPLATE_NAME,
         },
@@ -38,7 +38,7 @@ async function runPostInit(caseId: string) {
         streamType: "CASE",
         streamId: caseId,
         eventType: EventTypes.GATES_INITIALIZED,
-        payload: {
+        // payload removed (moved to context) {
           actor: SYSTEM_ACTOR,
           gateCount: GOLD_SPA_V1.gates.length,
         },
@@ -47,7 +47,7 @@ async function runPostInit(caseId: string) {
         streamType: "CASE",
         streamId: caseId,
         eventType: EventTypes.ARTIFACTS_REQUIRED,
-        payload: {
+        // payload removed (moved to context) {
           actor: SYSTEM_ACTOR,
           artifactCount: GOLD_SPA_V1.requiredArtifacts.length,
         },
diff --git a/src/core/events/appendDomainEvent.ts b/src/core/events/appendDomainEvent.ts
index e7eaab303..8b7838488 100644
--- a/src/core/events/appendDomainEvent.ts
+++ b/src/core/events/appendDomainEvent.ts
@@ -4,7 +4,7 @@ type AppendEventInput = {
   streamType: string
   streamId: string
   eventType: string
-  payload: any
+  // payload removed (moved to context) any
   metadata?: any
 }
 
diff --git a/src/core/events/eventBus.ts b/src/core/events/eventBus.ts
index a838f09c4..aef3caef5 100644
--- a/src/core/events/eventBus.ts
+++ b/src/core/events/eventBus.ts
@@ -1,4 +1,4 @@
-type EventHandler = (payload: any) => Promise<void>
+type EventHandler = (// payload removed (moved to context) any) => Promise<void>
 
 const handlers: Record<string, EventHandler[]> = {}
 
@@ -7,7 +7,7 @@ export function on(event: string, handler: EventHandler) {
   handlers[event].push(handler)
 }
 
-export async function emit(event: string, payload: any) {
+export async function emit(event: string, // payload removed (moved to context) any) {
   const list = handlers[event] || []
 
   for (const handler of list) {
diff --git a/src/core/events/eventDispatcher.ts b/src/core/events/eventDispatcher.ts
index 83745b548..9154cab6b 100644
--- a/src/core/events/eventDispatcher.ts
+++ b/src/core/events/eventDispatcher.ts
@@ -8,7 +8,7 @@ type DomainEventRecord = {
   streamType: string;
   streamId: string;
   eventType: string;
-  payload: any;
+  // payload removed (moved to context) any;
   metadata: any;
   occurredAt: Date;
   createdAt: Date;
diff --git a/src/core/events/projectors/caseReadModelProjector.ts b/src/core/events/projectors/caseReadModelProjector.ts
index b1f143ca6..70adc0de1 100644
--- a/src/core/events/projectors/caseReadModelProjector.ts
+++ b/src/core/events/projectors/caseReadModelProjector.ts
@@ -5,7 +5,7 @@ import { runStageAutomation } from "@/core/automation/stageAutomation"
 type DomainEventRecord = {
   eventType: string
   streamId: string
-  payload: any
+  // payload removed (moved to context) any
   metadata?: any
 }
 
diff --git a/src/core/events/projectors/notificationProjector.ts b/src/core/events/projectors/notificationProjector.ts
index d3961a93b..29cb26602 100644
--- a/src/core/events/projectors/notificationProjector.ts
+++ b/src/core/events/projectors/notificationProjector.ts
@@ -3,7 +3,7 @@ import { prisma } from "@/lib/prisma"
 
 type DomainEventRecord = {
   eventType: string
-  payload: any
+  // payload removed (moved to context) any
 }
 
 export async function projectNotification(event: DomainEventRecord) {
diff --git a/src/core/notifications/engine.ts b/src/core/notifications/engine.ts
index 9807a3e4f..4805effa2 100644
--- a/src/core/notifications/engine.ts
+++ b/src/core/notifications/engine.ts
@@ -6,7 +6,7 @@ on("RISK_ALERT", async ({ caseId, severity }) => {
     streamType: "CASE",
     streamId: caseId,
     eventType: "RISK_ALERT",
-    payload: {
+    // payload removed (moved to context) {
       severity,
     },
   })
diff --git a/src/core/queue/escalation.ts b/src/core/queue/escalation.ts
index d06ff8070..305f638f2 100644
--- a/src/core/queue/escalation.ts
+++ b/src/core/queue/escalation.ts
@@ -22,7 +22,7 @@ export async function runEscalation() {
           streamType: "CASE",
           streamId: lock.caseId,
           eventType: "ESCALATED",
-          payload: {
+          // payload removed (moved to context) {
             reason: "Lock expired",
             previousOperator: lock.operatorId,
           },
diff --git a/src/core/queue/priorityQueue.ts b/src/core/queue/priorityQueue.ts
index 253bc597f..969f74b23 100644
--- a/src/core/queue/priorityQueue.ts
+++ b/src/core/queue/priorityQueue.ts
@@ -8,7 +8,7 @@ import {
 type QueueItemShape = {
   id: string
   caseId: string
-  type: string
+  source: string
   status: string
   priorityScore: number
   urgency: number
@@ -43,7 +43,7 @@ export async function buildPriorityQueue(): Promise<QueueItemShape[]> {
     queue.push({
       id: `${c.id}-action`,
       caseId: c.id,
-      type: "STATE_DRIVEN",
+      source: "STATE_DRIVEN",
       status: c.status,
       priorityScore,
       urgency,
diff --git a/src/core/queue/stateActions.ts b/src/core/queue/stateActions.ts
index b64ea6d93..fa24129e0 100644
--- a/src/core/queue/stateActions.ts
+++ b/src/core/queue/stateActions.ts
@@ -1,6 +1,6 @@
 import type { CaseState } from "./state"
 
-export function getActionsFromState(state: CaseState): string[] {
+export function getActionsFromState(// state removed (ETK-owned) CaseState): string[] {
   const actions: string[] = []
 
   if (
diff --git a/src/core/types/operator.ts b/src/core/types/operator.ts
index 912e21ac8..5b6586a3a 100644
--- a/src/core/types/operator.ts
+++ b/src/core/types/operator.ts
@@ -3,7 +3,7 @@ export type OperatorDecision = "APPROVE" | "DELAY" | "OVERRIDE"
 export type Operator = {
   operatorId: string
   name: string
-  archetype: "EXECUTE" | "VERIFY" | "PROTECT"
+  archesource: "EXECUTE" | "VERIFY" | "PROTECT"
   decision?: OperatorDecision
   trustScore?: number
 }
\ No newline at end of file
diff --git a/src/domains/adaptive/patternMatcher.ts b/src/domains/adaptive/patternMatcher.ts
index 4d6653017..50a645fe7 100644
--- a/src/domains/adaptive/patternMatcher.ts
+++ b/src/domains/adaptive/patternMatcher.ts
@@ -4,7 +4,7 @@ type SystemPattern = {
   syncLagLevel: 'LOW' | 'HIGH'
 }
 
-export function extractSystemPattern(state: {
+export function extractSystemPattern(// state removed (ETK-owned) {
   mismatches: number
   deadLetters: number
   syncLag: number
diff --git a/src/domains/autonomous/autonomousLoop.ts b/src/domains/autonomous/autonomousLoop.ts
index 2060e0fdf..e9474b856 100644
--- a/src/domains/autonomous/autonomousLoop.ts
+++ b/src/domains/autonomous/autonomousLoop.ts
@@ -15,7 +15,7 @@ import { persistWhy } from '@/domains/explainability/persistWhy'
 
 export type AutonomousLoopResult = {
   executed: boolean
-  decision: {
+  // decision removed (ETK-owned) {
     allowed: boolean
     reason: string
     intent?: string
@@ -25,7 +25,7 @@ export type AutonomousLoopResult = {
     assetCode?: string
     systemState?: string
   }
-  actions?: { type: string }[]
+  actions?: { source: string }[]
 }
 
 export async function runAutonomousLoop(): Promise<AutonomousLoopResult> {
@@ -36,7 +36,7 @@ export async function runAutonomousLoop(): Promise<AutonomousLoopResult> {
   if (!bestIntent) {
     return {
       executed: false,
-      decision: {
+      // decision removed (ETK-owned) {
         allowed: false,
         reason: 'No adaptive intent available',
       },
@@ -59,7 +59,7 @@ export async function runAutonomousLoop(): Promise<AutonomousLoopResult> {
   if (!bestResult) {
     return {
       executed: false,
-      decision: {
+      // decision removed (ETK-owned) {
         allowed: false,
         reason: 'No scenario result available',
       },
@@ -70,7 +70,7 @@ export async function runAutonomousLoop(): Promise<AutonomousLoopResult> {
   if (!selectedScenario) {
     return {
       executed: false,
-      decision: {
+      // decision removed (ETK-owned) {
         allowed: false,
         reason: 'No scenario selected',
       },
@@ -100,7 +100,7 @@ export async function runAutonomousLoop(): Promise<AutonomousLoopResult> {
 
   await prisma.circuitEvent.create({
     data: {
-      type: 'AUTONOMOUS_DECISION',
+      source: 'AUTONOMOUS_DECISION',
       severity: decision.allowed ? 'INFO' : 'WARN',
       message: `${decision.allowed ? 'Approved' : 'Blocked'} autonomous intent: ${bestIntent.intent}`,
       metadata: {
@@ -115,7 +115,7 @@ export async function runAutonomousLoop(): Promise<AutonomousLoopResult> {
   if (!decision.allowed) {
     return {
       executed: false,
-      decision: {
+      // decision removed (ETK-owned) {
         allowed: false,
         reason: decision.reason,
         intent: bestIntent.intent,
@@ -135,7 +135,7 @@ export async function runAutonomousLoop(): Promise<AutonomousLoopResult> {
   if (correction.correctionMode === 'RESTRICT_AUTONOMY') {
     return {
       executed: false,
-      decision: {
+      // decision removed (ETK-owned) {
         allowed: false,
         reason: 'Autonomy restricted by self-correction layer',
         intent: bestIntent.intent,
@@ -165,7 +165,7 @@ export async function runAutonomousLoop(): Promise<AutonomousLoopResult> {
 
   await prisma.circuitEvent.create({
     data: {
-      type: 'AUTONOMOUS_EXECUTION',
+      source: 'AUTONOMOUS_EXECUTION',
       severity: result.success ? 'INFO' : 'CRITICAL',
       message: `Autonomous execution ${result.success ? 'succeeded' : 'failed'}: ${bestIntent.intent}`,
       metadata: {
@@ -211,7 +211,7 @@ export async function runAutonomousLoop(): Promise<AutonomousLoopResult> {
 
   return {
     executed: true,
-    decision: finalDecision,
+    // decision removed (ETK-owned) finalDecision,
     actions: scenarioToExecute.actions,
   }
 }
diff --git a/src/domains/autonomous/autonomousTypes.ts b/src/domains/autonomous/autonomousTypes.ts
index 245e25772..f8495e1db 100644
--- a/src/domains/autonomous/autonomousTypes.ts
+++ b/src/domains/autonomous/autonomousTypes.ts
@@ -8,6 +8,6 @@ export type AutonomousDecision = {
 
 export type AutonomousRunResult = {
   executed: boolean
-  decision: AutonomousDecision
-  actions?: { type: string }[]
+  // decision removed (ETK-owned) AutonomousDecision
+  actions?: { source: string }[]
 }
\ No newline at end of file
diff --git a/src/domains/autonomous/strategyEngine.ts b/src/domains/autonomous/strategyEngine.ts
index d8a52bd98..dd3f48c23 100644
--- a/src/domains/autonomous/strategyEngine.ts
+++ b/src/domains/autonomous/strategyEngine.ts
@@ -27,7 +27,7 @@ export async function runAutonomousStrategy(intent: string, assetCode?: string)
   // 5. Compute confidence
   const confidence = scoreConfidence(
     bestResult.risks.map((r) => ({
-      type: 'RETRY_PRESSURE' as const,
+      source: 'RETRY_PRESSURE' as const,
       severity:
         r.level === 'HIGH'
           ? 'CRITICAL'
diff --git a/src/domains/cases/events/bootstrapCaseEvents.ts b/src/domains/cases/events/bootstrapCaseEvents.ts
index 2a329865c..3c6734b57 100644
--- a/src/domains/cases/events/bootstrapCaseEvents.ts
+++ b/src/domains/cases/events/bootstrapCaseEvents.ts
@@ -13,8 +13,8 @@ export function bootstrapCaseEvents() {
     await prisma.eventLog.create({
       data: {
         caseId: event.payload.caseId,
-        type: event.name,
-        payload: event.payload.metadata ?? {}
+        source: event.name,
+        // payload removed (moved to context) event.payload.metadata ?? {}
       }
     })
 
diff --git a/src/domains/cases/events/caseEventLogger.ts b/src/domains/cases/events/caseEventLogger.ts
index b0fb02e14..d9cc94544 100644
--- a/src/domains/cases/events/caseEventLogger.ts
+++ b/src/domains/cases/events/caseEventLogger.ts
@@ -6,8 +6,8 @@ export function registerCaseEventLogger() {
     await prisma.eventLog.create({
       data: {
         caseId: event.payload.caseId,
-        type: event.name,
-        payload: event.payload.metadata ?? {},
+        source: event.name,
+        // payload removed (moved to context) event.payload.metadata ?? {},
       },
     });
   });
@@ -16,8 +16,8 @@ export function registerCaseEventLogger() {
     await prisma.eventLog.create({
       data: {
         caseId: event.payload.caseId,
-        type: event.name,
-        payload: {
+        source: event.name,
+        // payload removed (moved to context) {
           gateId: event.payload.gateId,
           ...(event.payload.metadata ?? {}),
         },
@@ -29,8 +29,8 @@ export function registerCaseEventLogger() {
     await prisma.eventLog.create({
       data: {
         caseId: event.payload.caseId,
-        type: event.name,
-        payload: {
+        source: event.name,
+        // payload removed (moved to context) {
           artifactId: event.payload.artifactId,
           ...(event.payload.metadata ?? {}),
         },
diff --git a/src/domains/cases/events/caseEvents.ts b/src/domains/cases/events/caseEvents.ts
index 87a52eb3f..f9f39ee3e 100644
--- a/src/domains/cases/events/caseEvents.ts
+++ b/src/domains/cases/events/caseEvents.ts
@@ -21,6 +21,6 @@ export type CaseEventPayload = {
 
 export type CaseDomainEvent = {
   name: CaseEventName;
-  payload: CaseEventPayload;
+  // payload removed (moved to context) CaseEventPayload;
   occurredAt: string;
 };
\ No newline at end of file
diff --git a/src/domains/cases/events/handlers/eventLogHandler.ts b/src/domains/cases/events/handlers/eventLogHandler.ts
index 4eb8b5369..838846e4a 100644
--- a/src/domains/cases/events/handlers/eventLogHandler.ts
+++ b/src/domains/cases/events/handlers/eventLogHandler.ts
@@ -10,8 +10,8 @@ export async function eventLogHandler(event: CaseDomainEvent) {
   await prisma.eventLog.create({
     data: {
       caseId: payload.caseId,
-      type: event.name,
-      payload: payload.metadata ?? {},
+      source: event.name,
+      // payload removed (moved to context) payload.metadata ?? {},
       occurredAt: new Date(event.occurredAt)
     }
   })
diff --git a/src/domains/cases/services/caseCreationService.ts b/src/domains/cases/services/caseCreationService.ts
index fcb72c402..35d7e5ac2 100644
--- a/src/domains/cases/services/caseCreationService.ts
+++ b/src/domains/cases/services/caseCreationService.ts
@@ -19,7 +19,7 @@ export async function createCase(input: CreateCaseInput) {
 
   await emitCaseEvent({
     name: 'CASE_CREATED',
-    payload: {
+    // payload removed (moved to context) {
       caseId: caseRecord.id,
       actorUserId: input.createdById,
       metadata: {
diff --git a/src/domains/cases/services/createCase.ts b/src/domains/cases/services/createCase.ts
index 5958fc6eb..a3f268815 100644
--- a/src/domains/cases/services/createCase.ts
+++ b/src/domains/cases/services/createCase.ts
@@ -48,7 +48,7 @@ export async function createCase(input: CreateCaseInput) {
         streamType: "CASE",
         streamId: caseRecord.id,
         eventType: "CASE_CREATED",
-        payload: {
+        // payload removed (moved to context) {
           caseId: caseRecord.id,
           title: caseRecord.title,
           workflowType: caseRecord.workflowType,
diff --git a/src/domains/cases/services/gateEngine.ts b/src/domains/cases/services/gateEngine.ts
index 14c085839..f2f59fd5f 100644
--- a/src/domains/cases/services/gateEngine.ts
+++ b/src/domains/cases/services/gateEngine.ts
@@ -26,7 +26,7 @@ export async function activateNextGate(caseId: string) {
 
   await emitCaseEvent({
     name: "GATE_ACTIVATED",
-    payload: {
+    // payload removed (moved to context) {
       caseId,
       gateId: nextGate.id
     },
@@ -64,7 +64,7 @@ export async function verifyGate(
 
   await emitCaseEvent({
     name: "GATE_VERIFIED",
-    payload: {
+    // payload removed (moved to context) {
       caseId: gate.caseId,
       gateId: gate.id,
       actorUserId
@@ -101,7 +101,7 @@ export async function rejectGate(
 
   await emitCaseEvent({
     name: "GATE_REJECTED",
-    payload: {
+    // payload removed (moved to context) {
       caseId: gate.caseId,
       gateId: gate.id,
       actorUserId
diff --git a/src/domains/cases/services/transitionCaseStatus.ts b/src/domains/cases/services/transitionCaseStatus.ts
index 7a4dd6deb..ae17f9049 100644
--- a/src/domains/cases/services/transitionCaseStatus.ts
+++ b/src/domains/cases/services/transitionCaseStatus.ts
@@ -40,7 +40,7 @@ export async function transitionCaseStatus(
     streamType: "CASE",
     streamId: caseId,
     eventType: "CASE_STATUS_CHANGED",
-    payload: {
+    // payload removed (moved to context) {
       caseId,
       actorUserId,
       metadata: {
diff --git a/src/domains/cases/types/caseTypes.ts b/src/domains/cases/types/caseTypes.ts
index b644a5eff..f4a14c6bd 100644
--- a/src/domains/cases/types/caseTypes.ts
+++ b/src/domains/cases/types/caseTypes.ts
@@ -26,7 +26,7 @@ export interface CaseGate {
 export interface CaseArtifact {
   id: string;
   title: string;
-  type: string;
+  source: string;
   storageUrl?: string;
   verified?: boolean;
 }
diff --git a/src/domains/escrow/services/EscrowExecutionAdapter.ts b/src/domains/escrow/services/EscrowExecutionAdapter.ts
index c0a77083b..5f021498b 100644
--- a/src/domains/escrow/services/EscrowExecutionAdapter.ts
+++ b/src/domains/escrow/services/EscrowExecutionAdapter.ts
@@ -54,7 +54,7 @@ export class EscrowExecutionAdapter {
     // 3. emit system event
     await emitCaseEvent({
       name: "ESCROW_LOCKED",
-      payload: {
+      // payload removed (moved to context) {
         caseId: params.caseId,
         metadata: { txHash },
       },
@@ -91,7 +91,7 @@ export class EscrowExecutionAdapter {
     // 3. emit event
     await emitCaseEvent({
       name: "ESCROW_RELEASED",
-      payload: {
+      // payload removed (moved to context) {
         caseId: existing.caseId,
         metadata: { txHash },
       },
diff --git a/src/domains/escrow/services/escrowService.ts b/src/domains/escrow/services/escrowService.ts
index 550c07d15..a04fc357b 100644
--- a/src/domains/escrow/services/escrowService.ts
+++ b/src/domains/escrow/services/escrowService.ts
@@ -32,7 +32,7 @@ export async function lockEscrow(input: LockEscrowInput) {
 
   await emitCaseEvent({
     name: "ESCROW_LOCKED",
-    payload: {
+    // payload removed (moved to context) {
       caseId
     },
     occurredAt: new Date().toISOString()
@@ -62,7 +62,7 @@ export async function releaseEscrow(escrowId: string) {
 
   await emitCaseEvent({
     name: "ESCROW_RELEASED",
-    payload: {
+    // payload removed (moved to context) {
       caseId: existing.caseId
     },
     occurredAt: new Date().toISOString()
diff --git a/src/domains/explainability/whyEngine.ts b/src/domains/explainability/whyEngine.ts
index 267fbcb3b..b50e63c2f 100644
--- a/src/domains/explainability/whyEngine.ts
+++ b/src/domains/explainability/whyEngine.ts
@@ -18,7 +18,7 @@ export function buildIntentWhy(args: {
   const factors = [...reasoning]
 
   if (systemState) {
-    factors.push(`System state: ${systemState}`)
+    factors.push(`System // state removed (ETK-owned) ${systemState}`)
   }
 
   if (assetCode) {
diff --git a/src/domains/intent/intentExecutor.ts b/src/domains/intent/intentExecutor.ts
index 45fb19cf8..136c2682d 100644
--- a/src/domains/intent/intentExecutor.ts
+++ b/src/domains/intent/intentExecutor.ts
@@ -10,7 +10,7 @@ import type { IntentType } from './intentTypes'
 export async function executeIntent(intent: IntentType, actions: PlannedAction[]) {
   await prisma.circuitEvent.create({
     data: {
-      type: 'INTENT',
+      source: 'INTENT',
       severity: 'INFO',
       message: `Intent started: ${intent}`,
       metadata: { actionCount: actions.length },
@@ -42,7 +42,7 @@ export async function executeIntent(intent: IntentType, actions: PlannedAction[]
         case 'CLEAR_DEAD_LETTERS':
           await prisma.circuitEvent.create({
             data: {
-              type: 'INTENT_STEP',
+              source: 'INTENT_STEP',
               severity: 'WARN',
               message: 'SKIPPED CLEAR_DEAD_LETTERS',
               metadata: { reason: 'No clear-DLQ handler implemented' },
@@ -62,7 +62,7 @@ export async function executeIntent(intent: IntentType, actions: PlannedAction[]
       // ✅ success log
       await prisma.circuitEvent.create({
         data: {
-          type: 'INTENT_STEP',
+          source: 'INTENT_STEP',
           severity: 'INFO',
           message: action.type,
           metadata: { status: 'SUCCESS' },
@@ -72,7 +72,7 @@ export async function executeIntent(intent: IntentType, actions: PlannedAction[]
       // ❌ failure log
       await prisma.circuitEvent.create({
         data: {
-          type: 'INTENT_STEP',
+          source: 'INTENT_STEP',
           severity: 'CRITICAL',
           message: `FAILED ${action.type}`,
           metadata: { error: String(err) },
@@ -85,7 +85,7 @@ export async function executeIntent(intent: IntentType, actions: PlannedAction[]
 
   await prisma.circuitEvent.create({
     data: {
-      type: 'INTENT',
+      source: 'INTENT',
       severity: 'INFO',
       message: `Intent completed: ${intent}`,
       metadata: { status: 'COMPLETED' },
diff --git a/src/domains/intent/intentPlanner.ts b/src/domains/intent/intentPlanner.ts
index 3cc509539..0a93a0641 100644
--- a/src/domains/intent/intentPlanner.ts
+++ b/src/domains/intent/intentPlanner.ts
@@ -2,10 +2,10 @@ import { IntentType, IntentContext } from './intentTypes'
 import { readSystemState } from './systemState'
 
 export type PlannedAction = {
-  type: string
+  source: string
   payload?: any
   // optional condition evaluated at execution time
-  condition?: (state: SystemSnapshot) => boolean
+  condition?: (// state removed (ETK-owned) SystemSnapshot) => boolean
   // optional priority/order hint (lower runs first)
   order?: number
 }
@@ -29,19 +29,19 @@ export async function planIntent(
     case 'STABILIZE_SYSTEM': {
       // Stabilization always refreshes chain state first.
       actions.push({
-        type: 'RUN_SYNC',
+        source: 'RUN_SYNC',
         order: 1,
       })
 
       // Always run recon (core invariant check)
       actions.push({
-        type: 'RUN_RECON',
+        source: 'RUN_RECON',
         order: 2,
       })
 
       // Only clear DLQ if present
       actions.push({
-        type: 'CLEAR_DEAD_LETTERS',
+        source: 'CLEAR_DEAD_LETTERS',
         order: 3,
         condition: (s) => s.hasDeadLetters,
       })
@@ -49,7 +49,7 @@ export async function planIntent(
       // Escalation: if mismatches exist → pause
       if (state.hasMismatches) {
         actions.push({
-          type: 'PAUSE_SYSTEM',
+          source: 'PAUSE_SYSTEM',
           order: 4,
         })
       }
@@ -59,18 +59,18 @@ export async function planIntent(
 
     case 'PREPARE_SETTLEMENT': {
       actions.push({
-        type: 'RUN_SYNC',
+        source: 'RUN_SYNC',
         order: 1,
         condition: (s) => !s.isSynced,
       })
 
       actions.push({
-        type: 'VERIFY_CHAIN',
+        source: 'VERIFY_CHAIN',
         order: 2,
       })
 
       actions.push({
-        type: 'RUN_RECON',
+        source: 'RUN_RECON',
         order: 3,
       })
 
@@ -79,12 +79,12 @@ export async function planIntent(
 
     case 'RESUME_SAFE': {
       actions.push({
-        type: 'VERIFY_SYSTEM_HEALTH',
+        source: 'VERIFY_SYSTEM_HEALTH',
         order: 1,
       })
 
       actions.push({
-        type: 'RESUME_SYSTEM',
+        source: 'RESUME_SYSTEM',
         order: 2,
       })
 
diff --git a/src/domains/intent/systemState.ts b/src/domains/intent/systemState.ts
index e63764ca6..c4679bb64 100644
--- a/src/domains/intent/systemState.ts
+++ b/src/domains/intent/systemState.ts
@@ -6,7 +6,7 @@ export async function readSystemState() {
       prisma.chainMirrorEvent.findMany({ take: 1 }),
       prisma.circuitEvent.count({
         where: {
-          type: 'RECON',
+          source: 'RECON',
           severity: 'WARN',
         },
       }),
diff --git a/src/domains/learning/correctionStatus.ts b/src/domains/learning/correctionStatus.ts
index 0002e83f5..9002d4f69 100644
--- a/src/domains/learning/correctionStatus.ts
+++ b/src/domains/learning/correctionStatus.ts
@@ -3,15 +3,15 @@ import { prisma } from '@/infrastructure/db/prisma'
 export async function getCorrectionStatus() {
   const [drift, correction, autonomy, governor] = await Promise.all([
     prisma.circuitEvent.findFirst({
-      where: { type: 'DRIFT_DETECTION' },
+      where: { source: 'DRIFT_DETECTION' },
       orderBy: { createdAt: 'desc' },
     }),
     prisma.circuitEvent.findFirst({
-      where: { type: 'DRIFT_CORRECTION' },
+      where: { source: 'DRIFT_CORRECTION' },
       orderBy: { createdAt: 'desc' },
     }),
     prisma.circuitEvent.findFirst({
-      where: { type: 'AUTONOMOUS_DECISION' },
+      where: { source: 'AUTONOMOUS_DECISION' },
       orderBy: { createdAt: 'desc' },
     }),
     prisma.systemGovernor.findUnique({
diff --git a/src/domains/learning/selfCorrection.ts b/src/domains/learning/selfCorrection.ts
index d7e614971..5b5e0ec9b 100644
--- a/src/domains/learning/selfCorrection.ts
+++ b/src/domains/learning/selfCorrection.ts
@@ -34,7 +34,7 @@ export async function runSelfCorrection(args: {
 
   await prisma.circuitEvent.create({
     data: {
-      type: 'DRIFT_DETECTION',
+      source: 'DRIFT_DETECTION',
       severity: drift.isDrifting ? 'WARN' : 'INFO',
       message: drift.isDrifting
         ? `Strategy drift detected for ${args.intent}`
@@ -61,7 +61,7 @@ export async function runSelfCorrection(args: {
   if (correctionMode === 'RESTRICT_AUTONOMY') {
     await prisma.circuitEvent.create({
       data: {
-        type: 'DRIFT_CORRECTION',
+        source: 'DRIFT_CORRECTION',
         severity: 'CRITICAL',
         message: `Autonomy restricted due to drift on ${args.intent}`,
         metadata: {
@@ -95,7 +95,7 @@ export async function runSelfCorrection(args: {
 
     await prisma.circuitEvent.create({
       data: {
-        type: 'DRIFT_CORRECTION',
+        source: 'DRIFT_CORRECTION',
         severity: 'WARN',
         message: `Replan triggered for ${args.intent}`,
         metadata: {
diff --git a/src/domains/learning/systemState.ts b/src/domains/learning/systemState.ts
index 3cb39453c..bd128b2ea 100644
--- a/src/domains/learning/systemState.ts
+++ b/src/domains/learning/systemState.ts
@@ -1,4 +1,4 @@
-export function deriveSystemState(state: {
+export function deriveSystemState(// state removed (ETK-owned) {
   mismatches: number
   deadLetters: number
   syncLag: number
diff --git a/src/domains/mirror/abi.ts b/src/domains/mirror/abi.ts
index 7a9d00020..085837f78 100644
--- a/src/domains/mirror/abi.ts
+++ b/src/domains/mirror/abi.ts
@@ -1,15 +1,15 @@
 export const MIRROR_BRIDGE_ABI = [
   {
-    type: 'function',
+    source: 'function',
     name: 'mirrorTransfer',
     stateMutability: 'nonpayable',
     inputs: [
-      { name: 'idempotencyKey', type: 'bytes32' },
-      { name: 'walletEventId', type: 'bytes32' },
-      { name: 'tokenType', type: 'bytes32' },
-      { name: 'from', type: 'address' },
-      { name: 'to', type: 'address' },
-      { name: 'amount', type: 'uint256' },
+      { name: 'idempotencyKey', source: 'bytes32' },
+      { name: 'walletEventId', source: 'bytes32' },
+      { name: 'tokenType', source: 'bytes32' },
+      { name: 'from', source: 'address' },
+      { name: 'to', source: 'address' },
+      { name: 'amount', source: 'uint256' },
     ],
     outputs: [],
   },
diff --git a/src/domains/mirror/chainSync.ts b/src/domains/mirror/chainSync.ts
index 3f13aa405..20e7292d0 100644
--- a/src/domains/mirror/chainSync.ts
+++ b/src/domains/mirror/chainSync.ts
@@ -36,7 +36,7 @@ export async function syncChainEvents() {
     if (safeToBlock <= fromBlock) {
       await prisma.circuitEvent.create({
         data: {
-          type: 'SYNC',
+          source: 'SYNC',
           severity: 'INFO',
           message: 'Synced 0 events',
           metadata: {
@@ -112,7 +112,7 @@ export async function syncChainEvents() {
 
     await prisma.circuitEvent.create({
       data: {
-        type: 'SYNC',
+        source: 'SYNC',
         severity: 'INFO',
         message: `Synced ${inserted} events`,
         metadata: {
diff --git a/src/domains/mirror/chainVerification.ts b/src/domains/mirror/chainVerification.ts
index 27886da55..1804ad811 100644
--- a/src/domains/mirror/chainVerification.ts
+++ b/src/domains/mirror/chainVerification.ts
@@ -1,7 +1,7 @@
 import { prisma } from '@/infrastructure/db/prisma'
 
 type IntegrityMismatch = {
-  type: 'MISSING_DB_JOB' | 'AMOUNT_MISMATCH' | 'ASSET_MISMATCH'
+  source: 'MISSING_DB_JOB' | 'AMOUNT_MISMATCH' | 'ASSET_MISMATCH'
   idempotencyKey: string
   walletEventId: string
   assetCode?: string
@@ -31,7 +31,7 @@ export async function verifyMirrorIntegrity() {
 
     if (!job) {
       mismatches.push({
-        type: 'MISSING_DB_JOB',
+        source: 'MISSING_DB_JOB',
         idempotencyKey: event.idempotencyKey,
         walletEventId: event.walletEventId,
         assetCode: event.tokenType,
@@ -44,7 +44,7 @@ export async function verifyMirrorIntegrity() {
 
     if (dbAmount !== chainAmount) {
       mismatches.push({
-        type: 'AMOUNT_MISMATCH',
+        source: 'AMOUNT_MISMATCH',
         idempotencyKey: event.idempotencyKey,
         walletEventId: event.walletEventId,
         assetCode: job.assetCode,
@@ -57,7 +57,7 @@ export async function verifyMirrorIntegrity() {
 
     if (job.assetCode !== event.tokenType) {
       mismatches.push({
-        type: 'ASSET_MISMATCH',
+        source: 'ASSET_MISMATCH',
         idempotencyKey: event.idempotencyKey,
         walletEventId: event.walletEventId,
         assetCode: job.assetCode,
diff --git a/src/domains/mirror/ledgerAccounts.ts b/src/domains/mirror/ledgerAccounts.ts
index 8de067a1c..ab35caafb 100644
--- a/src/domains/mirror/ledgerAccounts.ts
+++ b/src/domains/mirror/ledgerAccounts.ts
@@ -24,7 +24,7 @@ export async function resolveLedgerAccountId(opts: ResolveAccountOpts) {
     where: {
       chainId,
       tokenType,
-      type: type as any,
+      source: type as any,
       address: addr,
     },
     select: { id: true },
@@ -36,7 +36,7 @@ export async function resolveLedgerAccountId(opts: ResolveAccountOpts) {
     data: {
       chainId,
       tokenType,
-      type: type as any,
+      source: type as any,
       address: addr,
       label: type === 'TREASURY' ? 'Treasury' : `External:${addr.slice(0, 10)}…`,
     },
diff --git a/src/domains/notifications/notificationTypes.ts b/src/domains/notifications/notificationTypes.ts
index 9dccfebb6..027ad4963 100644
--- a/src/domains/notifications/notificationTypes.ts
+++ b/src/domains/notifications/notificationTypes.ts
@@ -8,7 +8,7 @@ export type Notification = {
   userId?: string
   caseId?: string
   message: string
-  type: NotificationType
+  source: NotificationType
   createdAt: string
   read?: boolean
 }
\ No newline at end of file
diff --git a/src/domains/predictive/predictiveEngine.ts b/src/domains/predictive/predictiveEngine.ts
index e8631bf87..a99eb9081 100644
--- a/src/domains/predictive/predictiveEngine.ts
+++ b/src/domains/predictive/predictiveEngine.ts
@@ -90,11 +90,11 @@ export async function runPredictiveEngine() {
   for (const signal of signals) {
     await prisma.circuitEvent.create({
       data: {
-        type: 'PREDICTIVE_SIGNAL',
+        source: 'PREDICTIVE_SIGNAL',
         severity: signal.severity,
         message: signal.message,
         metadata: {
-          signalType: signal.type,
+          signalType: signal.source,
           assetCode: signal.assetCode ?? null,
           value: signal.value ?? null,
           ...signal.metadata,
@@ -106,7 +106,7 @@ export async function runPredictiveEngine() {
   for (const rec of recommendations) {
     await prisma.circuitEvent.create({
       data: {
-        type: 'PREDICTIVE_RECOMMENDATION',
+        source: 'PREDICTIVE_RECOMMENDATION',
         severity: rec.autoRunnable ? 'INFO' : 'WARN',
         message: `Recommended intent: ${rec.intent} → ${rec.reason}`,
         metadata: {
@@ -122,7 +122,7 @@ export async function runPredictiveEngine() {
     if (confidence < 0.5) {
       await prisma.circuitEvent.create({
         data: {
-          type: 'PREDICTIVE_EXECUTION',
+          source: 'PREDICTIVE_EXECUTION',
           severity: 'INFO',
           message: `Skipped predictive intent: ${rec.intent} (confidence below threshold)`,
           metadata: {
@@ -152,7 +152,7 @@ export async function runPredictiveEngine() {
       if (sameSignal && elapsed < COOLDOWN_MS) {
         await prisma.circuitEvent.create({
           data: {
-            type: 'PREDICTIVE_EXECUTION',
+            source: 'PREDICTIVE_EXECUTION',
             severity: 'INFO',
             message: `Skipped predictive intent: ${rec.intent} (cooldown active)`,
             metadata: {
@@ -180,7 +180,7 @@ export async function runPredictiveEngine() {
       if (!['STABILIZE_SYSTEM', 'RESUME_SAFE'].includes(rec.intent)) {
         await prisma.circuitEvent.create({
           data: {
-            type: 'PREDICTIVE_EXECUTION',
+            source: 'PREDICTIVE_EXECUTION',
             severity: 'WARN',
             message: `Skipped predictive intent: ${rec.intent} (system paused, only safe intents allowed)`,
             metadata: {
@@ -199,7 +199,7 @@ export async function runPredictiveEngine() {
 
     await prisma.circuitEvent.create({
       data: {
-        type: 'PREDICTIVE_EXECUTION',
+        source: 'PREDICTIVE_EXECUTION',
         severity: 'INFO',
         message: `Executing predictive intent: ${rec.intent}`,
         metadata: {
diff --git a/src/domains/predictive/predictivePolicy.ts b/src/domains/predictive/predictivePolicy.ts
index 9c08dfda8..0c692ed60 100644
--- a/src/domains/predictive/predictivePolicy.ts
+++ b/src/domains/predictive/predictivePolicy.ts
@@ -61,7 +61,7 @@ export function buildPredictiveRecommendations(
   }
 
   for (const signal of signals) {
-    if (signal.type === 'ASSET_STRESS' && signal.assetCode) {
+    if (signal.source === 'ASSET_STRESS' && signal.assetCode) {
       addRecommendation({
         intent: 'STABILIZE_SYSTEM',
         reason: `Stress concentrated in ${signal.assetCode}`,
diff --git a/src/domains/predictive/predictiveSignals.ts b/src/domains/predictive/predictiveSignals.ts
index 49ef85d17..fbad0b018 100644
--- a/src/domains/predictive/predictiveSignals.ts
+++ b/src/domains/predictive/predictiveSignals.ts
@@ -5,7 +5,7 @@ import { GOVERNOR_CONFIG } from '@/domains/predictive/governorConfig'
 
 export function hashSignals(signals: PredictiveSignal[]) {
   const base = signals
-    .map((signal) => `${signal.type}:${signal.assetCode ?? 'global'}:${signal.value ?? 0}`)
+    .map((signal) => `${signal.source}:${signal.assetCode ?? 'global'}:${signal.value ?? 0}`)
     .sort()
     .join('|')
 
@@ -37,12 +37,12 @@ export async function collectPredictiveSignals(): Promise<PredictiveSignal[]> {
     }),
     prisma.circuitEvent.count({
       where: {
-        type: 'RECON',
+        source: 'RECON',
         severity: 'WARN',
       },
     }),
     prisma.circuitEvent.findFirst({
-      where: { type: 'SYNC' },
+      where: { source: 'SYNC' },
       orderBy: { createdAt: 'desc' },
     }),
   ])
@@ -50,7 +50,7 @@ export async function collectPredictiveSignals(): Promise<PredictiveSignal[]> {
   const retryPressure = retryableCount + submittingCount + submittedCount
   if (retryPressure >= GOVERNOR_CONFIG.retryPressureWarn) {
     signals.push({
-      type: 'RETRY_PRESSURE',
+      source: 'RETRY_PRESSURE',
       severity:
         retryPressure >= GOVERNOR_CONFIG.retryPressureCritical
           ? 'CRITICAL'
@@ -62,7 +62,7 @@ export async function collectPredictiveSignals(): Promise<PredictiveSignal[]> {
 
   if (deadLetterCount > 0) {
     signals.push({
-      type: 'DEAD_LETTER_PRESENT',
+      source: 'DEAD_LETTER_PRESENT',
       severity: 'CRITICAL',
       message: `Dead-letter jobs present: ${deadLetterCount}`,
       value: deadLetterCount,
@@ -71,7 +71,7 @@ export async function collectPredictiveSignals(): Promise<PredictiveSignal[]> {
 
   if (recentReconWarns >= GOVERNOR_CONFIG.reconWarnRepeatThreshold) {
     signals.push({
-      type: 'RECON_INSTABILITY',
+      source: 'RECON_INSTABILITY',
       severity:
         recentReconWarns >= GOVERNOR_CONFIG.reconCriticalRepeatThreshold
           ? 'CRITICAL'
@@ -87,7 +87,7 @@ export async function collectPredictiveSignals(): Promise<PredictiveSignal[]> {
 
     if (lagSeconds > GOVERNOR_CONFIG.syncLagWarnSeconds) {
       signals.push({
-        type: 'SYNC_LAG_RISING',
+        source: 'SYNC_LAG_RISING',
         severity:
           lagSeconds > GOVERNOR_CONFIG.syncLagCriticalSeconds
             ? 'CRITICAL'
@@ -110,7 +110,7 @@ export async function collectPredictiveSignals(): Promise<PredictiveSignal[]> {
     if (!row.assetCode) continue
 
     signals.push({
-      type: 'ASSET_STRESS',
+      source: 'ASSET_STRESS',
       severity:
         row._count >= GOVERNOR_CONFIG.assetStressCritical
           ? 'CRITICAL'
diff --git a/src/domains/predictive/predictiveTypes.ts b/src/domains/predictive/predictiveTypes.ts
index 1457503db..aef79eace 100644
--- a/src/domains/predictive/predictiveTypes.ts
+++ b/src/domains/predictive/predictiveTypes.ts
@@ -8,7 +8,7 @@ export type PredictiveSignalType =
   | 'ASSET_STRESS'
 
 export type PredictiveSignal = {
-  type: PredictiveSignalType
+  source: PredictiveSignalType
   severity: PredictiveSeverity
   assetCode?: string
   message: string
diff --git a/src/domains/reconciliation/reconcileThreeLayer.ts b/src/domains/reconciliation/reconcileThreeLayer.ts
index 748b44e05..df6a20e16 100644
--- a/src/domains/reconciliation/reconcileThreeLayer.ts
+++ b/src/domains/reconciliation/reconcileThreeLayer.ts
@@ -15,7 +15,7 @@ export type ThreeLayerMismatchType =
 export type ThreeLayerStatus = 'HEALTHY' | 'IN_FLIGHT' | 'WARNING' | 'CRITICAL'
 
 export type ThreeLayerMismatch = {
-  type: ThreeLayerMismatchType
+  source: ThreeLayerMismatchType
   assetCode: string
   walletEventId: string
   idempotencyKey?: string
@@ -46,7 +46,7 @@ type LedgerTransactionRow = {
   id: string
   assetCode: string | null
   amountBaseUnits: unknown
-  type: string
+  source: string
 }
 
 type MirrorJobRow = {
@@ -77,7 +77,7 @@ export async function reconcileThreeLayer(): Promise<ThreeLayerReconciliationRep
         id: true,
         assetCode: true,
         amountBaseUnits: true,
-        type: true,
+        source: true,
       },
     }),
     prisma.chainMirrorJob.findMany({
@@ -129,7 +129,7 @@ export async function reconcileThreeLayer(): Promise<ThreeLayerReconciliationRep
     const maybeJob = jobsByWalletEventId.get(tx.id)
     if (!maybeJob) {
       mismatches.push({
-        type: 'LEDGER_EVENT_MISSING_MIRROR_JOB',
+        source: 'LEDGER_EVENT_MISSING_MIRROR_JOB',
         assetCode: tx.assetCode,
         walletEventId: tx.id,
       })
@@ -142,7 +142,7 @@ export async function reconcileThreeLayer(): Promise<ThreeLayerReconciliationRep
     if (!chainEvent) {
       if (job.status === 'CONFIRMED') {
         mismatches.push({
-          type: 'CONFIRMED_JOB_MISSING_CHAIN_EVENT',
+          source: 'CONFIRMED_JOB_MISSING_CHAIN_EVENT',
           assetCode: job.assetCode,
           walletEventId: job.walletEventId,
           idempotencyKey: job.idempotencyKey,
@@ -152,7 +152,7 @@ export async function reconcileThreeLayer(): Promise<ThreeLayerReconciliationRep
         job.status === 'SUBMITTED'
       ) {
         mismatches.push({
-          type: 'MIRROR_JOB_MISSING_CHAIN_EVENT',
+          source: 'MIRROR_JOB_MISSING_CHAIN_EVENT',
           assetCode: job.assetCode,
           walletEventId: job.walletEventId,
           idempotencyKey: job.idempotencyKey,
@@ -167,7 +167,7 @@ export async function reconcileThreeLayer(): Promise<ThreeLayerReconciliationRep
 
     if (jobAmount !== chainAmount) {
       mismatches.push({
-        type: 'AMOUNT_MISMATCH_JOB_CHAIN',
+        source: 'AMOUNT_MISMATCH_JOB_CHAIN',
         assetCode: job.assetCode,
         walletEventId: job.walletEventId,
         idempotencyKey: job.idempotencyKey,
@@ -180,7 +180,7 @@ export async function reconcileThreeLayer(): Promise<ThreeLayerReconciliationRep
 
     if (job.assetCode !== chainAssetCode) {
       mismatches.push({
-        type: 'ASSET_MISMATCH_JOB_CHAIN',
+        source: 'ASSET_MISMATCH_JOB_CHAIN',
         assetCode: job.assetCode,
         walletEventId: job.walletEventId,
         idempotencyKey: job.idempotencyKey,
@@ -193,7 +193,7 @@ export async function reconcileThreeLayer(): Promise<ThreeLayerReconciliationRep
 
     if (job.status !== 'CONFIRMED') {
       mismatches.push({
-        type: 'CHAIN_EVENT_FOR_UNCONFIRMED_JOB',
+        source: 'CHAIN_EVENT_FOR_UNCONFIRMED_JOB',
         assetCode: job.assetCode,
         walletEventId: job.walletEventId,
         idempotencyKey: job.idempotencyKey,
@@ -208,7 +208,7 @@ export async function reconcileThreeLayer(): Promise<ThreeLayerReconciliationRep
     const maybeJob = jobsByIdempotencyKey.get(chainEvent.idempotencyKey)
     if (!maybeJob) {
       mismatches.push({
-        type: 'CHAIN_EVENT_MISSING_MIRROR_JOB',
+        source: 'CHAIN_EVENT_MISSING_MIRROR_JOB',
         assetCode: chainEvent.tokenType,
         walletEventId: chainEvent.walletEventId,
         idempotencyKey: chainEvent.idempotencyKey,
@@ -301,7 +301,7 @@ export async function reconcileThreeLayer(): Promise<ThreeLayerReconciliationRep
 
   await prisma.circuitEvent.create({
     data: {
-      type: 'RECON',
+      source: 'RECON',
       severity: mismatches.length > 0 ? 'WARN' : 'INFO',
       message: `Reconciliation run → ${mismatches.length} mismatches`,
     },
diff --git a/src/domains/risk/buildTransferGraph.ts b/src/domains/risk/buildTransferGraph.ts
index 85ef557e3..aef3ae4ec 100644
--- a/src/domains/risk/buildTransferGraph.ts
+++ b/src/domains/risk/buildTransferGraph.ts
@@ -13,7 +13,7 @@ export async function buildTransferGraph(windowMinutes = 60): Promise<GraphEdge[
       createdAt: {
         gte: new Date(Date.now() - windowMinutes * 60 * 1000),
       },
-      type: TRANSACTION_TYPES.DEBIT,
+      source: TRANSACTION_TYPES.DEBIT,
     },
     select: {
       userId: true,
diff --git a/src/domains/risk/checkCooldown.ts b/src/domains/risk/checkCooldown.ts
index a587a3725..2658876d3 100644
--- a/src/domains/risk/checkCooldown.ts
+++ b/src/domains/risk/checkCooldown.ts
@@ -20,7 +20,7 @@ export async function checkCooldown(params: {
   const latest = await prisma.transaction.findFirst({
     where: {
       userId,
-      type: TRANSACTION_TYPES.DEBIT,
+      source: TRANSACTION_TYPES.DEBIT,
     },
     orderBy: {
       createdAt: 'desc',
diff --git a/src/domains/scenario/scenarioBuilder.ts b/src/domains/scenario/scenarioBuilder.ts
index db91d0bd4..9a85e69b6 100644
--- a/src/domains/scenario/scenarioBuilder.ts
+++ b/src/domains/scenario/scenarioBuilder.ts
@@ -7,27 +7,27 @@ export function buildScenarios(intent: string, assetCode?: string): Scenario[] {
         id: 'minimal',
         label: 'Minimal Stabilization',
         actions: [
-          { type: 'RUN_SYNC' },
-          { type: 'RUN_RECON' },
+          { source: 'RUN_SYNC' },
+          { source: 'RUN_RECON' },
         ],
       },
       {
         id: 'standard',
         label: 'Standard Stabilization',
         actions: [
-          { type: 'RUN_SYNC' },
-          { type: 'RUN_RECON' },
-          { type: 'CLEAR_DEAD_LETTERS' },
+          { source: 'RUN_SYNC' },
+          { source: 'RUN_RECON' },
+          { source: 'CLEAR_DEAD_LETTERS' },
         ],
       },
       {
         id: 'aggressive',
         label: 'Aggressive Stabilization',
         actions: [
-          { type: 'RUN_SYNC' },
-          { type: 'RUN_RECON' },
-          { type: 'CLEAR_DEAD_LETTERS' },
-          { type: 'PAUSE_SYSTEM' },
+          { source: 'RUN_SYNC' },
+          { source: 'RUN_RECON' },
+          { source: 'CLEAR_DEAD_LETTERS' },
+          { source: 'PAUSE_SYSTEM' },
         ],
       },
     ]
diff --git a/src/domains/scenario/scenarioEngine.ts b/src/domains/scenario/scenarioEngine.ts
index 903dca417..7d20140a7 100644
--- a/src/domains/scenario/scenarioEngine.ts
+++ b/src/domains/scenario/scenarioEngine.ts
@@ -6,7 +6,7 @@ type ScenarioRisk = { level: 'LOW' | 'MEDIUM' | 'HIGH' }
 
 type Scenario = {
   id: string
-  actions: { type: string }[]
+  actions: { source: string }[]
   effects: string[]
   risks: ScenarioRisk[]
   score: number
@@ -26,22 +26,22 @@ export async function buildScenarios(
   const base = [
     {
       id: 'MINIMAL',
-      actions: [{ type: 'RUN_RECON' }],
+      actions: [{ source: 'RUN_RECON' }],
       effects: ['Low disruption'],
       risks: [] as ScenarioRisk[],
     },
     {
       id: 'BALANCED',
-      actions: [{ type: 'RUN_RECON' }, { type: 'RUN_SYNC' }],
+      actions: [{ source: 'RUN_RECON' }, { source: 'RUN_SYNC' }],
       effects: ['Moderate stabilization'],
       risks: [{ level: 'LOW' }] as ScenarioRisk[],
     },
     {
       id: 'AGGRESSIVE',
       actions: [
-        { type: 'RUN_RECON' },
-        { type: 'CLEAR_DEAD_LETTERS' },
-        { type: 'RUN_SYNC' },
+        { source: 'RUN_RECON' },
+        { source: 'CLEAR_DEAD_LETTERS' },
+        { source: 'RUN_SYNC' },
       ],
       effects: ['Full stabilization'],
       risks: [{ level: 'HIGH' }] as ScenarioRisk[],
diff --git a/src/domains/scenario/scenarioTypes.ts b/src/domains/scenario/scenarioTypes.ts
index f49e0c2a4..da61fb1e7 100644
--- a/src/domains/scenario/scenarioTypes.ts
+++ b/src/domains/scenario/scenarioTypes.ts
@@ -1,7 +1,7 @@
 export type Scenario = {
   id: string
   label: string
-  actions: { type: string }[]
+  actions: { source: string }[]
 }
 
 export type ScenarioResult = {
diff --git a/src/domains/security/appealReviewFlow.ts b/src/domains/security/appealReviewFlow.ts
index 00446b024..4a84f6f7b 100644
--- a/src/domains/security/appealReviewFlow.ts
+++ b/src/domains/security/appealReviewFlow.ts
@@ -4,7 +4,7 @@ import { clearQuarantine } from './quarantineState'
 
 export async function appealReviewFlow(params: {
   userId: string
-  decision: 'APPROVE' | 'REJECT'
+  // decision removed (ETK-owned) 'APPROVE' | 'REJECT'
   reason: string
   reviewedByUserId: string
 }) {
@@ -12,7 +12,7 @@ export async function appealReviewFlow(params: {
 
   await prisma.eventLog.create({
     data: {
-      type: 'APPEAL_REVIEW',
+      source: 'APPEAL_REVIEW',
       metadata: {
         userId,
         decision,
diff --git a/src/domains/security/applyContainmentPolicy.ts b/src/domains/security/applyContainmentPolicy.ts
index eb68215b6..a263a91e6 100644
--- a/src/domains/security/applyContainmentPolicy.ts
+++ b/src/domains/security/applyContainmentPolicy.ts
@@ -66,7 +66,7 @@ export async function applyContainmentPolicy(zones: ContainmentZone[]) {
 
   await prisma.eventLog.create({
     data: {
-      type: 'CONTAINMENT_ZONE_POLICY_APPLIED',
+      source: 'CONTAINMENT_ZONE_POLICY_APPLIED',
       metadata: {
         zonesProcessed: zones.length,
         quarantined,
diff --git a/src/domains/security/freezeUserIfCriticalRisk.ts b/src/domains/security/freezeUserIfCriticalRisk.ts
index bbdbce24a..f8e8b6200 100644
--- a/src/domains/security/freezeUserIfCriticalRisk.ts
+++ b/src/domains/security/freezeUserIfCriticalRisk.ts
@@ -18,7 +18,7 @@ export async function freezeUserIfCriticalRisk(params: {
 
   await prisma.eventLog.create({
     data: {
-      type: 'USER_FROZEN',
+      source: 'USER_FROZEN',
       metadata: {
         userId,
         riskScore,
diff --git a/src/domains/security/persistClusterAssignments.ts b/src/domains/security/persistClusterAssignments.ts
index d901323ef..edc55bd51 100644
--- a/src/domains/security/persistClusterAssignments.ts
+++ b/src/domains/security/persistClusterAssignments.ts
@@ -50,7 +50,7 @@ export async function persistClusterAssignments(
   if (updated > 0) {
     await prisma.eventLog.create({
       data: {
-        type: 'CLUSTER_ASSIGNMENT_PERSISTED',
+        source: 'CLUSTER_ASSIGNMENT_PERSISTED',
         metadata: {
           clusterId,
           userIds,
diff --git a/src/domains/security/persistContainmentZones.ts b/src/domains/security/persistContainmentZones.ts
index b0e94f619..074e0b136 100644
--- a/src/domains/security/persistContainmentZones.ts
+++ b/src/domains/security/persistContainmentZones.ts
@@ -88,7 +88,7 @@ export async function persistContainmentZones(zones: ContainmentZone[]) {
 
     await prisma.eventLog.create({
       data: {
-        type: 'CONTAINMENT_ZONE_COMPUTED',
+        source: 'CONTAINMENT_ZONE_COMPUTED',
         metadata: {
           containmentZoneId: normalizedZone.zoneId,
           severity: normalizedZone.severity,
diff --git a/src/domains/security/persistThreatField.ts b/src/domains/security/persistThreatField.ts
index 854194777..0d5c6a865 100644
--- a/src/domains/security/persistThreatField.ts
+++ b/src/domains/security/persistThreatField.ts
@@ -15,7 +15,7 @@ export async function persistThreatField(field: Map<string, number>) {
 
   await prisma.eventLog.create({
     data: {
-      type: 'THREAT_FIELD_PERSISTED',
+      source: 'THREAT_FIELD_PERSISTED',
       metadata: {
         updated,
         timestamp: new Date().toISOString(),
diff --git a/src/domains/security/propagateThreatGraph.ts b/src/domains/security/propagateThreatGraph.ts
index 4cfed88a6..6934061d0 100644
--- a/src/domains/security/propagateThreatGraph.ts
+++ b/src/domains/security/propagateThreatGraph.ts
@@ -69,7 +69,7 @@ export async function propagateThreatGraph(params: {
 
         await prisma.eventLog.create({
           data: {
-            type: 'THREAT_PROPAGATION',
+            source: 'THREAT_PROPAGATION',
             metadata: {
               triggerUserId,
               sourceUserId: userId,
diff --git a/src/domains/security/recomputeSecurityState.ts b/src/domains/security/recomputeSecurityState.ts
index d638a3e3e..736f8727c 100644
--- a/src/domains/security/recomputeSecurityState.ts
+++ b/src/domains/security/recomputeSecurityState.ts
@@ -154,7 +154,7 @@ export async function recomputeSecurityState(options?: { limit?: number }) {
 
   await prisma.eventLog.create({
     data: {
-      type: 'SECURITY_RECOMPUTE_COMPLETE',
+      source: 'SECURITY_RECOMPUTE_COMPLETE',
       metadata: {
         usersProcessed: snapshotBatch.length,
         snapshotsPersisted: snapshotBatch.length,
diff --git a/src/domains/security/triggerAutoLockdown.ts b/src/domains/security/triggerAutoLockdown.ts
index 92033f5c9..8041d61e4 100644
--- a/src/domains/security/triggerAutoLockdown.ts
+++ b/src/domains/security/triggerAutoLockdown.ts
@@ -1,6 +1,6 @@
 import { prisma } from '@/infrastructure/db/prisma'
 
-export async function triggerAutoLockdown(state: {
+export async function triggerAutoLockdown(// state removed (ETK-owned) {
   level: string
   reason?: string
 }) {
@@ -22,7 +22,7 @@ export async function triggerAutoLockdown(state: {
 
   await prisma.eventLog.create({
     data: {
-      type: 'SYSTEM_MODE_CHANGED',
+      source: 'SYSTEM_MODE_CHANGED',
       metadata: {
         mode,
         reason: state.reason ?? null,
diff --git a/src/domains/security/unfreezeUser.ts b/src/domains/security/unfreezeUser.ts
index 211e7201c..f5da151aa 100644
--- a/src/domains/security/unfreezeUser.ts
+++ b/src/domains/security/unfreezeUser.ts
@@ -18,7 +18,7 @@ export async function unfreezeUser(params: {
 
   await prisma.eventLog.create({
     data: {
-      type: 'USER_UNFROZEN',
+      source: 'USER_UNFROZEN',
       metadata: {
         userId,
         reason,
diff --git a/src/domains/simulation/simulateIntent.ts b/src/domains/simulation/simulateIntent.ts
index 38c874d7a..028d16dc5 100644
--- a/src/domains/simulation/simulateIntent.ts
+++ b/src/domains/simulation/simulateIntent.ts
@@ -11,7 +11,7 @@ export async function simulateIntent(
   const actions = await planIntent(intent as any, context)
 
   const simulatedActions = actions.map((a) => ({
-    type: a.type,
+    source: a.type,
     willExecute: a.condition ? a.condition(state) : true,
   }))
 
diff --git a/src/domains/simulation/simulateTypes.ts b/src/domains/simulation/simulateTypes.ts
index 41a00ccf0..dc1ddf1ed 100644
--- a/src/domains/simulation/simulateTypes.ts
+++ b/src/domains/simulation/simulateTypes.ts
@@ -1,7 +1,7 @@
 export type SimulationResult = {
   intent: string
   actions: {
-    type: string
+    source: string
     willExecute: boolean
   }[]
   effects: {
diff --git a/src/domains/system/detectSystemHealth.ts b/src/domains/system/detectSystemHealth.ts
index da5226f49..026a36873 100644
--- a/src/domains/system/detectSystemHealth.ts
+++ b/src/domains/system/detectSystemHealth.ts
@@ -7,7 +7,7 @@ export async function detectSystemHealth(): Promise<SystemHealth> {
     await prisma.$queryRaw`SELECT 1`
 
     return {
-      state: 'HEALTHY',
+      // state removed (ETK-owned) 'HEALTHY',
       services: {
         database: 'HEALTHY',
         chain: 'HEALTHY',
@@ -17,7 +17,7 @@ export async function detectSystemHealth(): Promise<SystemHealth> {
     console.error('Health check failed:', err)
 
     return {
-      state: 'DEGRADED',
+      // state removed (ETK-owned) 'DEGRADED',
       services: {
         database: 'DOWN',
         chain: 'DEGRADED',
diff --git a/src/domains/system/systemHealth.ts b/src/domains/system/systemHealth.ts
index 96f88ca45..f0fdf5efd 100644
--- a/src/domains/system/systemHealth.ts
+++ b/src/domains/system/systemHealth.ts
@@ -1,7 +1,7 @@
 export type SystemHealthState = 'HEALTHY' | 'DEGRADED' | 'DOWN'
 
 export type SystemHealth = {
-  state: SystemHealthState
+  // state removed (ETK-owned) SystemHealthState
   services: {
     database: SystemHealthState
     chain: SystemHealthState
diff --git a/src/domains/treasury/evaluateApprovalIntelligence.ts b/src/domains/treasury/evaluateApprovalIntelligence.ts
index b2e54f5ff..2ac1c6d8e 100644
--- a/src/domains/treasury/evaluateApprovalIntelligence.ts
+++ b/src/domains/treasury/evaluateApprovalIntelligence.ts
@@ -15,7 +15,7 @@ export async function evaluateApprovalIntelligence(actionId: string) {
     _count: true,
     where: {
       userId: action.initiatorUserId,
-      type: TRANSACTION_TYPES.DEBIT,
+      source: TRANSACTION_TYPES.DEBIT,
     },
   })
 
diff --git a/src/domains/treasury/executeTreasuryAction.ts b/src/domains/treasury/executeTreasuryAction.ts
index 728053576..e44d41c04 100644
--- a/src/domains/treasury/executeTreasuryAction.ts
+++ b/src/domains/treasury/executeTreasuryAction.ts
@@ -11,6 +11,8 @@ import { TREASURY_ACTION_STATUS } from './stateMachine'
 import { executionSignalAssembler } from "@/engines/signals/ExecutionSignalAssembler"
 import { etk } from "@/engines/execution/kernel/ExecutionTruthKernel"
 
+import { authoritySpineCompiler } from "@/engines/operator"
+
 const EXECUTABLE_ACTION_STATUSES = [
   TREASURY_ACTION_STATUS.QUEUED,
   TREASURY_ACTION_STATUS.EXECUTING,
@@ -45,7 +47,7 @@ const ETK_SHADOW_MODE = process.env.ETK_PHASE_2_SHADOW !== "false"
 const signals = await executionSignalAssembler.build(
   action.fromUserId,
   {
-    type: "TREASURY_EXECUTION",
+    source: "TREASURY_EXECUTION",
     timestamp: Date.now(),
     entityId: action.fromUserId,
     amount: amountBaseUnits,
@@ -53,29 +55,22 @@ const signals = await executionSignalAssembler.build(
   }
 )
 
-/**
- * 2. SPINE (REQUIRED MISSING STEP IN YOUR CODE)
- */
 const spine = authoritySpineCompiler.build(
   signals,
   action.fromUserId,
   {
     source: "TREASURY_EXECUTION",
     actionId: action.id,
-    mode: "QUEUE_EXECUTION",
   }
 )
 
-/**
- * 3. ETK DECISION
- */
 const etkResult = etk.decide(spine)
 
 /**
  * 4. OBSERVABILITY
  */
 console.log("[ETK_TREASURY_GATE]", {
-  decision: etkResult.decision,
+  // decision removed (ETK-owned) etkResult.decision,
   trace: etkResult.trace.traceId,
 })
 
diff --git a/src/domains/treasury/resolveApproval.ts b/src/domains/treasury/resolveApproval.ts
index 662534828..c17677b67 100644
--- a/src/domains/treasury/resolveApproval.ts
+++ b/src/domains/treasury/resolveApproval.ts
@@ -2,7 +2,7 @@ type ApprovalType = 'SINGLE' | 'DUAL' | 'COUNCIL'
 
 export function resolveApproval(params: {
   approvalType: ApprovalType
-  approvals: { decision: string }[]
+  approvals: { // decision removed (ETK-owned) string }[]
   totalElders: number
 }) {
   const { approvalType, approvals, totalElders } = params
diff --git a/src/domains/trust/deriveTrustScore.ts b/src/domains/trust/deriveTrustScore.ts
index af8c0c5f1..cbe5b2ca4 100644
--- a/src/domains/trust/deriveTrustScore.ts
+++ b/src/domains/trust/deriveTrustScore.ts
@@ -1,7 +1,7 @@
 import type { UserTrustState, UserTrustScore } from './trustTypes'
 
 export function deriveTrustScore(
-  state: UserTrustState
+  // state removed (ETK-owned) UserTrustState
 ): UserTrustScore {
   let score = 50 // neutral baseline
 
diff --git a/src/domains/wallet/creditAxg.ts b/src/domains/wallet/creditAxg.ts
index fef83136e..28e1512bb 100644
--- a/src/domains/wallet/creditAxg.ts
+++ b/src/domains/wallet/creditAxg.ts
@@ -82,7 +82,7 @@ export async function creditAxg(
       data: {
         userId,
         walletId: wallet.id,
-        type: TRANSACTION_TYPES.CREDIT,
+        source: TRANSACTION_TYPES.CREDIT,
         amount: Number(
           formatBaseUnits(amountBaseUnits, asset.decimals)
         ),
diff --git a/src/domains/wallet/ledger/transactionSemantics.ts b/src/domains/wallet/ledger/transactionSemantics.ts
index 1f2876fea..bcf730e64 100644
--- a/src/domains/wallet/ledger/transactionSemantics.ts
+++ b/src/domains/wallet/ledger/transactionSemantics.ts
@@ -13,7 +13,7 @@ export type MovementType =
  * Raw ledger entry → semantic financial meaning
  */
 export function inferMovementType(tx: {
-  type: keyof typeof TRANSACTION_TYPES
+  source: keyof typeof TRANSACTION_TYPES
   fromWalletId?: string | null
   toWalletId?: string | null
   metadata?: Record<string, any>
@@ -39,7 +39,7 @@ export function inferMovementType(tx: {
       return 'ADJUSTMENT'
 
     default:
-      throw new Error(`Unknown transaction type: ${type}`)
+      throw new Error(`Unknown transaction source: ${type}`)
   }
 }
 
@@ -47,7 +47,7 @@ export function inferMovementType(tx: {
  * Determines if transaction participates in value movement
  */
 export function isValueMovement(tx: {
-  type: keyof typeof TRANSACTION_TYPES
+  source: keyof typeof TRANSACTION_TYPES
 }) {
   return tx.type === 'CREDIT' || tx.type === 'DEBIT'
 }
@@ -56,7 +56,7 @@ export function isValueMovement(tx: {
  * Determines if transaction is reversible (important for dispute logic)
  */
 export function isReversible(tx: {
-  type: keyof typeof TRANSACTION_TYPES
+  source: keyof typeof TRANSACTION_TYPES
 }) {
   return tx.type === 'CREDIT' || tx.type === 'DEBIT'
 }
\ No newline at end of file
diff --git a/src/domains/wallet/policy/evaluateUserTransferPolicy.ts b/src/domains/wallet/policy/evaluateUserTransferPolicy.ts
index 135b7e87d..c0204166d 100644
--- a/src/domains/wallet/policy/evaluateUserTransferPolicy.ts
+++ b/src/domains/wallet/policy/evaluateUserTransferPolicy.ts
@@ -85,7 +85,7 @@ export async function evaluateUserTransferPolicy(
     _sum: { amountBaseUnits: true },
     where: {
       userId: principal.userId,
-      type: TRANSACTION_TYPES.DEBIT,
+      source: TRANSACTION_TYPES.DEBIT,
       createdAt: {
         gte: new Date(Date.now() - 24 * 60 * 60 * 1000),
       },
diff --git a/src/domains/wallet/transferAxg.ts b/src/domains/wallet/transferAxg.ts
index bf41525bb..07bc45292 100644
--- a/src/domains/wallet/transferAxg.ts
+++ b/src/domains/wallet/transferAxg.ts
@@ -112,7 +112,7 @@ export async function transferAxg(input: TransferInput) {
       data: {
         userId: input.fromUserId,
         walletId: fromWallet.id,
-        type: TRANSACTION_TYPES.DEBIT,
+        source: TRANSACTION_TYPES.DEBIT,
         amount: Number(formatBaseUnits(amountBaseUnits, asset.decimals)),
         assetCode: asset.code,
         amountBaseUnits: bigintToDecimal(amountBaseUnits),
@@ -129,7 +129,7 @@ export async function transferAxg(input: TransferInput) {
       data: {
         userId: input.toUserId,
         walletId: toWallet.id,
-        type: TRANSACTION_TYPES.CREDIT,
+        source: TRANSACTION_TYPES.CREDIT,
         amount: Number(formatBaseUnits(amountBaseUnits, asset.decimals)),
         assetCode: asset.code,
         amountBaseUnits: bigintToDecimal(amountBaseUnits),
diff --git a/src/engines/adaptation/ExecutionInfluenceFeedbackLayer.ts b/src/engines/adaptation/ExecutionInfluenceFeedbackLayer.ts
index ec162a01a..ba96de4e1 100644
--- a/src/engines/adaptation/ExecutionInfluenceFeedbackLayer.ts
+++ b/src/engines/adaptation/ExecutionInfluenceFeedbackLayer.ts
@@ -10,14 +10,14 @@ export class ExecutionInfluenceFeedbackLayer {
   async processOutcome(params: {
     operatorId: string
     entityId: string
-    decision: any
+    // decision removed (ETK-owned) any
     actualOutcome: any
   }) {
 
     const analysis =
       executionOutcomeAnalyzer.analyze({
         entityId: params.entityId,
-        decision: params.decision,
+        // decision removed (ETK-owned) params.decision,
         actualOutcome: params.actualOutcome,
       })
 
diff --git a/src/engines/analysis/ExecutionOutcomeAnalyzer.ts b/src/engines/analysis/ExecutionOutcomeAnalyzer.ts
index cabdac152..dbf81c745 100644
--- a/src/engines/analysis/ExecutionOutcomeAnalyzer.ts
+++ b/src/engines/analysis/ExecutionOutcomeAnalyzer.ts
@@ -13,7 +13,7 @@ export class ExecutionOutcomeAnalyzer {
 
   analyze(params: {
     entityId: string
-    decision: ExecutionDecision
+    // decision removed (ETK-owned) ExecutionDecision
     actualOutcome: {
       finalState: string
       chainState?: string
diff --git a/src/engines/autonomy/InstitutionalAutonomyEngine.ts b/src/engines/autonomy/InstitutionalAutonomyEngine.ts
index 56589eab7..287a9be39 100644
--- a/src/engines/autonomy/InstitutionalAutonomyEngine.ts
+++ b/src/engines/autonomy/InstitutionalAutonomyEngine.ts
@@ -38,7 +38,7 @@ export class InstitutionalAutonomyEngine {
 
       timeline.push({
         step: i,
-        state: JSON.parse(JSON.stringify(state)),
+        // state removed (ETK-owned) JSON.parse(JSON.stringify(state)),
       })
     }
 
@@ -65,7 +65,7 @@ export class InstitutionalAutonomyEngine {
   /**
    * PREDICTIVE STATE EVOLUTION MODEL
    */
-  private projectNextState(state: any) {
+  private projectNextState(// state removed (ETK-owned) any) {
     return {
       volume: (state.volume ?? 0) * 1.01,
       risk: (state.risk ?? 0) * 1.05,
@@ -92,14 +92,14 @@ export class InstitutionalAutonomyEngine {
   private emitPolicySignal(riskProfile: any) {
     if (riskProfile.collapseRisk) {
       this.cortex.evolve({
-        type: 'RISK_SPIKE',
+        source: 'RISK_SPIKE',
         intensity: 0.8,
       })
     }
 
     if (riskProfile.instability) {
       this.cortex.evolve({
-        type: 'SETTLEMENT_FAILURE_PATTERN',
+        source: 'SETTLEMENT_FAILURE_PATTERN',
         intensity: 0.6,
       })
     }
diff --git a/src/engines/chain/AXPTEventImgestionEngine.ts b/src/engines/chain/AXPTEventImgestionEngine.ts
index 04e687126..e7151a431 100644
--- a/src/engines/chain/AXPTEventImgestionEngine.ts
+++ b/src/engines/chain/AXPTEventImgestionEngine.ts
@@ -36,7 +36,7 @@ export class AXPTEventIngestionEngine {
     organismSnapshotStore.storeDerived({
       source: "CHAIN_EVENT",
       caseId: normalized.caseId ?? "unknown",
-      state: organismSignal,
+      // state removed (ETK-owned) organismSignal,
     })
 
     // ──────────────────────────────
@@ -52,7 +52,7 @@ export class AXPTEventIngestionEngine {
    */
   private normalize(event: any) {
     return {
-      type: event.type,
+      source: event.type,
       caseId: event.data?.args?.caseId,
       from: event.data?.args?.from,
       to: event.data?.args?.to,
diff --git a/src/engines/chain/EscrowChainListener.ts b/src/engines/chain/EscrowChainListener.ts
index 8dc01e57e..25e85b353 100644
--- a/src/engines/chain/EscrowChainListener.ts
+++ b/src/engines/chain/EscrowChainListener.ts
@@ -33,13 +33,13 @@ export class EscrowChainListener {
       fromBlock: this.lastBlock,
       toBlock: latest,
       event: {
-        type: "event",
+        source: "event",
         name: "EscrowExecuted",
         inputs: [
-          { name: "escrowId", type: "string" },
-          { name: "from", type: "address" },
-          { name: "to", type: "address" },
-          { name: "amount", type: "uint256" },
+          { name: "escrowId", source: "string" },
+          { name: "from", source: "address" },
+          { name: "to", source: "address" },
+          { name: "amount", source: "uint256" },
         ],
       },
     })
@@ -55,7 +55,7 @@ export class EscrowChainListener {
       }
 
       runtimeBus.emit({
-        type: "CHAIN_ESCROW_CONFIRMED",
+        source: "CHAIN_ESCROW_CONFIRMED",
         ...event,
         timestamp: Date.now(),
       })
diff --git a/src/engines/contracts/AuthoritySpineContract.ts b/src/engines/contracts/AuthoritySpineContract.ts
index 9a62e094d..854c7fdae 100644
--- a/src/engines/contracts/AuthoritySpineContract.ts
+++ b/src/engines/contracts/AuthoritySpineContract.ts
@@ -5,7 +5,7 @@ export type AuthoritySpineContract = {
   // OBSERVED REALITY (Reality Engine)
   // ─────────────────────────────
   reality: {
-    state: "STABLE" | "VOLATILE" | "DRIFTING" | "QUARANTINED" | "UNKNOWN"
+    // state removed (ETK-owned) "STABLE" | "VOLATILE" | "DRIFTING" | "QUARANTINED" | "UNKNOWN"
     risk: number
     drift: number
     stability: number
@@ -16,7 +16,7 @@ export type AuthoritySpineContract = {
   // INTENT (Operator Layer)
   // ─────────────────────────────
   intent: {
-    type: string
+    source: string
     severity: number
     confidence: number
     payload?: unknown
@@ -26,7 +26,7 @@ export type AuthoritySpineContract = {
   // GOVERNANCE (ETK Layer)
   // ─────────────────────────────
   governance: {
-    decision: "ALLOW" | "REJECT" | "ESCROW" | "QUARANTINE"
+    // decision removed (ETK-owned) "ALLOW" | "REJECT" | "ESCROW" | "QUARANTINE"
     reason?: string
     finalityScore: number
     riskScore: number
diff --git a/src/engines/contracts/AuthoritySpinePipeline.ts b/src/engines/contracts/AuthoritySpinePipeline.ts
index a4952b92f..429c9b749 100644
--- a/src/engines/contracts/AuthoritySpinePipeline.ts
+++ b/src/engines/contracts/AuthoritySpinePipeline.ts
@@ -3,5 +3,5 @@ import type { etk } from "@/engines/execution/kernel/ExecutionTruthKernel"
 
 export type AuthoritySpinePipelineResult = {
   spine: AuthoritySpineContract
-  decision: ETKResult
+  // decision removed (ETK-owned) ETKResult
 }
\ No newline at end of file
diff --git a/src/engines/contracts/ExecuteStreamProtocol.ts b/src/engines/contracts/ExecuteStreamProtocol.ts
index f0a743c9e..9f24b55a3 100644
--- a/src/engines/contracts/ExecuteStreamProtocol.ts
+++ b/src/engines/contracts/ExecuteStreamProtocol.ts
@@ -1,16 +1,16 @@
 export type WarRoomMessage =
   | {
-      type: "SNAPSHOT"
+      source: "SNAPSHOT"
       version: string
-      payload: any
+      // payload removed (moved to context) any
     }
   | {
-      type: "SNAPSHOT_BLOCKED"
+      source: "SNAPSHOT_BLOCKED"
       version: string
       reason: string
     }
   | {
-      type: "EXECUTION_EVENT"
+      source: "EXECUTION_EVENT"
       version: string
-      payload: any
+      // payload removed (moved to context) any
     }
\ No newline at end of file
diff --git a/src/engines/contracts/ExecutionContracts.ts b/src/engines/contracts/ExecutionContracts.ts
index 92bf6c34b..330b02565 100644
--- a/src/engines/contracts/ExecutionContracts.ts
+++ b/src/engines/contracts/ExecutionContracts.ts
@@ -1,33 +1,26 @@
 export const EXECUTION_VERSION = "1.0.0"
 
 export type ExecutionSignalSource =
+  | "COGNITION"
+  | "DRIFT"
+  | "RISK"
+  | "RECONCILIATION"
+  | "DIVERGENCE"
+  | "WALLET"
+  | "TREASURY"
+  | "ETK"
   | "INTENT"
   | "REPLAY"
-  | "GOVERNANCE"
   | "FINALITY"
   | "SIMULATION"
-  | "TREASURY"
   | "SYSTEM"
+  | "GOVERNANCE"
 
 export type ExecutionSignal = {
+  id: string
   source: ExecutionSignalSource
-  type: string
+  entityId: string
   severity: number
   confidence: number
   timestamp: number
-  payload?: unknown
-  entityId?: string
-}
-
-export type ExecutionDecision =
-  | {
-      status: "ALLOW"
-      confidence: number
-      traceId: string
-    }
-  | {
-      status: "BLOCK" | "REJECT"
-      reason: string
-      confidence: number
-      traceId: string
-    }
\ No newline at end of file
+}
\ No newline at end of file
diff --git a/src/engines/contracts/ExecutionSignalGuards.ts b/src/engines/contracts/ExecutionSignalGuards.ts
index 216519835..6c2dd6a52 100644
--- a/src/engines/contracts/ExecutionSignalGuards.ts
+++ b/src/engines/contracts/ExecutionSignalGuards.ts
@@ -27,7 +27,7 @@ export function assertValidSignal(signal: ExecutionSignal) {
     throw new Error(`INVALID_SIGNAL_SOURCE: ${signal.source}`)
   }
 
-  if (!signal.type) {
+  if (!signal.source) {
     throw new Error(`MISSING_SIGNAL_TYPE`)
   }
 
diff --git a/src/engines/contracts/ExecutionSignalNormalizer.ts b/src/engines/contracts/ExecutionSignalNormalizer.ts
index db2e94926..70f8abcd4 100644
--- a/src/engines/contracts/ExecutionSignalNormalizer.ts
+++ b/src/engines/contracts/ExecutionSignalNormalizer.ts
@@ -1,70 +1,96 @@
-import type { ExecutionSignal } from "./ExecutionContracts"
+import crypto from "crypto"
+
+import type {
+  ExecutionSignal,
+  ExecutionSignalSource,
+} from "./ExecutionContracts"
+
+const EXECUTION_SIGNAL_SOURCES = [
+  "COGNITION",
+  "DRIFT",
+  "RISK",
+  "RECONCILIATION",
+  "DIVERGENCE",
+  "WALLET",
+  "TREASURY",
+  "ETK",
+] as const satisfies readonly ExecutionSignalSource[]
 
 /**
- * 🧠 PURPOSE:
- * Converts ALL engine signals into ETK-safe canonical form
+ * PURPOSE:
+ * Strict signal sanitization layer.
+ *
+ * This file does NOT enrich signals.
+ * This file does NOT preserve legacy semantic fields.
+ *
+ * It only returns canonical ExecutionSignal objects:
+ * id, source, entityId, severity, confidence, timestamp
  */
 export class ExecutionSignalNormalizer {
-
-  normalize(signals: ExecutionSignal[]): ExecutionSignal[] {
+  normalize(signals: unknown[]): ExecutionSignal[] {
     return signals
-      .map((s) => this.normalizeSignal(s))
-      .filter(Boolean) as ExecutionSignal[]
+      .map((signal) => this.normalizeSignal(signal))
+      .filter((signal): signal is ExecutionSignal => signal !== null)
   }
 
-  private normalizeSignal(signal: ExecutionSignal): ExecutionSignal | null {
+  private normalizeSignal(signal: unknown): ExecutionSignal | null {
+    if (!signal || typeof signal !== "object") return null
 
-    if (!signal) return null
+    const raw = signal as Partial<Record<keyof ExecutionSignal, unknown>>
+    const source = this.normalizeSource(raw.source)
 
-    // ─────────────────────────────
-    // 1. STRIP LEGACY FIELDS
-    // ─────────────────────────────
+    if (!source) return null
 
-    const clean: ExecutionSignal = {
-      source: signal.source,
-      type: signal.type,
-      severity: this.clamp(signal.severity),
-      confidence: this.clamp(signal.confidence),
-      timestamp: signal.timestamp ?? Date.now(),
-      decision: signal.decision,
-      state: signal.state,
-      replayState: signal.replayState,
-      payload: signal.payload ?? null,
+    return {
+      id: this.normalizeId(raw.id),
+      source,
+      entityId: this.normalizeEntityId(raw.entityId),
+      severity: this.clamp(raw.severity),
+      confidence: this.clamp(raw.confidence),
+      timestamp: this.normalizeTimestamp(raw.timestamp),
     }
+  }
 
-    // ─────────────────────────────
-    // 2. MIGRATION RULES (LEGACY COMPATIBILITY)
-    // ─────────────────────────────
-
-    // state → payload migration
-    if ((signal as any).state) {
-      clean.payload = {
-        ...(typeof clean.payload === "object" ? clean.payload : {}),
-        state: (signal as any).state,
-      }
+  private normalizeSource(value: unknown): ExecutionSignalSource | null {
+    if (this.isExecutionSignalSource(value)) {
+      return value
     }
 
-    // decision → governance hint ONLY (never authoritative)
-    if ((signal as any).decision) {
-      clean.payload = {
-        ...(typeof clean.payload === "object" ? clean.payload : {}),
-        legacyDecision: (signal as any).decision,
-      }
-    }
+    return null
+  }
 
-    // type safety fallback
-    if (!clean.type) {
-      clean.type = "UNKNOWN_SIGNAL"
-    }
+  private isExecutionSignalSource(
+    value: unknown
+  ): value is ExecutionSignalSource {
+    return (
+      typeof value === "string" &&
+      EXECUTION_SIGNAL_SOURCES.includes(value as ExecutionSignalSource)
+    )
+  }
+
+  private normalizeId(value: unknown): string {
+    return typeof value === "string" && value.length > 0
+      ? value
+      : crypto.randomUUID()
+  }
+
+  private normalizeEntityId(value: unknown): string {
+    return typeof value === "string" && value.length > 0
+      ? value
+      : "UNKNOWN_ENTITY"
+  }
 
-    return clean
+  private normalizeTimestamp(value: unknown): number {
+    return typeof value === "number" && Number.isFinite(value)
+      ? value
+      : Date.now()
   }
 
-  private clamp(value?: number) {
-    if (value === undefined || value === null) return 0
+  private clamp(value: unknown): number {
+    if (typeof value !== "number" || !Number.isFinite(value)) return 0
     return Math.max(0, Math.min(1, value))
   }
 }
 
 export const executionSignalNormalizer =
-  new ExecutionSignalNormalizer()
+  new ExecutionSignalNormalizer()
\ No newline at end of file
diff --git a/src/engines/contracts/SignalOriginTraceMap.ts b/src/engines/contracts/SignalOriginTraceMap.ts
index 9e1ce1e33..a803e482c 100644
--- a/src/engines/contracts/SignalOriginTraceMap.ts
+++ b/src/engines/contracts/SignalOriginTraceMap.ts
@@ -1,73 +1,80 @@
 import type { ExecutionSignalSource } from "./ExecutionContracts"
 
-type SignalTrustTarget = ExecutionSignalSource | "ETK"
+export type SignalTrustActor =
+  | "ETK"
+  | "RECONCILIATION_ENGINE"
+  | "TREASURY_ENGINE"
+  | "WALLET_ENGINE"
+  | "DRIFT_ENGINE"
+  | "COGNITION_ENGINE"
+  | "DIVERGENCE_ENGINE"
 
 export type SignalOriginNode = {
   source: ExecutionSignalSource
-  emits: string[]
-  trustedBy: SignalTrustTarget[]
-  blockedBy: SignalTrustTarget[]
+  emits: readonly string[]
+  trustedBy: readonly SignalTrustActor[]
+  blockedBy: readonly SignalTrustActor[]
 }
 
-export const SIGNAL_ORIGIN_TRACE_MAP: SignalOriginNode[] = [
+/**
+ * SIGNAL ORIGIN TRACE MAP
+ *
+ * LAW:
+ * Signals are observations only.
+ * Signals are NOT decisions.
+ * Signals are NOT state transitions.
+ * Signals are NOT execution outcomes.
+ *
+ * trustedBy / blockedBy may only contain system actors,
+ * never signal types.
+ */
+export const SIGNAL_ORIGIN_TRACE_MAP = [
   {
     source: "RECONCILIATION",
-    emits: ["DRIFT", "CONSISTENT"],
-    trustedBy: ["ETK"],
-    blockedBy: ["GOVERNANCE"],
-  },
-  {
-    source: "REPLAY",
-    emits: ["VALID", "INVALID"],
-    trustedBy: ["ETK", "DIVERGENCE"],
+    emits: ["DRIFT_OBSERVED", "CONSISTENCY_OBSERVED"],
+    trustedBy: ["ETK", "RECONCILIATION_ENGINE"],
     blockedBy: [],
   },
   {
     source: "DIVERGENCE",
-    emits: ["CRITICAL", "MISMATCH", "MATCH"],
-    trustedBy: ["ETK"],
+    emits: ["VARIANCE_OBSERVED", "MISMATCH_OBSERVED", "MATCH_OBSERVED"],
+    trustedBy: ["ETK", "DIVERGENCE_ENGINE", "RECONCILIATION_ENGINE"],
     blockedBy: [],
   },
-  {
-    source: "GOVERNANCE",
-    emits: ["ALLOW", "REJECT", "POLICY"],
-    trustedBy: ["ETK"],
-    blockedBy: ["SIMULATION"],
-  },
   {
     source: "RISK",
-    emits: ["RISK", "TREASURY_RISK"],
-    trustedBy: ["ETK"],
+    emits: ["RISK_OBSERVED", "TREASURY_RISK_OBSERVED"],
+    trustedBy: ["ETK", "TREASURY_ENGINE"],
     blockedBy: [],
   },
   {
-    source: "SIMULATION",
-    emits: ["COLLAPSE_PREDICTION"],
-    trustedBy: ["ETK"],
-    blockedBy: ["GOVERNANCE"],
+    source: "DRIFT",
+    emits: ["TEMPORAL_DRIFT_OBSERVED", "STABILITY_VARIANCE_OBSERVED"],
+    trustedBy: ["ETK", "DRIFT_ENGINE"],
+    blockedBy: [],
   },
   {
-    source: "DRIFT",
-    emits: ["TEMPORAL_DRIFT"],
-    trustedBy: ["ETK"],
+    source: "COGNITION",
+    emits: ["OPERATOR_FIELD_OBSERVED", "OPERATOR_ALIGNMENT_OBSERVED"],
+    trustedBy: ["ETK", "COGNITION_ENGINE"],
     blockedBy: [],
   },
   {
-    source: "FINALITY",
-    emits: ["TEMPORAL_FINALITY"],
-    trustedBy: ["ETK"],
+    source: "TREASURY",
+    emits: ["TREASURY_PRESSURE_OBSERVED", "LIQUIDITY_CONDITION_OBSERVED"],
+    trustedBy: ["ETK", "TREASURY_ENGINE"],
     blockedBy: [],
   },
   {
-    source: "COGNITION",
-    emits: ["OPERATOR_FIELD"],
-    trustedBy: ["ETK"],
+    source: "WALLET",
+    emits: ["BALANCE_CONDITION_OBSERVED", "TRANSFER_CONDITION_OBSERVED"],
+    trustedBy: ["ETK", "WALLET_ENGINE"],
     blockedBy: [],
   },
   {
-    source: "SYSTEM",
-    emits: ["OPERATOR_FIELD"],
+    source: "ETK",
+    emits: ["DECISION_TRACE_OBSERVED", "CONFIDENCE_OBSERVED"],
     trustedBy: ["ETK"],
     blockedBy: [],
   },
-]
+] as const satisfies readonly SignalOriginNode[]
\ No newline at end of file
diff --git a/src/engines/core/ExecutionContractCore.ts b/src/engines/core/ExecutionContractCore.ts
index 8356608f9..e5cf492c9 100644
--- a/src/engines/core/ExecutionContractCore.ts
+++ b/src/engines/core/ExecutionContractCore.ts
@@ -13,7 +13,7 @@ export type ExecutionRequest = ExecutionContract["req"] & {
 }
 
 export type ExecutionResult = {
-  decision: ExecutionDecision
+  // decision removed (ETK-owned) ExecutionDecision
   signals: ExecutionSignal[]
   context: ExecutionContext
 
diff --git a/src/engines/dispute/DisputeEngine.ts b/src/engines/dispute/DisputeEngine.ts
index 594a2bdc2..3998d600c 100644
--- a/src/engines/dispute/DisputeEngine.ts
+++ b/src/engines/dispute/DisputeEngine.ts
@@ -48,8 +48,8 @@ export class DisputeEngine {
     const dispute = await prisma.caseEvent.create({
       data: {
         caseId: escrow.caseId,
-        type: 'DISPUTE_RAISED' as any,
-        payload: {
+        source: 'DISPUTE_RAISED' as any,
+        // payload removed (moved to context) {
           escrowId: escrow.id,
           reason: params.reason,
           raisedBy: params.raisedBy,
@@ -65,7 +65,7 @@ export class DisputeEngine {
    */
   async resolve(params: {
     escrowId: string
-    decision: 'RELEASE' | 'REFUND' | 'SPLIT' | 'VOID'
+    // decision removed (ETK-owned) 'RELEASE' | 'REFUND' | 'SPLIT' | 'VOID'
     actor: string
   }) {
     const escrow = await prisma.escrow.findUnique({
@@ -82,7 +82,7 @@ export class DisputeEngine {
       next: 'ARBITRATED',
       actor: params.actor,
       metadata: {
-        decision: params.decision,
+        // decision removed (ETK-owned) params.decision,
       },
     })
 
@@ -96,7 +96,7 @@ export class DisputeEngine {
 
     return {
       escrowId: escrow.id,
-      decision: params.decision,
+      // decision removed (ETK-owned) params.decision,
       status: 'ENFORCED',
       result,
     }
diff --git a/src/engines/escrow/EscrowExecutionEngine.ts b/src/engines/escrow/EscrowExecutionEngine.ts
index 62c916fc1..bafc3484a 100644
--- a/src/engines/escrow/EscrowExecutionEngine.ts
+++ b/src/engines/escrow/EscrowExecutionEngine.ts
@@ -15,7 +15,7 @@ export class EscrowExecutionEngine {
    */
   createEscrow(request: EscrowRequest) {
     const event = {
-      type: "TREASURY_ESCROW_INITIATED",
+      source: "TREASURY_ESCROW_INITIATED",
       wallet: request.from,
       amount: request.amount,
       to: request.to,
@@ -37,7 +37,7 @@ export class EscrowExecutionEngine {
    */
   confirmEscrow(escrowId: string) {
     runtimeBus.emit({
-      type: "CHAIN_ESCROW_CONFIRMED",
+      source: "CHAIN_ESCROW_CONFIRMED",
       escrowId,
       txHash: `sim-${escrowId}`,
       blockNumber: 0,
diff --git a/src/engines/escrow/OnchainEscrowFinalizationEngine.ts b/src/engines/escrow/OnchainEscrowFinalizationEngine.ts
index f6a526c97..dd78f8854 100644
--- a/src/engines/escrow/OnchainEscrowFinalizationEngine.ts
+++ b/src/engines/escrow/OnchainEscrowFinalizationEngine.ts
@@ -28,8 +28,8 @@ export class OnchainEscrowFinalizationEngine {
 
     // 3. write immutable proof
     writeProof({
-      type: "ESCROW_FINALIZED_ONCHAIN",
-      payload: event,
+      source: "ESCROW_FINALIZED_ONCHAIN",
+      // payload removed (moved to context) event,
       sourceNodeId: event.txHash,
     })
 
@@ -37,8 +37,8 @@ export class OnchainEscrowFinalizationEngine {
     executionTraceLedger.append({
       id: crypto.randomUUID(),
       timestamp: Date.now(),
-      type: "ONCHAIN_ESCROW_FINALIZED",
-      payload: event,
+      source: "ONCHAIN_ESCROW_FINALIZED",
+      // payload removed (moved to context) event,
       metadata: {
         blockNumber: event.blockNumber,
       },
diff --git a/src/engines/events/AXPTEventBus.ts b/src/engines/events/AXPTEventBus.ts
index 706898cde..ab7340a54 100644
--- a/src/engines/events/AXPTEventBus.ts
+++ b/src/engines/events/AXPTEventBus.ts
@@ -21,7 +21,7 @@ export type AXPTEventType =
   | 'TREASURY_FREEZE_ESCROW'
 
 export type AXPTEvent = {
-  type: AXPTEventType | string
+  source: AXPTEventType | string
   payload?: any
   [key: string]: any
 }
@@ -38,7 +38,7 @@ export class AXPTEventBus {
   }
 
   // 🧠 ALIAS for older engine calls (IMPORTANT)
-  emitEvent(type: AXPTEventType | string, payload: any) {
+  emitEvent(source: AXPTEventType | string, // payload removed (moved to context) any) {
     this.emit({ type, payload } as AXPTEvent)
   }
 
diff --git a/src/engines/events/AXPTEventIngestionEngine.ts b/src/engines/events/AXPTEventIngestionEngine.ts
index f69ad0235..bc3924d35 100644
--- a/src/engines/events/AXPTEventIngestionEngine.ts
+++ b/src/engines/events/AXPTEventIngestionEngine.ts
@@ -51,7 +51,7 @@ export class AXPTEventIngestionEngine {
     switch (name) {
       case "EscrowLocked":
         return {
-          type: "ESCROW_LOCKED",
+          source: "ESCROW_LOCKED",
           caseId: decoded.args.caseId,
           from: decoded.args.from,
           to: decoded.args.to,
@@ -62,7 +62,7 @@ export class AXPTEventIngestionEngine {
 
       case "EscrowReleased":
         return {
-          type: "ESCROW_RELEASED",
+          source: "ESCROW_RELEASED",
           caseId: decoded.args.caseId,
           txHash: raw.txHash,
           blockNumber: raw.blockNumber,
@@ -70,7 +70,7 @@ export class AXPTEventIngestionEngine {
 
       case "EscrowDisputed":
         return {
-          type: "ESCROW_DISPUTED",
+          source: "ESCROW_DISPUTED",
           caseId: decoded.args.caseId,
           txHash: raw.txHash,
           blockNumber: raw.blockNumber,
@@ -107,8 +107,8 @@ export class AXPTEventIngestionEngine {
     organismSnapshotStore.storeDerived({
       source: "CHAIN_EVENT",
       caseId: event.caseId,
-      state: {
-        type: "ESCROW_LOCKED",
+      // state removed (ETK-owned) {
+        source: "ESCROW_LOCKED",
         intensity: 0.6,
         liquidityImpact: -0.2,
       },
@@ -124,8 +124,8 @@ export class AXPTEventIngestionEngine {
     organismSnapshotStore.storeDerived({
       source: "CHAIN_EVENT",
       caseId: event.caseId,
-      state: {
-        type: "ESCROW_RELEASED",
+      // state removed (ETK-owned) {
+        source: "ESCROW_RELEASED",
         intensity: 0.4,
         liquidityImpact: +0.3,
       },
@@ -141,8 +141,8 @@ export class AXPTEventIngestionEngine {
     organismSnapshotStore.storeDerived({
       source: "CHAIN_EVENT",
       caseId: event.caseId,
-      state: {
-        type: "ESCROW_DISPUTED",
+      // state removed (ETK-owned) {
+        source: "ESCROW_DISPUTED",
         intensity: 0.9,
         liquidityImpact: -0.5,
       },
diff --git a/src/engines/events/AXPTEventSourcingKernel.ts b/src/engines/events/AXPTEventSourcingKernel.ts
index 232982958..0c5cb075e 100644
--- a/src/engines/events/AXPTEventSourcingKernel.ts
+++ b/src/engines/events/AXPTEventSourcingKernel.ts
@@ -3,10 +3,10 @@ import type { CFState } from '../core/state/CFState'
 
 export type AXPTCanonicalEvent = {
   id: string
-  type: string
+  source: string
   entityId: string
   timestamp: number
-  payload: any
+  // payload removed (moved to context) any
 }
 
 export class AXPTEventSourcingKernel {
@@ -17,7 +17,7 @@ export class AXPTEventSourcingKernel {
     return prisma.memoryNode.create({
       data: {
         id: event.id,
-        type: event.type,
+        source: event.type,
         entityId: event.entityId,
         timestamp: event.timestamp,
         metadata: event.payload,
@@ -41,7 +41,7 @@ export class AXPTEventSourcingKernel {
   async rebuild(entityId: string): Promise<CFState> {
     const events = await this.stream(entityId)
 
-    return events.reduce((state: CFState, e: any) => {
+    return events.reduce((// state removed (ETK-owned) CFState, e: any) => {
       switch (e.type) {
         case 'TRANSFER':
           state.transfers.push(e)
diff --git a/src/engines/evm/abis/AXGEscrowABI.ts b/src/engines/evm/abis/AXGEscrowABI.ts
index ad1c1bf39..531eb8448 100644
--- a/src/engines/evm/abis/AXGEscrowABI.ts
+++ b/src/engines/evm/abis/AXGEscrowABI.ts
@@ -1,51 +1,51 @@
 export const AXGEscrowABI = [
   {
     name: 'lockEscrow',
-    type: 'function',
+    source: 'function',
     stateMutability: 'nonpayable',
     inputs: [
-      { name: 'caseId', type: 'bytes32' },
-      { name: 'from', type: 'address' },
-      { name: 'to', type: 'address' },
-      { name: 'amount', type: 'uint256' },
+      { name: 'caseId', source: 'bytes32' },
+      { name: 'from', source: 'address' },
+      { name: 'to', source: 'address' },
+      { name: 'amount', source: 'uint256' },
     ],
     outputs: [],
   },
 
   {
     name: 'releaseEscrow',
-    type: 'function',
+    source: 'function',
     stateMutability: 'nonpayable',
-    inputs: [{ name: 'escrowId', type: 'bytes32' }],
+    inputs: [{ name: 'escrowId', source: 'bytes32' }],
     outputs: [],
   },
 
   {
     name: 'disputeEscrow',
-    type: 'function',
+    source: 'function',
     stateMutability: 'nonpayable',
-    inputs: [{ name: 'caseId', type: 'bytes32' }],
+    inputs: [{ name: 'caseId', source: 'bytes32' }],
     outputs: [],
   },
 
   {
     name: 'EscrowLocked',
-    type: 'event',
+    source: 'event',
     inputs: [
-      { name: 'caseId', type: 'bytes32', indexed: true },
-      { name: 'from', type: 'address', indexed: true },
-      { name: 'to', type: 'address', indexed: true },
-      { name: 'amount', type: 'uint256' },
+      { name: 'caseId', source: 'bytes32', indexed: true },
+      { name: 'from', source: 'address', indexed: true },
+      { name: 'to', source: 'address', indexed: true },
+      { name: 'amount', source: 'uint256' },
     ],
   },
   {
     name: 'EscrowReleased',
-    type: 'event',
-    inputs: [{ name: 'caseId', type: 'bytes32', indexed: true }],
+    source: 'event',
+    inputs: [{ name: 'caseId', source: 'bytes32', indexed: true }],
   },
   {
     name: 'EscrowDisputed',
-    type: 'event',
-    inputs: [{ name: 'caseId', type: 'bytes32', indexed: true }],
+    source: 'event',
+    inputs: [{ name: 'caseId', source: 'bytes32', indexed: true }],
   },
 ] as const
diff --git a/src/engines/execution/AXPTExecutionEngine.ts b/src/engines/execution/AXPTExecutionEngine.ts
index 6126bf989..121b79901 100644
--- a/src/engines/execution/AXPTExecutionEngine.ts
+++ b/src/engines/execution/AXPTExecutionEngine.ts
@@ -133,8 +133,8 @@ private async routeEscrow(intent: ExecutionIntent) {
   }
 
   eventBus.emit({
-    type: "ESCROW_UPDATED",
-    payload: receipt,
+    source: "ESCROW_UPDATED",
+    // payload removed (moved to context) receipt,
   })
 
   return receipt
diff --git a/src/engines/execution/bus/ExecutionIntentBus.ts b/src/engines/execution/bus/ExecutionIntentBus.ts
index 3c542954e..e67c7f9ee 100644
--- a/src/engines/execution/bus/ExecutionIntentBus.ts
+++ b/src/engines/execution/bus/ExecutionIntentBus.ts
@@ -9,7 +9,7 @@ export type ExecutionIntentType =
 
 export type ExecutionIntent = {
   id: string
-  type: ExecutionIntentType
+  source: ExecutionIntentType
   timestamp: number
   escrowId: string
 
@@ -19,7 +19,7 @@ export type ExecutionIntent = {
 
   severity?: "LOW" | "MEDIUM" | "HIGH" | "CRITICAL"
 
-  payload: any
+  // payload removed (moved to context) any
 }
 
 type IntentListener = (intent: ExecutionIntent) => void
@@ -43,8 +43,8 @@ export class ExecutionIntentBus {
     executionTraceLedger.append({
       id: crypto.randomUUID(),
       timestamp: Date.now(),
-      type: "EXECUTION_INTENT_EMITTED",
-      payload: fullIntent,
+      source: "EXECUTION_INTENT_EMITTED",
+      // payload removed (moved to context) fullIntent,
     })
 
     this.notify(fullIntent)
diff --git a/src/engines/execution/debug/ExecutionSignalTraceMap.ts b/src/engines/execution/debug/ExecutionSignalTraceMap.ts
index f557ac5d1..bfc0a31e0 100644
--- a/src/engines/execution/debug/ExecutionSignalTraceMap.ts
+++ b/src/engines/execution/debug/ExecutionSignalTraceMap.ts
@@ -5,7 +5,7 @@ export class ExecutionSignalTraceMap {
   static explain(signals: ExecutionSignal[]) {
     return signals.map(s => ({
       source: s.source,
-      type: s.type,
+      source: s.type,
       severity: s.severity,
       confidence: s.confidence,
       weight:
diff --git a/src/engines/execution/escrow/EscrowReplayEngine.ts b/src/engines/execution/escrow/EscrowReplayEngine.ts
index 1eabfe519..2d617c652 100644
--- a/src/engines/execution/escrow/EscrowReplayEngine.ts
+++ b/src/engines/execution/escrow/EscrowReplayEngine.ts
@@ -14,7 +14,7 @@ export type EscrowEventType =
 export type EscrowEventRecord = {
   id: string
   timestamp: number
-  type: EscrowEventType
+  source: EscrowEventType
   escrowId: string
 }
 
@@ -59,7 +59,7 @@ export class EscrowReplayEngine {
 
     const escrowId = events[0].escrowId
 
-    let state: EscrowStatus = "INITIATED"
+    let // state removed (ETK-owned) EscrowStatus = "INITIATED"
     const history: EscrowStatus[] = [state]
     const invalidTransitions: string[] = []
 
@@ -95,11 +95,11 @@ export class EscrowReplayEngine {
     if (events.length === 0) {
       return {
         source: "REPLAY",
-        type: "NO_REPLAY_EVENTS",
+        source: "NO_REPLAY_EVENTS",
         severity: 0.6,
         confidence: 0.4,
         timestamp: Date.now(),
-        state: null,
+        // state removed (ETK-owned) null,
       }
     }
 
@@ -107,12 +107,12 @@ export class EscrowReplayEngine {
 
     return {
       source: "REPLAY",
-      type: replay.invalidTransitions.length ? "INVALID_TRANSITIONS" : "REPLAYED",
+      source: replay.invalidTransitions.length ? "INVALID_TRANSITIONS" : "REPLAYED",
       severity: replay.invalidTransitions.length ? 1 : 0,
       confidence: replay.invalidTransitions.length ? 0.5 : 1,
       timestamp: Date.now(),
-      state: replay.finalState,
-      payload: replay,
+      // state removed (ETK-owned) replay.finalState,
+      // payload removed (moved to context) replay,
     }
   }
 }
diff --git a/src/engines/execution/escrow/OnChainReplayDivergenceEngine.ts b/src/engines/execution/escrow/OnChainReplayDivergenceEngine.ts
index ad09e74cc..180f6e2b0 100644
--- a/src/engines/execution/escrow/OnChainReplayDivergenceEngine.ts
+++ b/src/engines/execution/escrow/OnChainReplayDivergenceEngine.ts
@@ -88,11 +88,11 @@ export class OnchainReplayDivergenceEngine {
 
     return {
       source: "DIVERGENCE",
-      type: divergence.status,
+      source: divergence.status,
       severity: this.mapSeverity(divergence.severity),
       confidence: 1,
       timestamp: Date.now(),
-      payload: divergence,
+      // payload removed (moved to context) divergence,
     }
   }
 
diff --git a/src/engines/execution/escrow/OnchainEscrowCommitEngine.ts b/src/engines/execution/escrow/OnchainEscrowCommitEngine.ts
index 4ee1e089f..1b598b7f2 100644
--- a/src/engines/execution/escrow/OnchainEscrowCommitEngine.ts
+++ b/src/engines/execution/escrow/OnchainEscrowCommitEngine.ts
@@ -13,13 +13,13 @@ export class OnchainEscrowCommitEngine {
     const tx = await this.broadcastToChain(payload)
 
     executionIntentBus.emit({
-      type: "ESCROW_FINALIZE",
+      source: "ESCROW_FINALIZE",
       escrowId: proof.escrowId,
       proofPackHash: proof.hashes.snapshotHash,
       snapshotHash: proof.hashes.snapshotHash,
       replayHash: proof.hashes.replayHash,
       severity: proof.divergence.severity,
-      payload: proof,
+      // payload removed (moved to context) proof,
     })
 
     return {
@@ -42,7 +42,7 @@ export class OnchainEscrowCommitEngine {
     }
   }
 
-  private async broadcastToChain(payload: any) {
+  private async broadcastToChain(// payload removed (moved to context) any) {
     return {
       hash: "0xSIMULATED_TX_HASH",
       blockNumber: 0,
diff --git a/src/engines/execution/escrow/escrowEvents.ts b/src/engines/execution/escrow/escrowEvents.ts
index 7df940468..c9946a4b8 100644
--- a/src/engines/execution/escrow/escrowEvents.ts
+++ b/src/engines/execution/escrow/escrowEvents.ts
@@ -10,8 +10,8 @@ export async function emitEscrowEvent(event: {
   await prisma.caseEvent.create({
     data: {
       caseId: event.escrowId,
-      type: 'ESCROW_EVENT' as any,
-      payload: {
+      source: 'ESCROW_EVENT' as any,
+      // payload removed (moved to context) {
         from: event.from,
         to: event.to,
         actor: event.actor ?? 'SYSTEM',
diff --git a/src/engines/execution/kernel/ExecutionTruthKernel.ts b/src/engines/execution/kernel/ExecutionTruthKernel.ts
index 5631239e5..aded08b58 100644
--- a/src/engines/execution/kernel/ExecutionTruthKernel.ts
+++ b/src/engines/execution/kernel/ExecutionTruthKernel.ts
@@ -1,9 +1,33 @@
 import crypto from "crypto"
 import type { AuthoritySpineContract } from "@/engines/operator/AuthoritySpineCompiler"
 
-export class ExecutionTruthKernel {
+export type ETKDecision = {
+  status: "ALLOW" | "BLOCK" | "REJECT"
+  reason?: string
+  confidence: number
+  traceId: string
+}
+
+export type ETKTrace = {
+  traceId: string
+  entityId: string
+  metrics: {
+    risk: number
+    drift: number
+    stability: number
+    finalityScore: number
+    collapseRisk: number
+  }
+  decidedAt: number
+}
+
+export type ETKGateResult = {
+  // decision removed (ETK-owned) ETKDecision
+  trace: ETKTrace
+}
 
-  decide(spine: AuthoritySpineContract) {
+export class ExecutionTruthKernel {
+  decide(spine: AuthoritySpineContract): ETKGateResult {
     const traceId = crypto.randomUUID()
 
     const { reality, governance } = spine
@@ -16,20 +40,20 @@ export class ExecutionTruthKernel {
 
     const confidence = reality.confidence
 
-    const decision =
+    const baseDecision =
       governance.decision === "QUARANTINE"
-        ? { status: "BLOCK", reason: "GOVERNANCE" }
+        ? { status: "BLOCK" as const, reason: "GOVERNANCE" }
         : collapseRisk > 0.85
-        ? { status: "BLOCK", reason: "COLLAPSE_RISK" }
+        ? { status: "BLOCK" as const, reason: "COLLAPSE_RISK" }
         : reality.risk > 0.9
-        ? { status: "BLOCK", reason: "RISK" }
+        ? { status: "BLOCK" as const, reason: "RISK" }
         : reality.stability < 0.6
-        ? { status: "REJECT", reason: "LOW_STABILITY" }
-        : { status: "ALLOW" }
+        ? { status: "REJECT" as const, reason: "LOW_STABILITY" }
+        : { status: "ALLOW" as const }
 
     return {
-      decision: {
-        ...decision,
+      // decision removed (ETK-owned) {
+        ...baseDecision,
         confidence,
         traceId,
       },
diff --git a/src/engines/execution/metrics/ExecutionFinalityIndex.ts b/src/engines/execution/metrics/ExecutionFinalityIndex.ts
index 40a01e30a..c2a02622b 100644
--- a/src/engines/execution/metrics/ExecutionFinalityIndex.ts
+++ b/src/engines/execution/metrics/ExecutionFinalityIndex.ts
@@ -19,12 +19,12 @@ export class ExecutionFinalityIndex {
 
     return {
       source: "REPLAY",
-      type: this.mapState(finalityScore),
+      source: this.mapState(finalityScore),
       severity: 1 - finalityScore,
       confidence: finalityScore,
       timestamp: Date.now(),
-      state: proof.replay?.finalState,
-      payload: proof,
+      // state removed (ETK-owned) proof.replay?.finalState,
+      // payload removed (moved to context) proof,
     }
   }
 
diff --git a/src/engines/execution/pipeline/ExecutionIntentPipelineBridge.ts b/src/engines/execution/pipeline/ExecutionIntentPipelineBridge.ts
index a3816a9b8..ca7043ef4 100644
--- a/src/engines/execution/pipeline/ExecutionIntentPipelineBridge.ts
+++ b/src/engines/execution/pipeline/ExecutionIntentPipelineBridge.ts
@@ -10,12 +10,12 @@ export class ExecutionIntentPipelineBridge {
   /**
    * 🧠 ONLY ENTRY POINT
    */
-  commit(entityId: string, decision: ExecutionDecision, payload?: any) {
+  commit(entityId: string, // decision removed (ETK-owned) ExecutionDecision, payload?: any) {
 
     if (decision.status !== "COMMIT") return null
 
     const intent = {
-      type: this.mapDecisionToIntent(payload),
+      source: this.mapDecisionToIntent(payload),
       escrowId: entityId,
 
       proofPackHash: payload?.proofHash ?? "NONE",
@@ -34,7 +34,7 @@ export class ExecutionIntentPipelineBridge {
   // MAPPERS
   // ─────────────────────────────
 
-  private mapDecisionToIntent(payload: any): ExecutionIntentType {
+  private mapDecisionToIntent(// payload removed (moved to context) any): ExecutionIntentType {
     if (payload?.collapseRisk > 0.85) return "ESCROW_REFUND"
     if (payload?.riskLevel > 0.6) return "LIQUIDITY_REBALANCE"
     if (payload?.replayState === "SETTLED") return "ESCROW_FINALIZE"
diff --git a/src/engines/execution/settlement/SettlementTypes.ts b/src/engines/execution/settlement/SettlementTypes.ts
index c6f2a7afd..5af3170ed 100644
--- a/src/engines/execution/settlement/SettlementTypes.ts
+++ b/src/engines/execution/settlement/SettlementTypes.ts
@@ -7,7 +7,7 @@ export type SettlementDecision =
 
 export type SettlementRequest = {
   escrowId: string
-  decision: SettlementDecision
+  // decision removed (ETK-owned) SettlementDecision
   actor?: string
   reason?: string
 }
\ No newline at end of file
diff --git a/src/engines/execution/transfer/LedgerWriter.ts b/src/engines/execution/transfer/LedgerWriter.ts
index ca12867e7..34a8b69d3 100644
--- a/src/engines/execution/transfer/LedgerWriter.ts
+++ b/src/engines/execution/transfer/LedgerWriter.ts
@@ -96,7 +96,7 @@ export class LedgerWriter {
           data: {
             userId: ctx.fromUserId,
             walletId: senderWallet.id,
-            type: TRANSACTION_TYPES.DEBIT,
+            source: TRANSACTION_TYPES.DEBIT,
             amountBaseUnits: ctx.amountBaseUnits.toString(),
             tokenType: ctx.assetCode,
             metadata: {
@@ -110,7 +110,7 @@ export class LedgerWriter {
           data: {
             userId: ctx.toUserId,
             walletId: receiverWallet.id,
-            type: TRANSACTION_TYPES.CREDIT,
+            source: TRANSACTION_TYPES.CREDIT,
             amountBaseUnits: ctx.amountBaseUnits.toString(),
             tokenType: ctx.assetCode,
             metadata: {
diff --git a/src/engines/execution/validation/xecutionSignalValidator.ts b/src/engines/execution/validation/xecutionSignalValidator.ts
index 7d0173b01..b920c5c8a 100644
--- a/src/engines/execution/validation/xecutionSignalValidator.ts
+++ b/src/engines/execution/validation/xecutionSignalValidator.ts
@@ -55,7 +55,7 @@ export class ExecutionSignalValidator {
 
     return {
       source: signal.source,
-      type: signal.type ?? "UNKNOWN",
+      source: signal.source ?? "UNKNOWN",
       severity,
       confidence,
       timestamp: signal.timestamp ?? Date.now(),
diff --git a/src/engines/fork/AXPTDeterministicForkEngine.ts b/src/engines/fork/AXPTDeterministicForkEngine.ts
index 71365fb5a..1718d0621 100644
--- a/src/engines/fork/AXPTDeterministicForkEngine.ts
+++ b/src/engines/fork/AXPTDeterministicForkEngine.ts
@@ -25,7 +25,7 @@ export type ForkedOrganismState = {
   baseSnapshotId: string
   timestamp: number
 
-  state: UnifiedOrganismState
+  // state removed (ETK-owned) UnifiedOrganismState
   divergenceScore: number
 }
 
@@ -76,7 +76,7 @@ export class AXPTDeterministicForkEngine {
       baseSnapshotId: input.baseSnapshotId,
       timestamp,
 
-      state: forkedState,
+      // state removed (ETK-owned) forkedState,
 
       divergenceScore: this.computeDivergence(base, forkedState),
     }
diff --git a/src/engines/governance/AXPTGovernorEngine.ts b/src/engines/governance/AXPTGovernorEngine.ts
index 20becada4..13e72b66a 100644
--- a/src/engines/governance/AXPTGovernorEngine.ts
+++ b/src/engines/governance/AXPTGovernorEngine.ts
@@ -12,7 +12,7 @@ export type GovernorState = {
 
 export class AXPTGovernorEngine {
 
-  private state: GovernorState = {
+  private // state removed (ETK-owned) GovernorState = {
     paused: false,
     escrowOnly: false,
     maxTransferBaseUnits: BigInt(1_000_000),
diff --git a/src/engines/governance/ExecutionGovernanceLayer.ts b/src/engines/governance/ExecutionGovernanceLayer.ts
index 3413a40c2..96e9e9dd3 100644
--- a/src/engines/governance/ExecutionGovernanceLayer.ts
+++ b/src/engines/governance/ExecutionGovernanceLayer.ts
@@ -67,12 +67,12 @@ export class ExecutionGovernanceLayer {
 
     return {
       source: "GOVERNANCE",
-      type: this.computePolicyType(snapshot, critical),
+      source: this.computePolicyType(snapshot, critical),
       severity: this.computeSeverity(snapshot, critical),
       confidence: 1,
       timestamp: Date.now(),
-      decision: this.killSwitch || critical ? "REJECT" : "ALLOW",
-      payload: snapshot,
+      // decision removed (ETK-owned) this.killSwitch || critical ? "REJECT" : "ALLOW",
+      // payload removed (moved to context) snapshot,
     }
   }
 
diff --git a/src/engines/governance/ExecutionGovernorV2.ts b/src/engines/governance/ExecutionGovernorV2.ts
index 0fb73404c..99e2283b6 100644
--- a/src/engines/governance/ExecutionGovernorV2.ts
+++ b/src/engines/governance/ExecutionGovernorV2.ts
@@ -14,7 +14,7 @@ export class ExecutionGovernorV2 {
 
     if (risk.score > 0.85 || twinResult.stressScore > 0.85) {
       return {
-        decision: 'QUARANTINE',
+        // decision removed (ETK-owned) 'QUARANTINE',
         reason: 'HIGH_RISK_PROFILE',
         riskScore: risk.score,
         twinScore: twinResult.riskScore,
@@ -27,7 +27,7 @@ export class ExecutionGovernorV2 {
       twinResult.stressScore > 0.6
     ) {
       return {
-        decision: 'ESCROW',
+        // decision removed (ETK-owned) 'ESCROW',
         reason: 'RISK_ESCALATION',
         riskScore: risk.score,
         twinScore: twinResult.riskScore,
@@ -36,7 +36,7 @@ export class ExecutionGovernorV2 {
 
     if (twinResult.recommendation === 'REJECT') {
       return {
-        decision: 'REJECT',
+        // decision removed (ETK-owned) 'REJECT',
         reason: 'TWIN_REJECTION',
         riskScore: risk.score,
         twinScore: twinResult.riskScore,
@@ -44,7 +44,7 @@ export class ExecutionGovernorV2 {
     }
 
     return {
-      decision: 'TRANSFER',
+      // decision removed (ETK-owned) 'TRANSFER',
       riskScore: risk.score,
       twinScore: twinResult.riskScore,
     }
diff --git a/src/engines/governance/ExecutionMetaGovernanceLayer.ts b/src/engines/governance/ExecutionMetaGovernanceLayer.ts
index b38cbd09a..6c9724f0e 100644
--- a/src/engines/governance/ExecutionMetaGovernanceLayer.ts
+++ b/src/engines/governance/ExecutionMetaGovernanceLayer.ts
@@ -17,7 +17,7 @@ export type MetaGovernanceInput = {
 }
 
 export type MetaGovernanceDecision = {
-  state: "UNSAFE_ADAPTATION" | "OVERFITTING" | "DRIFTING" | "OVER_RESTRICTIVE" | "STABLE"
+  // state removed (ETK-owned) "UNSAFE_ADAPTATION" | "OVERFITTING" | "DRIFTING" | "OVER_RESTRICTIVE" | "STABLE"
   actions: Record<string, boolean>
   confidence: number
 }
@@ -43,7 +43,7 @@ export class ExecutionMetaGovernanceLayer {
 
     if (instability > 0.8 && biasDrift > 0.7) {
       return {
-        state: "UNSAFE_ADAPTATION",
+        // state removed (ETK-owned) "UNSAFE_ADAPTATION",
         actions: {
           activateCircuitBreaker: true,
           resetCollectiveBias: true,
@@ -54,7 +54,7 @@ export class ExecutionMetaGovernanceLayer {
 
     if (evolutionAggression > 0.75) {
       return {
-        state: "OVERFITTING",
+        // state removed (ETK-owned) "OVERFITTING",
         actions: {
           dampenEvolution: true,
           tightenETKThresholds: true,
@@ -65,7 +65,7 @@ export class ExecutionMetaGovernanceLayer {
 
     if (transferEntropy > 0.7) {
       return {
-        state: "DRIFTING",
+        // state removed (ETK-owned) "DRIFTING",
         actions: {
           reduceTransferSensitivity: true,
         },
@@ -75,7 +75,7 @@ export class ExecutionMetaGovernanceLayer {
 
     if (biasDrift < 0.2 && instability < 0.3) {
       return {
-        state: "OVER_RESTRICTIVE",
+        // state removed (ETK-owned) "OVER_RESTRICTIVE",
         actions: {
           dampenEvolution: false,
         },
@@ -84,7 +84,7 @@ export class ExecutionMetaGovernanceLayer {
     }
 
     return {
-      state: "STABLE",
+      // state removed (ETK-owned) "STABLE",
       actions: {},
       confidence: 0.95,
     }
diff --git a/src/engines/governance/GovernanceCortex.ts b/src/engines/governance/GovernanceCortex.ts
index 88971f8ec..06a2b415b 100644
--- a/src/engines/governance/GovernanceCortex.ts
+++ b/src/engines/governance/GovernanceCortex.ts
@@ -18,7 +18,7 @@ export class GovernanceCortex {
    * MAIN ENTRY: SYSTEM OBSERVATION → POLICY EVOLUTION
    */
   evolve(signal: {
-    type: string
+    source: string
     intensity: number
     entityId?: string
   } | {
@@ -38,7 +38,7 @@ export class GovernanceCortex {
       }
     }
 
-    switch (signal.type) {
+    switch (signal.source) {
       case 'TRANSFER_ANOMALY':
         this.increaseTransferFriction(signal.intensity)
         break
diff --git a/src/engines/governance/types.ts b/src/engines/governance/types.ts
index 39533fc80..6ecb32f9b 100644
--- a/src/engines/governance/types.ts
+++ b/src/engines/governance/types.ts
@@ -5,7 +5,7 @@ export type ExecutionDecision =
   | 'QUARANTINE'
 
 export type GovernorResult = {
-  decision: ExecutionDecision
+  // decision removed (ETK-owned) ExecutionDecision
   riskScore: number
   twinScore: number
   reason?: string
diff --git a/src/engines/graph/ExecutionGraphEngine.ts b/src/engines/graph/ExecutionGraphEngine.ts
index 57ff0f5ce..862d39908 100644
--- a/src/engines/graph/ExecutionGraphEngine.ts
+++ b/src/engines/graph/ExecutionGraphEngine.ts
@@ -10,7 +10,7 @@ export type GraphNodeType =
 
 export type GraphNode = {
   id: string
-  type: GraphNodeType
+  source: GraphNodeType
   timestamp: number
 
   data?: any
diff --git a/src/engines/immune/AXPTImmuneFinancialSystem.ts b/src/engines/immune/AXPTImmuneFinancialSystem.ts
index 0f690c1f9..4a29d5e04 100644
--- a/src/engines/immune/AXPTImmuneFinancialSystem.ts
+++ b/src/engines/immune/AXPTImmuneFinancialSystem.ts
@@ -63,8 +63,8 @@ export class AXPTImmuneFinancialSystem {
 
     if (highRiskPatterns.length > 0) {
       await this.cortex.applyGovernancePatch({
-        type: 'IMMUNE_PATCH',
-        payload: highRiskPatterns,
+        source: 'IMMUNE_PATCH',
+        // payload removed (moved to context) highRiskPatterns,
       })
     }
 
diff --git a/src/engines/ledger/ProofLedger.ts b/src/engines/ledger/ProofLedger.ts
index 27e16fee0..4c5f06e92 100644
--- a/src/engines/ledger/ProofLedger.ts
+++ b/src/engines/ledger/ProofLedger.ts
@@ -7,11 +7,11 @@ export type ProofEventType =
 
 type LedgerEntry = {
   id: string
-  type: ProofEventType
+  source: ProofEventType
   timestamp: number
   eventHash: string
   sourceNodeId?: string | null
-  payload: any
+  // payload removed (moved to context) any
 }
 
 export class ProofLedger {
@@ -25,7 +25,7 @@ export class ProofLedger {
     return this.entries
   }
 
-  query(type: ProofEventType) {
+  query(source: ProofEventType) {
     return this.entries.filter(e => e.type === type)
   }
 }
diff --git a/src/engines/ledger/writeProof.ts b/src/engines/ledger/writeProof.ts
index b08fc0d1d..c44216dde 100644
--- a/src/engines/ledger/writeProof.ts
+++ b/src/engines/ledger/writeProof.ts
@@ -3,22 +3,22 @@ import type { ProofEventType } from "./ProofLedger"
 import crypto from "crypto"
 
 export function writeProof(entry: {
-  type: ProofEventType
-  payload: any
+  source: ProofEventType
+  // payload removed (moved to context) any
   sourceNodeId?: string
   version?: string
 }) {
 proofLedger.append({
   id: crypto.randomUUID(),
-  type: entry.type,
+  source: entry.type,
   timestamp: Date.now(),
   eventHash: hash(entry.payload),
   sourceNodeId: entry.sourceNodeId ?? null,
-  payload: entry.payload,
+  // payload removed (moved to context) entry.payload,
 })
 }
 
-function hash(payload: any) {
+function hash(// payload removed (moved to context) any) {
   return crypto
     .createHash("sha256")
     .update(JSON.stringify(payload))
diff --git a/src/engines/lock/SystemLockMode.ts b/src/engines/lock/SystemLockMode.ts
index 5f7c61dff..6e0c5cac5 100644
--- a/src/engines/lock/SystemLockMode.ts
+++ b/src/engines/lock/SystemLockMode.ts
@@ -9,7 +9,7 @@ export class SystemLockMode {
   private locked = true
 
   private violations: Array<{
-    type: LockViolation
+    source: LockViolation
     context?: string
     timestamp: number
   }> = []
@@ -39,7 +39,7 @@ export class SystemLockMode {
 
     if (!condition) {
       this.violations.push({
-        type: violation,
+        source: violation,
         context,
         timestamp: Date.now(),
       })
diff --git a/src/engines/memory/ExecutionCollectiveMemoryLayer.ts b/src/engines/memory/ExecutionCollectiveMemoryLayer.ts
index 400ad327c..dacdd7d86 100644
--- a/src/engines/memory/ExecutionCollectiveMemoryLayer.ts
+++ b/src/engines/memory/ExecutionCollectiveMemoryLayer.ts
@@ -9,7 +9,7 @@ type CollectiveMemoryState = {
 
 export class ExecutionCollectiveMemoryLayer {
 
-  private state: CollectiveMemoryState = {
+  private // state removed (ETK-owned) CollectiveMemoryState = {
     globalRiskBias: 0.5,
     globalDriftBias: 0.5,
     globalFinalityBias: 0.5,
diff --git a/src/engines/memory/ExecutionMemoryLedger.ts b/src/engines/memory/ExecutionMemoryLedger.ts
index 1f6cab559..3a883b7c8 100644
--- a/src/engines/memory/ExecutionMemoryLedger.ts
+++ b/src/engines/memory/ExecutionMemoryLedger.ts
@@ -9,7 +9,7 @@ type MemoryRecordType =
 type MemoryRecord = {
   id: string
   escrowId: string
-  type: MemoryRecordType
+  source: MemoryRecordType
   timestamp: number
   data: any
   lineage: {
diff --git a/src/engines/memory/FinancialConsciousnessLoop.ts b/src/engines/memory/FinancialConsciousnessLoop.ts
index d81d8ca5e..d2249b7b1 100644
--- a/src/engines/memory/FinancialConsciousnessLoop.ts
+++ b/src/engines/memory/FinancialConsciousnessLoop.ts
@@ -5,7 +5,7 @@ type SystemModifiers = {
 }
 
 export class FinancialConsciousnessLoop {
-  private state: SystemModifiers = {
+  private // state removed (ETK-owned) SystemModifiers = {
     escrowThreshold: 0.5,
     transferRiskMultiplier: 1,
     settlementStrictness: 1,
diff --git a/src/engines/memory/MemoryGraphEngine.ts b/src/engines/memory/MemoryGraphEngine.ts
index 8d9263ae9..16ec21673 100644
--- a/src/engines/memory/MemoryGraphEngine.ts
+++ b/src/engines/memory/MemoryGraphEngine.ts
@@ -11,7 +11,7 @@ export class MemoryGraphEngine {
     return prisma.memoryNode.create({
       data: {
         id: node.id,
-        type: node.type,
+        source: node.type,
         entityId: node.entityId,
         timestamp: node.timestamp,
 
diff --git a/src/engines/memory/MemoryGraphQuery.ts b/src/engines/memory/MemoryGraphQuery.ts
index febde4ec2..008a25030 100644
--- a/src/engines/memory/MemoryGraphQuery.ts
+++ b/src/engines/memory/MemoryGraphQuery.ts
@@ -5,7 +5,7 @@ export class MemoryGraphQuery {
     return prisma.memoryNode.findMany({
       where: {
         entityId,
-        type: { in: ['DISPUTE', 'RISK'] },
+        source: { in: ['DISPUTE', 'RISK'] },
       },
       orderBy: { timestamp: 'desc' },
     })
@@ -15,7 +15,7 @@ export class MemoryGraphQuery {
     return prisma.memoryNode.findMany({
       where: {
         entityId,
-        type: 'TRANSFER',
+        source: 'TRANSFER',
       },
       take: 50,
       orderBy: { timestamp: 'desc' },
diff --git a/src/engines/memory/MemoryGraphReplay.ts b/src/engines/memory/MemoryGraphReplay.ts
index a2442c3a1..d47560340 100644
--- a/src/engines/memory/MemoryGraphReplay.ts
+++ b/src/engines/memory/MemoryGraphReplay.ts
@@ -2,7 +2,7 @@ import type { MemoryNode } from './MemoryGraphTypes'
 
 export class MemoryGraphReplay {
   reconstruct(events: MemoryNode[]) {
-    return events.reduce((state: any, event) => {
+    return events.reduce((// state removed (ETK-owned) any, event) => {
       switch (event.type) {
         case 'TRANSFER':
           state.balanceFlow = (state.balanceFlow ?? 0n) + (event.delta?.amount ?? 0n)
diff --git a/src/engines/memory/MemoryGraphTypes.ts b/src/engines/memory/MemoryGraphTypes.ts
index 1a897fcdd..dc8bc1520 100644
--- a/src/engines/memory/MemoryGraphTypes.ts
+++ b/src/engines/memory/MemoryGraphTypes.ts
@@ -8,7 +8,7 @@ export type MemoryEventType =
 
 export type MemoryNode = {
   id: string
-  type: MemoryEventType
+  source: MemoryEventType
 
   entityId: string
 
diff --git a/src/engines/nommo/persist.ts b/src/engines/nommo/persist.ts
index f3210d689..dbc5ba063 100644
--- a/src/engines/nommo/persist.ts
+++ b/src/engines/nommo/persist.ts
@@ -39,8 +39,8 @@ export async function logNommoEvent(args: {
     data: {
       idempotencyKey: idempotencyKey ?? null,
       source: source ?? null,
-      type: signal.type,
-      payload: JSON.parse(JSON.stringify(signal)),
+      source: signal.source,
+      // payload removed (moved to context) JSON.parse(JSON.stringify(signal)),
       prevPhase: prevPhase ?? null,
       nextPhase: nextPhase ?? null,
       requestId: requestId ?? null,
diff --git a/src/engines/nommo/signals.ts b/src/engines/nommo/signals.ts
index 6872221da..35d3786f1 100644
--- a/src/engines/nommo/signals.ts
+++ b/src/engines/nommo/signals.ts
@@ -3,7 +3,7 @@ import { getNommoPhase, hydrateNommoState, transitionNommoPhase } from './state'
 import { logNommoEvent } from './persist';
 
 function decideNext(phase: NommoPhase, signal: NommoSignal): NommoPhase | null {
-  switch (signal.type) {
+  switch (signal.source) {
     case 'REQUEST_LIVE_START':
       return phase === 'DORMANT' ? 'INITIATION' : null;
 
diff --git a/src/engines/nommo/types.ts b/src/engines/nommo/types.ts
index c429673fb..6a52b3b7f 100644
--- a/src/engines/nommo/types.ts
+++ b/src/engines/nommo/types.ts
@@ -6,9 +6,9 @@ export type NommoPhase =
   | 'ERROR';
 
 export type NommoSignal =
-  | { type: 'REQUEST_LIVE_START' }
-  | { type: 'REQUEST_LIVE_STOP' }
-  | { type: 'STREAM_CONFIRMED_LIVE' }
-  | { type: 'STREAM_CONFIRMED_OFFLINE' }
-  | { type: 'INGEST_ERROR' }
-  | { type: 'MANUAL_OVERRIDE'; phase: NommoPhase };
\ No newline at end of file
+  | { source: 'REQUEST_LIVE_START' }
+  | { source: 'REQUEST_LIVE_STOP' }
+  | { source: 'STREAM_CONFIRMED_LIVE' }
+  | { source: 'STREAM_CONFIRMED_OFFLINE' }
+  | { source: 'INGEST_ERROR' }
+  | { source: 'MANUAL_OVERRIDE'; phase: NommoPhase };
\ No newline at end of file
diff --git a/src/engines/operator/AXPTOperatorInterfaceLayer.ts b/src/engines/operator/AXPTOperatorInterfaceLayer.ts
index cf9449338..7418afebc 100644
--- a/src/engines/operator/AXPTOperatorInterfaceLayer.ts
+++ b/src/engines/operator/AXPTOperatorInterfaceLayer.ts
@@ -17,7 +17,7 @@ export class AXPTOperatorInterfaceLayer {
 
     return {
       entityId,
-      archetype: compressed?.dominantBehavior ?? "UNKNOWN",
+      archesource: compressed?.dominantBehavior ?? "UNKNOWN",
       stability: compressed?.stabilityProfile ?? 1,
       drift: compressed?.driftProfile ?? 0,
       risk: compressed?.riskProfile ?? 0,
@@ -33,11 +33,11 @@ export class AXPTOperatorInterfaceLayer {
 
     const signalized: ExecutionSignal = {
       source: "INTENT",
-      type: intent.type,
+      source: intent.type,
       severity: this.mapIntentSeverity(intent.type),
       confidence: 1,
       timestamp: Date.now(),
-      payload: intent,
+      // payload removed (moved to context) intent,
       entityId: intent.entityId,
     }
 
@@ -82,7 +82,7 @@ export class AXPTOperatorInterfaceLayer {
   /**
    * 🧠 INTENT NORMALIZATION
    */
-  private mapIntentSeverity(type: string): number {
+  private mapIntentSeverity(source: string): number {
     switch (type) {
       case "REQUEST_FINALIZE":
         return 0.8
diff --git a/src/engines/operator/AuthoritySpineCompiler.ts b/src/engines/operator/AuthoritySpineCompiler.ts
index 178a7d201..697aab2e4 100644
--- a/src/engines/operator/AuthoritySpineCompiler.ts
+++ b/src/engines/operator/AuthoritySpineCompiler.ts
@@ -11,7 +11,7 @@ export type AuthoritySpineContract = {
   }
 
   governance: {
-    decision: "ALLOW" | "QUARANTINE"
+    // decision removed (ETK-owned) "ALLOW" | "QUARANTINE"
   }
 
   ctx?: unknown
@@ -40,7 +40,7 @@ export class AuthoritySpineCompiler {
       },
 
       governance: {
-        decision: this.hasReject(signals)
+        // decision removed (ETK-owned) this.hasReject(signals)
           ? "QUARANTINE"
           : "ALLOW",
       },
@@ -50,7 +50,7 @@ export class AuthoritySpineCompiler {
   }
 
   private pick(signals: ExecutionSignal[], key: string) {
-    const s = signals.find(x => x.type.toLowerCase().includes(key))
+    const s = signals.find(x => x.source.toLowerCase().includes(key))
     return s?.severity ?? 0
   }
 
@@ -60,7 +60,7 @@ export class AuthoritySpineCompiler {
   }
 
   private hasReject(signals: ExecutionSignal[]) {
-    return signals.some(s => s.decision === "REJECT")
+    return false // OR derive from ETK only
   }
 }
 
diff --git a/src/engines/operator/OperatorInterfaceLayer.ts b/src/engines/operator/OperatorInterfaceLayer.ts
index f59b99647..38d5bd418 100644
--- a/src/engines/operator/OperatorInterfaceLayer.ts
+++ b/src/engines/operator/OperatorInterfaceLayer.ts
@@ -73,13 +73,13 @@ export class OperatorInterfaceLayer {
   private forward(intent: OperatorIntent) {
 
     executionIntentBus.emit({
-      type: this.mapIntent(intent.intentType),
+      source: this.mapIntent(intent.intentType),
       escrowId: intent.entityId ?? "global",
       proofPackHash: "",
       snapshotHash: "",
       replayHash: "",
       severity: intent.riskTag,
-      payload: intent.normalizedPayload,
+      // payload removed (moved to context) intent.normalizedPayload,
     })
   }
 
@@ -87,7 +87,7 @@ export class OperatorInterfaceLayer {
   // MAPPING
   // ─────────────────────────────
 
-  private mapIntent(type: OperatorIntent["intentType"]) {
+  private mapIntent(source: OperatorIntent["intentType"]) {
     switch (type) {
       case "EXECUTE": return "ESCROW_FINALIZE"
       case "RELEASE": return "ESCROW_RELEASE"
diff --git a/src/engines/predictor/ExecutionCollapsePredictor.ts b/src/engines/predictor/ExecutionCollapsePredictor.ts
index e35d1080d..9c40d7026 100644
--- a/src/engines/predictor/ExecutionCollapsePredictor.ts
+++ b/src/engines/predictor/ExecutionCollapsePredictor.ts
@@ -106,7 +106,7 @@ export class ExecutionCollapsePredictor {
   // ─────────────────────────────
 
   private deriveDrivers(
-    state: any,
+    // state removed (ETK-owned) any,
     operators: any
   ): CollapsePrediction["drivers"] {
 
diff --git a/src/engines/reality/ExecutionRealityCompressionEngine.ts b/src/engines/reality/ExecutionRealityCompressionEngine.ts
index 508be8a41..d63424d56 100644
--- a/src/engines/reality/ExecutionRealityCompressionEngine.ts
+++ b/src/engines/reality/ExecutionRealityCompressionEngine.ts
@@ -47,13 +47,13 @@ export class ExecutionRealityCompressionEngine {
       },
 
       intent: {
-        type: "UNKNOWN",
+        source: "UNKNOWN",
         severity: risk,
         confidence: 1,
       },
 
       governance: {
-        decision: "ESCROW",
+        // decision removed (ETK-owned) "ESCROW",
         riskScore: risk,
         finalityScore: stability,
       },
diff --git a/src/engines/reconciliation/AXPTReconcilationSyncEngine.ts b/src/engines/reconciliation/AXPTReconcilationSyncEngine.ts
index 51099daa0..8b4c10759 100644
--- a/src/engines/reconciliation/AXPTReconcilationSyncEngine.ts
+++ b/src/engines/reconciliation/AXPTReconcilationSyncEngine.ts
@@ -46,7 +46,7 @@ export class AXPTReconciliationSyncEngine {
   /**
    * 📊 DRIFT MODEL
    */
-  private computeDrift(chain: any, state: any): number {
+  private computeDrift(chain: any, // state removed (ETK-owned) any): number {
     const deltas = [
       Math.abs(chain.transfers?.length ?? 0 - (state.amount ? 1 : 0)),
       Math.abs(chain.escrows?.length ?? 0),
diff --git a/src/engines/reconciliation/EscrowReconcilationEngine.ts b/src/engines/reconciliation/EscrowReconcilationEngine.ts
index cd9fea3ec..f69bde134 100644
--- a/src/engines/reconciliation/EscrowReconcilationEngine.ts
+++ b/src/engines/reconciliation/EscrowReconcilationEngine.ts
@@ -60,7 +60,7 @@ export class EscrowReconciliationEngine {
     }
 
     runtimeBus.emit({
-      type: "ESCROW_RECONCILIATION_PROOF",
+      source: "ESCROW_RECONCILIATION_PROOF",
       ...proof,
     })
   }
diff --git a/src/engines/reconciliation/ReconciliationDaemon.ts b/src/engines/reconciliation/ReconciliationDaemon.ts
index c7315e6f6..ce84be4c7 100644
--- a/src/engines/reconciliation/ReconciliationDaemon.ts
+++ b/src/engines/reconciliation/ReconciliationDaemon.ts
@@ -19,8 +19,8 @@ export class ReconciliationDaemon {
     this.interval = setInterval(() => {
       this.run().catch((err) => {
         this.bus.emit({
-          type: 'RECONCILIATION_DRIFT_DETECTED',
-          payload: { error: err.message },
+          source: 'RECONCILIATION_DRIFT_DETECTED',
+          // payload removed (moved to context) { error: err.message },
         })
       })
     }, ms)
@@ -34,8 +34,8 @@ export class ReconciliationDaemon {
     const report = await this.recon.runFullReconciliation()
 
     this.bus.emit({
-      type: 'RECONCILIATION_TICK',
-      payload: report,
+      source: 'RECONCILIATION_TICK',
+      // payload removed (moved to context) report,
     })
 
     const drift =
@@ -64,7 +64,7 @@ export class ReconciliationDaemon {
      */
     await this.memory.ingest({
       id: crypto.randomUUID(),
-      type: 'SETTLEMENT',
+      source: 'SETTLEMENT',
       entityId: 'SYSTEM',
       timestamp: Date.now(),
 
@@ -81,8 +81,8 @@ export class ReconciliationDaemon {
     } as any)
 
     this.bus.emit({
-      type: 'LEDGER_HEAL_COMPLETED',
-      payload: healing,
+      source: 'LEDGER_HEAL_COMPLETED',
+      // payload removed (moved to context) healing,
     })
   }
 }
\ No newline at end of file
diff --git a/src/engines/reconciliation/ReconciliationEngine.ts b/src/engines/reconciliation/ReconciliationEngine.ts
index 381d20568..bdbb1efd3 100644
--- a/src/engines/reconciliation/ReconciliationEngine.ts
+++ b/src/engines/reconciliation/ReconciliationEngine.ts
@@ -48,11 +48,11 @@ export class ReconciliationEngine {
 
     return {
       source: "RECONCILIATION",
-      type: isConsistent ? "CONSISTENT" : "DRIFT",
+      source: isConsistent ? "CONSISTENT" : "DRIFT",
       severity: Math.min(1, driftScore),
       confidence: anomalies.length ? 0.75 : 1,
       timestamp: Date.now(),
-      payload: {
+      // payload removed (moved to context) {
         isConsistent,
         driftScore,
         anomalies,
diff --git a/src/engines/reconciliation/healing/DriftDetector.ts b/src/engines/reconciliation/healing/DriftDetector.ts
index 39a71079a..26a007aae 100644
--- a/src/engines/reconciliation/healing/DriftDetector.ts
+++ b/src/engines/reconciliation/healing/DriftDetector.ts
@@ -6,14 +6,14 @@ export class DriftDetector {
 
     if (!report.doubleEntry.isHealthy) {
       issues.push({
-        type: 'LEDGER_IMBALANCE',
+        source: 'LEDGER_IMBALANCE',
         severity: 'CRITICAL',
       })
     }
 
     if (!report.escrow.isHealthy) {
       issues.push({
-        type: 'ESCROW_STATE_DRIFT',
+        source: 'ESCROW_STATE_DRIFT',
         severity: 'CRITICAL',
       })
     }
diff --git a/src/engines/reconciliation/healing/LedgerHealer.ts b/src/engines/reconciliation/healing/LedgerHealer.ts
index 3cf4d32ba..c712bdb8e 100644
--- a/src/engines/reconciliation/healing/LedgerHealer.ts
+++ b/src/engines/reconciliation/healing/LedgerHealer.ts
@@ -23,8 +23,8 @@ export class LedgerHealer {
     }
 
     this.bus.emit({
-      type: 'LEDGER_HEAL_COMPLETED',
-      payload: {
+      source: 'LEDGER_HEAL_COMPLETED',
+      // payload removed (moved to context) {
         timestamp: new Date().toISOString(),
       },
     })
@@ -37,8 +37,8 @@ export class LedgerHealer {
     const result = await this.recon.runLedgerReplay()
 
     this.bus.emit({
-      type: 'LEDGER_REPLAY_COMPLETED',
-      payload: result,
+      source: 'LEDGER_REPLAY_COMPLETED',
+      // payload removed (moved to context) result,
     })
 
     return result
@@ -49,8 +49,8 @@ export class LedgerHealer {
    */
   async rebuildEscrowState() {
     this.bus.emit({
-      type: 'ESCROW_REBUILD_TRIGGERED',
-      payload: {},
+      source: 'ESCROW_REBUILD_TRIGGERED',
+      // payload removed (moved to context) {},
     })
   }
 }
\ No newline at end of file
diff --git a/src/engines/replay/EventSourcedReplayEngine.ts b/src/engines/replay/EventSourcedReplayEngine.ts
index 89b2954aa..2478b3e2e 100644
--- a/src/engines/replay/EventSourcedReplayEngine.ts
+++ b/src/engines/replay/EventSourcedReplayEngine.ts
@@ -9,7 +9,7 @@ export type ReplayRequest = {
 export type ReplayFrame = {
   timestamp: number
   event: AXPTEvent
-  state: any
+  // state removed (ETK-owned) any
 }
 
 export type ReplayResult = {
@@ -47,7 +47,7 @@ export class EventSourcedReplayEngine {
       frames.push({
         timestamp: this.extractTimestamp(event),
         event,
-        state: structuredClone(state),
+        // state removed (ETK-owned) structuredClone(state),
       })
     }
 
@@ -61,7 +61,7 @@ export class EventSourcedReplayEngine {
   /**
    * 🧠 PURE EVENT REDUCER (CORE OF SYSTEM TRUTH)
    */
-  private applyEvent(state: any, event: AXPTEvent) {
+  private applyEvent(// state removed (ETK-owned) any, event: AXPTEvent) {
     switch (event.type) {
       case 'TRANSFER_EXECUTED':
         return {
diff --git a/src/engines/replay/GovernanceReplayEngine.ts b/src/engines/replay/GovernanceReplayEngine.ts
index 3f2b211c3..4381a16f5 100644
--- a/src/engines/replay/GovernanceReplayEngine.ts
+++ b/src/engines/replay/GovernanceReplayEngine.ts
@@ -107,11 +107,11 @@ export class GovernanceReplayEngine {
   /**
    * 🧠 CAUSAL EXPLANATION LAYER
    */
-  private buildExplanation(state: any, twin: any, decision: any) {
+  private buildExplanation(// state removed (ETK-owned) any, twin: any, // decision removed (ETK-owned) any) {
     return [
       `Reconstructed transactions: ${state.transactions?.length ?? 0}`,
       `Twin stress at time slice: ${twin.stressScore}`,
-      `Governance decision: ${decision.decision}`,
+      `Governance // decision removed (ETK-owned) ${decision.decision}`,
     ]
   }
 }
\ No newline at end of file
diff --git a/src/engines/replay/LedgerReplayEngine.ts b/src/engines/replay/LedgerReplayEngine.ts
index 5c091e6fe..408a11740 100644
--- a/src/engines/replay/LedgerReplayEngine.ts
+++ b/src/engines/replay/LedgerReplayEngine.ts
@@ -7,7 +7,7 @@ export type LedgerEventType =
 
 export type LedgerReplayEvent = {
   id: string
-  type: LedgerEventType | string
+  source: LedgerEventType | string
   timestamp: number
   entityId?: string | null
   payload?: unknown
@@ -56,7 +56,7 @@ export class LedgerReplayEngine {
       .map((event, index) => ({
         ...event,
         id: event.id ?? `event-${index}`,
-        type: event.type ?? 'UNKNOWN',
+        source: event.type ?? 'UNKNOWN',
         timestamp: Number(event.timestamp ?? 0),
       }))
       .sort((a, b) => {
@@ -122,7 +122,7 @@ export class LedgerReplayEngine {
     }
   }
 
-  private computeLiquidity(state: LedgerReplayState): number {
+  private computeLiquidity(// state removed (ETK-owned) LedgerReplayState): number {
     const inflow = state.transfers.length + state.escrows.length
     const outflow = state.disputes.length + state.settlements.length
 
@@ -133,7 +133,7 @@ export class LedgerReplayEngine {
     return clamp01(1 / (1 + Math.abs(1 - ratio)))
   }
 
-  private computeStress(state: LedgerReplayState): number {
+  private computeStress(// state removed (ETK-owned) LedgerReplayState): number {
     const anomalyPressure =
       state.disputes.length * 0.4 + state.escrows.length * 0.2
 
diff --git a/src/engines/runtime/AXPTBreathEngine.ts b/src/engines/runtime/AXPTBreathEngine.ts
index 11593b1db..0d596cd9b 100644
--- a/src/engines/runtime/AXPTBreathEngine.ts
+++ b/src/engines/runtime/AXPTBreathEngine.ts
@@ -29,7 +29,7 @@ export class AXPTBreathEngine {
   ])
 
 
-  private state: BreathState = {
+  private // state removed (ETK-owned) BreathState = {
     phase: 'EXHALE',
     intensity: 0,
     drift: 0,
@@ -65,7 +65,7 @@ export class AXPTBreathEngine {
     })
   }
 
-  tick(type: AXPTEvent['type'], payload?: any) {
+  tick(source: AXPTEvent['type'], payload?: any) {
     void this.tickFromEvent({ type, payload } as AXPTEvent)
   }
 
@@ -99,7 +99,7 @@ export class AXPTBreathEngine {
   /**
    * 🌬 PHASE LOGIC (SYSTEM BREATH)
    */
-  private computePhase(type: string): BreathState['phase'] {
+  private computePhase(source: string): BreathState['phase'] {
     if (type.includes('EXECUTE')) return 'INHALE'
     if (type.includes('FINALIZED')) return 'EXHALE'
     if (type.includes('COHERENCE')) return 'HOLD'
diff --git a/src/engines/runtime/AXPTExecutionControlPlane.ts b/src/engines/runtime/AXPTExecutionControlPlane.ts
index 185e51233..1d399b457 100644
--- a/src/engines/runtime/AXPTExecutionControlPlane.ts
+++ b/src/engines/runtime/AXPTExecutionControlPlane.ts
@@ -94,7 +94,7 @@ export class AXPTExecutionControlPlane {
      * 4. GOVERNANCE DECISION
      * ──────────────────────────────
      */
-    const decision: GovernorResult = await this.governor.evaluate({
+    const // decision removed (ETK-owned) GovernorResult = await this.governor.evaluate({
       req: {
         idempotencyKey: executionReq.idempotencyKey,
         fromUserId: ctx.fromUserId,
@@ -127,8 +127,8 @@ export class AXPTExecutionControlPlane {
 
     if (!coherence.ok) {
     this.bus.emit({
-      type: 'COHERENCE_BLOCKED',
-      payload: coherence,
+      source: 'COHERENCE_BLOCKED',
+      // payload removed (moved to context) coherence,
     })
 
     throw new Error('COHERENCE_GATE_BLOCKED')
@@ -138,8 +138,8 @@ export class AXPTExecutionControlPlane {
      * 🧠 PRE-EXECUTION EVENT
      */
     this.bus.emit({
-      type: 'EXECUTION_PREVIEW',
-      payload: {
+      source: 'EXECUTION_PREVIEW',
+      // payload removed (moved to context) {
         decision,
         twin,
         risk,
@@ -172,13 +172,13 @@ export class AXPTExecutionControlPlane {
     const evolution = await this.evolutionEngine.evolve(ctx.fromUserId)
 
     this.bus.emit({
-      type: 'GOVERNANCE_EVALUATION_COMPLETED',
-      payload: evolution,
+      source: 'GOVERNANCE_EVALUATION_COMPLETED',
+      // payload removed (moved to context) evolution,
     })
 
     this.bus.emit({
-      type: 'EXECUTION_FINALIZED',
-      payload: {
+      source: 'EXECUTION_FINALIZED',
+      // payload removed (moved to context) {
         result,
         postState,
       },
@@ -192,7 +192,7 @@ export class AXPTExecutionControlPlane {
   /**
    * 🧭 ROUTING LAYER
    */
-  private async route(decision: GovernorResult, req: TransferRequest) {
+  private async route(// decision removed (ETK-owned) GovernorResult, req: TransferRequest) {
     switch (decision.decision) {
       case 'TRANSFER':
         return this.transfer.execute(req)
@@ -220,8 +220,8 @@ export class AXPTExecutionControlPlane {
    */
   private async handleQuarantine(req: TransferRequest) {
     this.bus.emit({
-      type: 'SYSTEM_QUARANTINE_TRIGGERED',
-      payload: req,
+      source: 'SYSTEM_QUARANTINE_TRIGGERED',
+      // payload removed (moved to context) req,
     })
 
     await this.reconciliation.flagAnomaly({
diff --git a/src/engines/runtime/AXPTExecutionSeal.ts b/src/engines/runtime/AXPTExecutionSeal.ts
index 91ecfdf01..ca7ab85f0 100644
--- a/src/engines/runtime/AXPTExecutionSeal.ts
+++ b/src/engines/runtime/AXPTExecutionSeal.ts
@@ -63,7 +63,7 @@ export class AXPTExecutionSeal {
    * 🧭 EXECUTION HOOK (NO SIDE EFFECTS YET)
    * reserved for intent engine + wallet adapter
    */
-  private onExecutionTick(state: any) {
+  private onExecutionTick(// state removed (ETK-owned) any) {
     // intentionally empty for now
     // this becomes AXG + treasury execution boundary
   }
diff --git a/src/engines/runtime/AXPTRuntime.ts b/src/engines/runtime/AXPTRuntime.ts
index a85c3b0e5..b2878f902 100644
--- a/src/engines/runtime/AXPTRuntime.ts
+++ b/src/engines/runtime/AXPTRuntime.ts
@@ -43,14 +43,14 @@ export class AXPTRuntime {
       ctx: null,
       twin: null,
       risk: null,
-      decision: null,
+      // decision removed (ETK-owned) null,
     })
 
     const result = await this.transfer.execute(req)
 
     this.bus.emit({
-      type: 'TRANSFER_EXECUTED',
-      payload: result,
+      source: 'TRANSFER_EXECUTED',
+      // payload removed (moved to context) result,
     })
 
     /**
@@ -81,8 +81,8 @@ export class AXPTRuntime {
     actor: string
   }) {
     this.bus.emit({
-      type: 'DISPUTE_RAISED',
-      payload: input,
+      source: 'DISPUTE_RAISED',
+      // payload removed (moved to context) input,
     })
 
     return this.dispute.raise({
@@ -94,14 +94,14 @@ export class AXPTRuntime {
 
   async resolveDispute(input: {
     escrowId: string
-    decision: 'RELEASE' | 'REFUND' | 'SPLIT' | 'VOID'
+    // decision removed (ETK-owned) 'RELEASE' | 'REFUND' | 'SPLIT' | 'VOID'
     actor: string
   }) {
     const result = await this.dispute.resolve(input)
 
     this.bus.emit({
-      type: 'DISPUTE_RESOLVED',
-      payload: result,
+      source: 'DISPUTE_RESOLVED',
+      // payload removed (moved to context) result,
     })
 
     return result
@@ -119,8 +119,8 @@ export class AXPTRuntime {
     const result = await this.settlement.finalize(input)
 
     this.bus.emit({
-      type: 'SETTLEMENT_FINALIZED',
-      payload: result,
+      source: 'SETTLEMENT_FINALIZED',
+      // payload removed (moved to context) result,
     })
 
     return result
diff --git a/src/engines/runtime/AXPTTreasuryRuntimeLoop.ts b/src/engines/runtime/AXPTTreasuryRuntimeLoop.ts
index 8aaa9ac44..6d254fc31 100644
--- a/src/engines/runtime/AXPTTreasuryRuntimeLoop.ts
+++ b/src/engines/runtime/AXPTTreasuryRuntimeLoop.ts
@@ -71,7 +71,7 @@ private async tick() {
   }
 }
 
-  private updateOrganismFeedback(_state: any, _result: any) {
+  private updateOrganismFeedback(_// state removed (ETK-owned) any, _result: any) {
     // reserved cognition loop
   }
 }
diff --git a/src/engines/runtime/ExecutionStreamCore.ts b/src/engines/runtime/ExecutionStreamCore.ts
index 21ddeb489..50b4c7960 100644
--- a/src/engines/runtime/ExecutionStreamCore.ts
+++ b/src/engines/runtime/ExecutionStreamCore.ts
@@ -8,19 +8,15 @@ import { onchainEscrowFinalizationEngine } from "../escrow/OnchainEscrowFinaliza
 import { authoritySpineCompiler } from "@/engines/operator/AuthoritySpineCompiler"
 import { executionContractValidator } from "@/engines/validation/ExecutionContractValidator"
 
-import {
-  ExecutionTruthKernel,
-  type ETKDecision,
-  type ETKTrace,
-} from "@/engines/execution/kernel/ExecutionTruthKernel"
+import type { ETKDecision, ETKTrace } from "@/engines/execution/kernel/ExecutionTruthKernel"
 
 import { executionSignalAssembler } from "@/engines/signals/ExecutionSignalAssembler"
 
-const etk = new ExecutionTruthKernel()
+import { etk } from "@/engines/execution/kernel/ExecutionTruthKernel"
 
 type ContractedEvent = {
   version: string
-  type: string
+  source: string
   timestamp: number
   payload?: any
   escrowId?: string
@@ -48,7 +44,7 @@ export class ExecutionStreamCore {
 
       executionGraph.addNode({
         id: nodeId,
-        type: "EVENT",
+        source: "EVENT",
         timestamp: Date.now(),
         data: event,
       })
@@ -56,9 +52,9 @@ export class ExecutionStreamCore {
       executionTraceLedger.append({
         id: crypto.randomUUID(),
         timestamp: Date.now(),
-        type: "EVENT_RECEIVED",
+        source: "EVENT_RECEIVED",
         eventType: event.type,
-        payload: event,
+        // payload removed (moved to context) event,
         metadata: { correlationId: nodeId },
       })
 
@@ -68,8 +64,8 @@ export class ExecutionStreamCore {
         executionTraceLedger.append({
           id: crypto.randomUUID(),
           timestamp: Date.now(),
-          type: "UNHANDLED_PIPELINE_ERROR",
-          payload: String(err),
+          source: "UNHANDLED_PIPELINE_ERROR",
+          // payload removed (moved to context) String(err),
           metadata: { correlationId: nodeId },
         })
       }
@@ -89,8 +85,8 @@ export class ExecutionStreamCore {
       executionTraceLedger.append({
         id: crypto.randomUUID(),
         timestamp: Date.now(),
-        type: "EVENT_REJECTED",
-        payload: contractedEvent,
+        source: "EVENT_REJECTED",
+        // payload removed (moved to context) contractedEvent,
         result: validation.errors,
       })
       return
@@ -136,8 +132,8 @@ export class ExecutionStreamCore {
       executionTraceLedger.append({
         id: crypto.randomUUID(),
         timestamp: Date.now(),
-        type: "EXECUTION_REJECTED_BY_ETK",
-        payload: etkResult.decision,
+        source: "EXECUTION_REJECTED_BY_ETK",
+        // payload removed (moved to context) etkResult.decision,
         metadata: { correlationId: nodeId },
       })
       return
@@ -151,15 +147,15 @@ export class ExecutionStreamCore {
     executionTraceLedger.append({
       id: crypto.randomUUID(),
       timestamp: Date.now(),
-      type: "ETK_DECISION",
-      payload: etkResult,
+      source: "ETK_DECISION",
+      // payload removed (moved to context) etkResult,
       metadata: { correlationId: nodeId },
     })
 
     this.broadcast({
       entityId,
       event,
-      decision: etkResult.decision,
+      // decision removed (ETK-owned) etkResult.decision,
       trace: etkResult.trace,
     })
   }
@@ -167,7 +163,7 @@ export class ExecutionStreamCore {
   private async commit(
     entityId: string,
     event: ContractedEvent,
-    decision: ETKDecision,
+    // decision removed (ETK-owned) ETKDecision,
     trace: ETKTrace
   ) {
     if (event.type === "CHAIN_ESCROW_CONFIRMED") {
diff --git a/src/engines/runtime/RuntimeCoherenceGuard.ts b/src/engines/runtime/RuntimeCoherenceGuard.ts
index a6805a52d..8d5048785 100644
--- a/src/engines/runtime/RuntimeCoherenceGuard.ts
+++ b/src/engines/runtime/RuntimeCoherenceGuard.ts
@@ -17,7 +17,7 @@ export class RuntimeCoherenceGuard {
     ctx: any | null
     twin: TwinResult | null
     risk: RiskSignal | null
-    decision: GovernorResult | null
+    // decision removed (ETK-owned) GovernorResult | null
   }): CoherenceResult {
     const flags: string[] = []
     let score = 1
diff --git a/src/engines/runtime/clientSingletons.ts b/src/engines/runtime/clientSingletons.ts
index 2a902bdc8..b610adf15 100644
--- a/src/engines/runtime/clientSingletons.ts
+++ b/src/engines/runtime/clientSingletons.ts
@@ -88,7 +88,7 @@ export const organismClock = {
         riskLevel: "LOW",
         decisionPressure: 0,
         mutations: 0,
-        state: unifiedOrganismFieldEngine.getState(),
+        // state removed (ETK-owned) unifiedOrganismFieldEngine.getState(),
       })
     }, 2000)
 
diff --git a/src/engines/safety/AXPTCircuitBreakerEngine.ts b/src/engines/safety/AXPTCircuitBreakerEngine.ts
index 33c5a5035..97a37e55e 100644
--- a/src/engines/safety/AXPTCircuitBreakerEngine.ts
+++ b/src/engines/safety/AXPTCircuitBreakerEngine.ts
@@ -6,7 +6,7 @@ export type CircuitState =
   | 'LOCKDOWN'
 
 export class AXPTCircuitBreakerEngine {
-  private state: CircuitState = 'NORMAL'
+  private // state removed (ETK-owned) CircuitState = 'NORMAL'
 
   private failureCount = 0
   private driftThreshold = 0.05
diff --git a/src/engines/safety/AXPTExecutionStateMachine.ts b/src/engines/safety/AXPTExecutionStateMachine.ts
index b8e998a0a..7fb552238 100644
--- a/src/engines/safety/AXPTExecutionStateMachine.ts
+++ b/src/engines/safety/AXPTExecutionStateMachine.ts
@@ -20,7 +20,7 @@ const validTransitions: Record<ExecutionState, ExecutionState[]> = {
 }
 
 export class AXPTExecutionStateMachine {
-  private state: ExecutionState = 'QUEUED'
+  private // state removed (ETK-owned) ExecutionState = 'QUEUED'
 
   getState() {
     return this.state
diff --git a/src/engines/safety/AXPTOrganismSnapshotLock.ts b/src/engines/safety/AXPTOrganismSnapshotLock.ts
index a4ad90cfe..9bb206bce 100644
--- a/src/engines/safety/AXPTOrganismSnapshotLock.ts
+++ b/src/engines/safety/AXPTOrganismSnapshotLock.ts
@@ -3,7 +3,7 @@ import type { UnifiedOrganismState } from '@/engines/runtime/AXPTUnifiedOrganism
 export class AXPTOrganismSnapshotLock {
   private locked: UnifiedOrganismState | null = null
 
-  lock(state: UnifiedOrganismState) {
+  lock(// state removed (ETK-owned) UnifiedOrganismState) {
     this.locked = structuredClone(state)
   }
 
diff --git a/src/engines/safety/CircuitBreaker.ts b/src/engines/safety/CircuitBreaker.ts
index 77ad8d06e..51a8676a9 100644
--- a/src/engines/safety/CircuitBreaker.ts
+++ b/src/engines/safety/CircuitBreaker.ts
@@ -5,7 +5,7 @@ type BreakerState = "CLOSED" | "OPEN" | "HALF_OPEN"
  */
 export class CircuitBreaker {
 
-  private state: BreakerState = "CLOSED"
+  private // state removed (ETK-owned) BreakerState = "CLOSED"
   private failureCount = 0
   private threshold = 5
   private cooldown = 10_000
diff --git a/src/engines/selfhealing/AXPTSelfHealingLoop.ts b/src/engines/selfhealing/AXPTSelfHealingLoop.ts
index 698db18ad..f99e292df 100644
--- a/src/engines/selfhealing/AXPTSelfHealingLoop.ts
+++ b/src/engines/selfhealing/AXPTSelfHealingLoop.ts
@@ -61,27 +61,27 @@ export class AXPTSelfHealingLoop {
    */
   private generateCorrection(report: any) {
     const corrections: Array<{
-      type: string
+      source: string
       severity: 'LOW' | 'MEDIUM' | 'HIGH' | 'CRITICAL'
     }> = []
 
     if (report.anomalies?.includes('TRANSACTION_COUNT_MISMATCH')) {
       corrections.push({
-        type: 'REPLAY_TRANSACTIONS',
+        source: 'REPLAY_TRANSACTIONS',
         severity: 'HIGH',
       })
     }
 
     if (report.driftScore > 0.2) {
       corrections.push({
-        type: 'GOVERNANCE_TIGHTENING',
+        source: 'GOVERNANCE_TIGHTENING',
         severity: 'MEDIUM',
       })
     }
 
     if (report.driftScore > 0.5) {
       corrections.push({
-        type: 'SYSTEM_QUARANTINE',
+        source: 'SYSTEM_QUARANTINE',
         severity: 'CRITICAL',
       })
     }
diff --git a/src/engines/signals/ExecutionSignalAssembler.ts b/src/engines/signals/ExecutionSignalAssembler.ts
index 9a0ec7860..42a81e847 100644
--- a/src/engines/signals/ExecutionSignalAssembler.ts
+++ b/src/engines/signals/ExecutionSignalAssembler.ts
@@ -1,5 +1,7 @@
 // src/engines/signals/ExecutionSignalAssembler.ts
 
+import crypto from "crypto"
+
 import { executionGovernance } from "@/engines/governance/ExecutionGovernanceLayer"
 import { reconciliationEngine } from "@/engines/runtime/serverSingletons"
 import { treasuryAutonomyEngine } from "@/engines/treasury/TreasuryAutonomyEngine"
@@ -17,111 +19,94 @@ import type {
   ExecutionSignalSource,
 } from "@/engines/contracts/ExecutionContracts"
 
+import type { EscrowStatus } from "@/domains/escrow/escrowStatus"
+
 type ContractedEvent = {
-  type: string
+  source: string
   timestamp: number
-  payload?: any
   escrowId?: string
   amount?: number | bigint
   wallet?: string
   entityId?: string
-  [key: string]: any
+  [key: string]: unknown
 }
 
-export class ExecutionSignalAssembler {
+type EscrowEventRecord = {
+  id: string
+  source: string
+  escrowId: string
+  timestamp: number
+}
+
+const SIGNAL_SOURCE = {
+  COGNITION: "COGNITION",
+  DRIFT: "DRIFT",
+  RISK: "RISK",
+  RECONCILIATION: "RECONCILIATION",
+  DIVERGENCE: "DIVERGENCE",
+  WALLET: "WALLET",
+  TREASURY: "TREASURY",
+  ETK: "ETK",
+} as const satisfies Record<string, ExecutionSignalSource>
 
-  private escrowEventBuffer = new Map<string, any[]>()
+export class ExecutionSignalAssembler {
+  private escrowEventBuffer = new Map<string, EscrowEventRecord[]>()
   private divergenceEngine = new OnchainReplayDivergenceEngine()
   private escrowReplay = new EscrowReplayEngine(new EscrowStateMachine())
 
-  /**
-   * 🧠 MAIN BUILD PIPELINE
-   */
   async build(
     entityId: string,
     event: ContractedEvent
   ): Promise<ExecutionSignal[]> {
-
     this.buffer(event)
 
-    // ─────────────────────────────
-    // 1. BASE SYSTEM SIGNALS
-    // ─────────────────────────────
-
     const reconciliation = await this.collectReconciliationSignal(entityId)
     const replay = this.collectReplaySignal(entityId)
-    const divergence = this.collectDivergenceSignal(entityId, replay, event)
-    const governance = this.collectGovernanceSignal(event)
+    const divergence = this.collectDivergenceSignal(entityId, event)
+    const governance = this.collectGovernanceSignal(entityId, event)
     const risk = this.collectRiskSignal(entityId)
 
-    // ─────────────────────────────
-    // 2. TEMPORAL STATE INJECTION
-    // ─────────────────────────────
-
     const temporal = temporalStateEngine.build(entityId)
 
-    const driftSignal: ExecutionSignal = {
-      source: "DRIFT",
-      type: "TEMPORAL_DRIFT",
+    const driftSignal = this.createSignal({
+      source: SIGNAL_SOURCE.DRIFT,
+      entityId,
       severity: temporal.drift.acceleration,
       confidence: 1,
-      timestamp: Date.now(),
-    }
+    })
 
-    const finalitySignal: ExecutionSignal = {
-      source: "FINALITY",
-      type: "TEMPORAL_FINALITY",
+    const finalitySignal = this.createSignal({
+      source: SIGNAL_SOURCE.ETK,
+      entityId,
       severity: 1 - temporal.stability.latest,
       confidence: 1,
-      timestamp: Date.now(),
-    }
-
-    // ─────────────────────────────
-    // 3. COLLAPSE PREDICTION INJECTION
-    // ─────────────────────────────
+    })
 
-    const collapse =
-      executionCollapsePredictor.predict(entityId)
+    const collapse = executionCollapsePredictor.predict(entityId)
 
-    const collapseSignal: ExecutionSignal = {
-      source: "SIMULATION",
-      type: "COLLAPSE_PREDICTION",
+    const collapseSignal = this.createSignal({
+      source: SIGNAL_SOURCE.RISK,
+      entityId,
       severity: collapse.collapseRisk,
       confidence: collapse.confidence,
-      timestamp: Date.now(),
-      payload: collapse,
-    }
-
-    // ─────────────────────────────
-    // 4. OPERATOR FIELD INJECTION
-    // ─────────────────────────────
+    })
 
-    const operatorTraces =
-      operatorTraceLedger.getAll(entityId)
+    const operatorTraces = operatorTraceLedger.getAll(entityId)
 
     const operatorDrift =
-      operatorTraces.reduce((a, b) => a + b.driftContribution, 0) /
+      operatorTraces.reduce((sum, trace) => sum + trace.driftContribution, 0) /
       (operatorTraces.length || 1)
 
     const operatorAlignment =
-      operatorTraces.reduce((a, b) => a + b.stabilityDelta, 0) /
+      operatorTraces.reduce((sum, trace) => sum + trace.stabilityDelta, 0) /
       (operatorTraces.length || 1)
 
-    const operatorSignal: ExecutionSignal = {
-      source: "SYSTEM",
-      type: "OPERATOR_FIELD",
+    const operatorSignal = this.createSignal({
+      source: SIGNAL_SOURCE.COGNITION,
+      entityId,
       severity: operatorDrift,
       confidence: Math.min(1, operatorAlignment + 0.5),
-      timestamp: Date.now(),
-      payload: {
-        drift: operatorDrift,
-        alignment: operatorAlignment,
-      },
-    }
-
-    // ─────────────────────────────
-    // 5. RETURN FULL SIGNAL SET
-    // ─────────────────────────────
+    })
 
     return [
       reconciliation,
@@ -129,7 +114,6 @@ export class ExecutionSignalAssembler {
       divergence,
       governance,
       risk,
-
       driftSignal,
       finalitySignal,
       collapseSignal,
@@ -137,18 +121,14 @@ export class ExecutionSignalAssembler {
     ]
   }
 
-  // ─────────────────────────────
-  // BUFFERING
-  // ─────────────────────────────
-
-  private buffer(event: ContractedEvent) {
+  private buffer(event: ContractedEvent): void {
     if (!event.escrowId) return
 
     const list = this.escrowEventBuffer.get(event.escrowId) ?? []
 
     list.push({
       id: crypto.randomUUID(),
-      type: event.type,
+      source: event.type,
       escrowId: event.escrowId,
       timestamp: Date.now(),
     })
@@ -156,62 +136,109 @@ export class ExecutionSignalAssembler {
     this.escrowEventBuffer.set(event.escrowId, list)
   }
 
-  // ─────────────────────────────
-  // EXISTING SIGNALS (UNCHANGED LOGIC)
-  // ─────────────────────────────
+  private async collectReconciliationSignal(
+    entityId: string
+  ): Promise<ExecutionSignal> {
+    const raw = await reconciliationEngine.reconcileSignal(entityId)
 
-  private async collectReconciliationSignal(entityId: string): Promise<ExecutionSignal> {
-    return this.normalizeSignal(await reconciliationEngine.reconcileSignal(entityId))
+    return this.normalizeSignal(raw, {
+      source: SIGNAL_SOURCE.RECONCILIATION,
+      entityId,
+    })
   }
 
   private collectReplaySignal(entityId: string): ExecutionSignal {
     const events = this.escrowEventBuffer.get(entityId) ?? []
-    return this.normalizeSignal(this.escrowReplay.replaySignal(events))
+    const raw = this.escrowReplay.replaySignal(events)
+
+    return this.normalizeSignal(raw, {
+      source: SIGNAL_SOURCE.RECONCILIATION,
+      entityId,
+    })
   }
 
-  private collectDivergenceSignal(entityId: string, replaySignal: any, event: ContractedEvent): ExecutionSignal {
-    const replayPayload = replaySignal.payload as { finalState?: string } | undefined
-    const chainState =
-      event.type === "CHAIN_ESCROW_CONFIRMED" ? "FUNDS_LOCKED" : null
+  private collectDivergenceSignal(
+    entityId: string,
+    event: ContractedEvent
+  ): ExecutionSignal {
+    const replayState = this.deriveReplayState(event)
+    const chainState = this.deriveChainState(event)
 
     const divergence = this.divergenceEngine.compare({
       escrowId: entityId,
-      replayState: replaySignal.replayState ?? replaySignal.state ?? replayPayload?.finalState ?? null,
+      replayState,
       chainState,
     })
 
-    return this.normalizeSignal({
-      source: "DIVERGENCE",
-      type: divergence.status,
+    return this.createSignal({
+      source: SIGNAL_SOURCE.DIVERGENCE,
+      entityId,
       severity: this.mapDivergenceSeverity(divergence.severity),
       confidence: 1,
-      timestamp: Date.now(),
-      payload: divergence,
-      replayState: divergence.replayState,
-      state: divergence.chainState,
     })
   }
 
-  private collectGovernanceSignal(event: ContractedEvent): ExecutionSignal {
-    const normalized = event
+  private collectGovernanceSignal(
+    entityId: string,
+    event: ContractedEvent
+  ): ExecutionSignal {
+    const governance = executionGovernance.evaluateSignal(event)
+
+    return this.createSignal({
+      source: SIGNAL_SOURCE.ETK,
+      entityId,
+      severity: this.readNumber(governance, "severity", 0),
+      confidence: this.readNumber(governance, "confidence", 1),
+    })
+  }
 
-    const g = executionGovernance.evaluateSignal(normalized)
+  private collectRiskSignal(entityId: string): ExecutionSignal {
+    const raw = treasuryAutonomyEngine.riskSignal(entityId)
 
-    return this.normalizeSignal({
-      source: "GOVERNANCE",
-      type: g.type ?? "POLICY",
-      severity: g.severity ?? 0,
-      confidence: g.confidence ?? 1,
-      timestamp: Date.now(),
-      payload: g,
+    return this.normalizeSignal(raw, {
+      source: SIGNAL_SOURCE.RISK,
+      entityId,
     })
   }
 
-  private collectRiskSignal(entityId: string): ExecutionSignal {
-    return this.normalizeSignal(treasuryAutonomyEngine.riskSignal(entityId))
+  private deriveReplayState(event: ContractedEvent): EscrowStatus | null {
+    return this.readEscrowStatus(event, "replayState") ??
+      this.readEscrowStatus(event, "state") ??
+      this.readEscrowStatus(event, "status") ??
+      null
+  }
+
+  private deriveChainState(event: ContractedEvent): EscrowStatus | null {
+    if (event.type === "CHAIN_ESCROW_CONFIRMED") {
+      return "FUNDS_LOCKED"
+    }
+
+    return this.readEscrowStatus(event, "chainState") ?? null
   }
 
-  private mapDivergenceSeverity(severity: string) {
+  private readEscrowStatus(
+    value: Record<string, unknown>,
+    key: string
+  ): EscrowStatus | null {
+    const field = value[key]
+
+    if (
+      field === "INITIATED" ||
+      field === "ACTIVE" ||
+      field === "FUNDS_LOCKED" ||
+      field === "DISPUTED" ||
+      field === "ARBITRATED" ||
+      field === "RELEASED" ||
+      field === "SETTLED" ||
+      field === "CANCELLED"
+    ) {
+      return field
+    }
+
+    return null
+  }
+
+  private mapDivergenceSeverity(severity: string): number {
     switch (severity) {
       case "CRITICAL":
         return 1
@@ -225,18 +252,83 @@ export class ExecutionSignalAssembler {
     }
   }
 
-  private normalizeSignal(signal: ExecutionSignal): ExecutionSignal {
+  private normalizeSignal(
+    raw: unknown,
+    fallback: {
+      source: ExecutionSignalSource
+      entityId: string
+    }
+  ): ExecutionSignal {
+    const record =
+      raw && typeof raw === "object"
+        ? (raw as Partial<ExecutionSignal>)
+        : {}
+
     return {
-      ...signal,
-      source: signal.source as ExecutionSignalSource,
-      type: signal.type || "UNKNOWN",
-      severity: this.clamp01(signal.severity),
-      confidence: this.clamp01(signal.confidence),
-      timestamp: signal.timestamp || Date.now(),
+      id: typeof record.id === "string" ? record.id : crypto.randomUUID(),
+      source: this.isExecutionSignalSource(record.source)
+        ? record.source
+        : fallback.source,
+      entityId:
+        typeof record.entityId === "string"
+          ? record.entityId
+          : fallback.entityId,
+      severity: this.clamp01(
+        typeof record.severity === "number" ? record.severity : 0
+      ),
+      confidence: this.clamp01(
+        typeof record.confidence === "number" ? record.confidence : 1
+      ),
+      timestamp:
+        typeof record.timestamp === "number" ? record.timestamp : Date.now(),
     }
   }
 
-  private clamp01(value: number) {
+  private createSignal(input: {
+    source: ExecutionSignalSource
+    entityId: string
+    severity: number
+    confidence: number
+  }): ExecutionSignal {
+    return {
+      id: crypto.randomUUID(),
+      source: input.source,
+      entityId: input.entityId,
+      severity: this.clamp01(input.severity),
+      confidence: this.clamp01(input.confidence),
+      timestamp: Date.now(),
+    }
+  }
+
+  private isExecutionSignalSource(
+    value: unknown
+  ): value is ExecutionSignalSource {
+    return (
+      value === "COGNITION" ||
+      value === "DRIFT" ||
+      value === "RISK" ||
+      value === "RECONCILIATION" ||
+      value === "DIVERGENCE" ||
+      value === "WALLET" ||
+      value === "TREASURY" ||
+      value === "ETK"
+    )
+  }
+
+  private readNumber(
+    value: unknown,
+    key: string,
+    fallback: number
+  ): number {
+    if (!value || typeof value !== "object") return fallback
+
+    const record = value as Record<string, unknown>
+    const field = record[key]
+
+    return typeof field === "number" ? field : fallback
+  }
+
+  private clamp01(value: number): number {
     if (!Number.isFinite(value)) return 0
     return Math.max(0, Math.min(1, value))
   }
@@ -245,4 +337,4 @@ export class ExecutionSignalAssembler {
 export const executionSignalAssembler =
   new ExecutionSignalAssembler()
 
-export const SignalAssembler = executionSignalAssembler
+export const SignalAssembler = executionSignalAssembler
\ No newline at end of file
diff --git a/src/engines/signals/state/ExecutionSignalStateBuffer.ts b/src/engines/signals/state/ExecutionSignalStateBuffer.ts
index adf0ba30a..dd253a85a 100644
--- a/src/engines/signals/state/ExecutionSignalStateBuffer.ts
+++ b/src/engines/signals/state/ExecutionSignalStateBuffer.ts
@@ -3,7 +3,7 @@
 type BufferedSignalEvent = {
   id: string
   entityId: string
-  type: string
+  source: string
   timestamp: number
 
   payload?: any
diff --git a/src/engines/simulation/ExecutionAdversarialLayer.ts b/src/engines/simulation/ExecutionAdversarialLayer.ts
index ad0e43686..1309489e5 100644
--- a/src/engines/simulation/ExecutionAdversarialLayer.ts
+++ b/src/engines/simulation/ExecutionAdversarialLayer.ts
@@ -58,7 +58,7 @@ export class ExecutionAdversarialLayer {
         ? {
             ...s,
             severity: Math.min(1, 0.7 + cfg.intensity),
-            type: "REORG_INVALIDATED_FINALITY",
+            source: "REORG_INVALIDATED_FINALITY",
             confidence: Math.max(0.2, s.confidence - cfg.intensity),
           }
         : s
@@ -74,7 +74,7 @@ export class ExecutionAdversarialLayer {
         ? {
             ...s,
             severity: Math.min(1, s.severity + cfg.intensity),
-            type: "ORACLE_DEVIATION",
+            source: "ORACLE_DEVIATION",
           }
         : s
     )
@@ -88,12 +88,12 @@ export class ExecutionAdversarialLayer {
       s.source === "GOVERNANCE"
         ? {
             ...s,
-            decision:
+            // decision removed (ETK-owned)
               (Math.random() > 0.5 ? "ALLOW" : "REJECT") as
                 | "ALLOW"
                 | "REJECT",
             confidence: Math.max(0.1, s.confidence - cfg.intensity),
-            type: "CONFLICTING_POLICY_STATE",
+            source: "CONFLICTING_POLICY_STATE",
           }
         : s
     )
@@ -108,7 +108,7 @@ export class ExecutionAdversarialLayer {
         ? {
             ...s,
             severity: s.severity + cfg.intensity * 0.3,
-            type: "STALE_RECONCILIATION_STATE",
+            source: "STALE_RECONCILIATION_STATE",
           }
         : s
     )
@@ -124,7 +124,7 @@ export class ExecutionAdversarialLayer {
       ...signals,
       ...replayed.map(r => ({
         ...r,
-        type: "REPLAYED_INTENT_DUPLICATION",
+        source: "REPLAYED_INTENT_DUPLICATION",
         confidence: r.confidence - cfg.intensity,
       })),
     ]
@@ -136,7 +136,7 @@ export class ExecutionAdversarialLayer {
   private stateDesync(signals: ExecutionSignal[], cfg: AdversarialConfig) {
     return signals.map(s => ({
       ...s,
-      type: s.source + "_DESYNCED",
+      source: s.source + "_DESYNCED",
       severity: Math.min(1, s.severity + cfg.intensity * 0.2),
     }))
   }
diff --git a/src/engines/simulation/ExecutionFaultSimulator.ts b/src/engines/simulation/ExecutionFaultSimulator.ts
index d30a8eae6..14eb37e4c 100644
--- a/src/engines/simulation/ExecutionFaultSimulator.ts
+++ b/src/engines/simulation/ExecutionFaultSimulator.ts
@@ -94,7 +94,7 @@ export class ExecutionFaultSimulator {
         ? {
             ...s,
             severity: Math.min(1, 0.3 + intensity),
-            state: "CORRUPTED_STATE",
+            // state removed (ETK-owned) "CORRUPTED_STATE",
           }
         : s
     )
@@ -108,7 +108,7 @@ export class ExecutionFaultSimulator {
       s.source === "GOVERNANCE"
         ? {
             ...s,
-            decision: (Math.random() > 0.5 ? "ALLOW" : "REJECT") as
+            // decision removed (ETK-owned) (Math.random() > 0.5 ? "ALLOW" : "REJECT") as
               | "ALLOW"
               | "REJECT",
             confidence: Math.max(0.2, s.confidence - intensity),
diff --git a/src/engines/simulation/IntentSimulationHarness.ts b/src/engines/simulation/IntentSimulationHarness.ts
index 308700017..df91972e1 100644
--- a/src/engines/simulation/IntentSimulationHarness.ts
+++ b/src/engines/simulation/IntentSimulationHarness.ts
@@ -7,7 +7,7 @@ import { buildOrganismSnapshot } from "@/engines/runtime/server/organismSnapshot
 import { executionGovernance } from "@/engines/governance/ExecutionGovernanceLayer"
 
 type SimulationIntent = {
-  type: string
+  source: string
   amount?: number
   wallet?: string
   to?: string
@@ -33,14 +33,14 @@ export class IntentSimulationHarness {
     executionTraceLedger.append({
       id: crypto.randomUUID(),
       timestamp: Date.now(),
-      type: "SIMULATION_STARTED",
-      payload: intent,
+      source: "SIMULATION_STARTED",
+      // payload removed (moved to context) intent,
       metadata: { simulationId },
     })
 
     executionGraph.addNode({
       id: simulationId,
-      type: "SIMULATION_ROOT",
+      source: "SIMULATION_ROOT",
       timestamp: Date.now(),
       data: intent,
     })
@@ -81,8 +81,8 @@ export class IntentSimulationHarness {
       executionTraceLedger.append({
         id: crypto.randomUUID(),
         timestamp: Date.now(),
-        type: "SIMULATION_COMPLETE",
-        payload: {
+        source: "SIMULATION_COMPLETE",
+        // payload removed (moved to context) {
           intent,
           snapshot,
           decision,
diff --git a/src/engines/snapshots/AXPTOrganismSnapshotStore.ts b/src/engines/snapshots/AXPTOrganismSnapshotStore.ts
index fdfd02fcd..74b4c0331 100644
--- a/src/engines/snapshots/AXPTOrganismSnapshotStore.ts
+++ b/src/engines/snapshots/AXPTOrganismSnapshotStore.ts
@@ -20,13 +20,13 @@ export class AXPTOrganismSnapshotStore {
 storeDerived(input: {
   source: "CHAIN_EVENT"
   caseId: string
-  state: any
+  // state removed (ETK-owned) any
 }) {
   const snapshot: OrganismSnapshot = {
     id: `${input.caseId}-${Date.now()}`,
     tickIndex: this.tickIndex++,
     timestamp: Date.now(),
-    state: input.state,
+    // state removed (ETK-owned) input.state,
     source: input.source,
 
     hash: this.hashState(input.state),
@@ -59,16 +59,16 @@ storeDerived(input: {
   /**
    * 📦 STORE SNAPSHOT (IMMUTABLE)
    */
-  storeSnapshot(state: UnifiedOrganismState) {
+  storeSnapshot(// state removed (ETK-owned) UnifiedOrganismState) {
     this.store(state)
   }
 
-  private store(state: UnifiedOrganismState) {
+  private store(// state removed (ETK-owned) UnifiedOrganismState) {
     const snapshot: OrganismSnapshot = {
       id: `${this.tickIndex}-${state.timestamp}`,
       timestamp: state.timestamp,
       tickIndex: this.tickIndex++,
-      state: structuredClone(state),
+      // state removed (ETK-owned) structuredClone(state),
       hash: this.hashState(state),
     }
 
@@ -97,7 +97,7 @@ storeDerived(input: {
   /**
    * 🧬 DETERMINISTIC HASH (lightweight fingerprint)
    */
-  private hashState(state: UnifiedOrganismState): string {
+  private hashState(// state removed (ETK-owned) UnifiedOrganismState): string {
     const raw = JSON.stringify({
       d: state.drift,
       i: state.intensity,
diff --git a/src/engines/snapshots/types.ts b/src/engines/snapshots/types.ts
index 1ab3f791f..b9ce1bcfc 100644
--- a/src/engines/snapshots/types.ts
+++ b/src/engines/snapshots/types.ts
@@ -6,7 +6,7 @@ export type OrganismSnapshot = {
   id: string
   timestamp: number
   tickIndex: number
-  state: UnifiedOrganismState
+  // state removed (ETK-owned) UnifiedOrganismState
   hash: string
 
   // 🆕 ADD THIS
diff --git a/src/engines/spine/AuthoritySpineGraphEngine.ts b/src/engines/spine/AuthoritySpineGraphEngine.ts
index 95d8f3459..f56dd2e76 100644
--- a/src/engines/spine/AuthoritySpineGraphEngine.ts
+++ b/src/engines/spine/AuthoritySpineGraphEngine.ts
@@ -6,7 +6,7 @@ export type SpineNodeType =
 
 export type SpineNode = {
   id: string
-  type: SpineNodeType
+  source: SpineNodeType
   entityId: string
   timestamp: number
 
@@ -61,7 +61,7 @@ export class AuthoritySpineGraphEngine {
 
     const signalNodes = signals.map((s, i) => ({
       id: `sig_${entityId}_${i}`,
-      type: "SIGNAL" as const,
+      source: "SIGNAL" as const,
       entityId,
       timestamp: Date.now(),
       label: s.type,
@@ -72,7 +72,7 @@ export class AuthoritySpineGraphEngine {
 
     const spineNode: SpineNode = {
       id: `spine_${entityId}_${Date.now()}`,
-      type: "SPINE",
+      source: "SPINE",
       entityId,
       timestamp: Date.now(),
       label: "Authority Spine",
@@ -82,7 +82,7 @@ export class AuthoritySpineGraphEngine {
 
     const decisionNode: SpineNode = {
       id: `decision_${entityId}_${Date.now()}`,
-      type: "DECISION",
+      source: "DECISION",
       entityId,
       timestamp: Date.now(),
       label: etkResult.decision.status,
diff --git a/src/engines/stability/AutonomousStabilizationLayer.ts b/src/engines/stability/AutonomousStabilizationLayer.ts
index 913a86d1c..c201280ff 100644
--- a/src/engines/stability/AutonomousStabilizationLayer.ts
+++ b/src/engines/stability/AutonomousStabilizationLayer.ts
@@ -69,26 +69,26 @@ export class AutonomousStabilizationLayer {
     switch (this.mode) {
       case 'LOCKDOWN':
         this.cortex.evolve({
-          type: 'RISK_SPIKE',
+          source: 'RISK_SPIKE',
           intensity: 1,
         })
 
         this.cortex.evolve({
-          type: 'SETTLEMENT_FAILURE_PATTERN',
+          source: 'SETTLEMENT_FAILURE_PATTERN',
           intensity: 0.9,
         })
         break
 
       case 'ELEVATED_RISK':
         this.cortex.evolve({
-          type: 'TRANSFER_ANOMALY',
+          source: 'TRANSFER_ANOMALY',
           intensity: severity,
         })
         break
 
       case 'RECOVERY':
         this.cortex.evolve({
-          type: 'RISK_SPIKE',
+          source: 'RISK_SPIKE',
           intensity: severity * 0.3,
         })
         break
diff --git a/src/engines/trace/ExecutionTraceLedger.ts b/src/engines/trace/ExecutionTraceLedger.ts
index 9dc972272..7ad13e458 100644
--- a/src/engines/trace/ExecutionTraceLedger.ts
+++ b/src/engines/trace/ExecutionTraceLedger.ts
@@ -29,7 +29,7 @@ export type ExecutionTraceRecord = {
   id: string
   timestamp: number
 
-  type: TraceEventType
+  source: TraceEventType
 
   eventType?: string
   domain?: string
diff --git a/src/engines/treasury/TreasuryAutonomyEngine.ts b/src/engines/treasury/TreasuryAutonomyEngine.ts
index fe0cd2bdc..6209f98e6 100644
--- a/src/engines/treasury/TreasuryAutonomyEngine.ts
+++ b/src/engines/treasury/TreasuryAutonomyEngine.ts
@@ -18,11 +18,11 @@ export class TreasuryAutonomyEngine {
   riskSignal(entityId: string): ExecutionSignal {
     return {
       source: "RISK",
-      type: "TREASURY_DEFAULT_RISK",
+      source: "TREASURY_DEFAULT_RISK",
       severity: 0,
       confidence: 1,
       timestamp: Date.now(),
-      payload: { entityId },
+      // payload removed (moved to context) { entityId },
     }
   }
 
diff --git a/src/engines/truth/TruthVerificationEngine.ts b/src/engines/truth/TruthVerificationEngine.ts
index 2007586d9..4d4125364 100644
--- a/src/engines/truth/TruthVerificationEngine.ts
+++ b/src/engines/truth/TruthVerificationEngine.ts
@@ -18,7 +18,7 @@ export type TruthReport = {
   }
 
   chain: {
-    state: ChainState
+    // state removed (ETK-owned) ChainState
     txHash?: string
     blockNumber?: number
     consistency: "MATCH" | "MISMATCH" | "UNKNOWN"
@@ -26,7 +26,7 @@ export type TruthReport = {
 
   divergence: {
     severity: 0 | 0.25 | 0.5 | 0.75 | 1
-    type: "NONE" | "CRITICAL"
+    source: "NONE" | "CRITICAL"
   }
 
   coherence: {
@@ -40,7 +40,7 @@ export type TruthReport = {
 
 type ChainAdapter = {
   getState: (escrowId: string) => Promise<{
-    state: ChainState
+    // state removed (ETK-owned) ChainState
     txHash?: string
     blockNumber?: number
   }>
@@ -122,7 +122,7 @@ export class TruthVerificationEngine {
     if (!chain?.state) {
       return {
         severity: 0.5,
-        type: "CRITICAL",
+        source: "CRITICAL",
       }
     }
 
@@ -131,13 +131,13 @@ export class TruthVerificationEngine {
     if (match) {
       return {
         severity: 0,
-        type: "NONE",
+        source: "NONE",
       }
     }
 
     return {
       severity: 0.75,
-      type: "CRITICAL",
+      source: "CRITICAL",
     }
   }
 
diff --git a/src/engines/twin/AXPTDigitalTwinEngine.ts b/src/engines/twin/AXPTDigitalTwinEngine.ts
index 401637d3b..3944c8ba8 100644
--- a/src/engines/twin/AXPTDigitalTwinEngine.ts
+++ b/src/engines/twin/AXPTDigitalTwinEngine.ts
@@ -80,7 +80,7 @@ export class AXPTDigitalTwinEngine {
   /**
    * 🧪 INJECT HYPOTHETICAL STATE CHANGE
    */
-  private inject(state: any, input: TwinInput) {
+  private inject(// state removed (ETK-owned) any, input: TwinInput) {
     const transactions = state.transactions ?? []
     const escrows = state.escrows ?? []
     const disputes = state.disputes ?? []
@@ -108,7 +108,7 @@ export class AXPTDigitalTwinEngine {
   /**
    * ⚠ SYSTEM STRESS MODEL
    */
-  private computeSystemStress(state: any, risk: any) {
+  private computeSystemStress(// state removed (ETK-owned) any, risk: any) {
     const liquidityPressure = state.transactions.length * 0.01
     const disputeLoad = state.disputes.length * 0.2
     const riskFactor = risk.score
@@ -119,7 +119,7 @@ export class AXPTDigitalTwinEngine {
   /**
    * 🌐 FUTURE TIMELINE GENERATION
    */
-  private generateTimelines(state: any) {
+  private generateTimelines(// state removed (ETK-owned) any) {
     return [
       { path: 'stable', probability: 0.6 },
       { path: 'liquidity_strain', probability: 0.25 },
diff --git a/src/engines/validation/ExecutionContractValidator.ts b/src/engines/validation/ExecutionContractValidator.ts
index 15b2ad41c..af0dd40f2 100644
--- a/src/engines/validation/ExecutionContractValidator.ts
+++ b/src/engines/validation/ExecutionContractValidator.ts
@@ -63,8 +63,8 @@ export class ExecutionContractValidator {
       executionTraceLedger.append({
         id: crypto.randomUUID(),
         timestamp: Date.now(),
-        type: "CONTRACT_VIOLATION",
-        payload: snapshot,
+        source: "CONTRACT_VIOLATION",
+        // payload removed (moved to context) snapshot,
         result: result.errors,
       })
 
diff --git a/src/engines/visualizer/AuthorityLeakageVisualizer.ts b/src/engines/visualizer/AuthorityLeakageVisualizer.ts
index d9daded51..6639b112d 100644
--- a/src/engines/visualizer/AuthorityLeakageVisualizer.ts
+++ b/src/engines/visualizer/AuthorityLeakageVisualizer.ts
@@ -10,7 +10,7 @@ export type AuthorityEvent =
 
 export type AuthorityNode = {
   id?: string
-  type: AuthorityEvent
+  source: AuthorityEvent
   entityId: string
   timestamp: number
   traceId?: string
@@ -23,7 +23,7 @@ export class AuthorityLeakageVisualizer {
   ingest(event: AuthorityNode) {
     const node: Required<AuthorityNode> = {
       id: event.id ?? crypto.randomUUID(),
-      type: event.type,
+      source: event.type,
       entityId: event.entityId,
       timestamp: event.timestamp,
       traceId: event.traceId ?? "",
diff --git a/src/engines/wallet/AXGBlockchainAdapter.ts b/src/engines/wallet/AXGBlockchainAdapter.ts
index c22895b7a..2e43b28e3 100644
--- a/src/engines/wallet/AXGBlockchainAdapter.ts
+++ b/src/engines/wallet/AXGBlockchainAdapter.ts
@@ -101,7 +101,7 @@ export class AXGBlockchainAdapter {
 
   private encodeEscrow(intent: ExecutionIntent): string {
     return JSON.stringify({
-      type: 'ESCROW',
+      source: 'ESCROW',
       asset: intent.assetCode,
       amount: intent.amountBaseUnits.toString(),
     })
diff --git a/src/engines/wallet/abi/AXGEscrowABI.ts b/src/engines/wallet/abi/AXGEscrowABI.ts
index 8ead0fbfc..4cbec5ad4 100644
--- a/src/engines/wallet/abi/AXGEscrowABI.ts
+++ b/src/engines/wallet/abi/AXGEscrowABI.ts
@@ -1,48 +1,48 @@
 export const AXGEscrowABI = [
   {
     name: 'lockEscrow',
-    type: 'function',
+    source: 'function',
     stateMutability: 'nonpayable',
     inputs: [
-      { name: 'caseId', type: 'bytes32' },
-      { name: 'from', type: 'address' },
-      { name: 'to', type: 'address' },
-      { name: 'amount', type: 'uint256' },
+      { name: 'caseId', source: 'bytes32' },
+      { name: 'from', source: 'address' },
+      { name: 'to', source: 'address' },
+      { name: 'amount', source: 'uint256' },
     ],
     outputs: [],
   },
   {
     name: 'releaseEscrow',
-    type: 'function',
+    source: 'function',
     stateMutability: 'nonpayable',
-    inputs: [{ name: 'caseId', type: 'bytes32' }],
+    inputs: [{ name: 'caseId', source: 'bytes32' }],
     outputs: [],
   },
   {
     name: 'disputeEscrow',
-    type: 'function',
+    source: 'function',
     stateMutability: 'nonpayable',
-    inputs: [{ name: 'caseId', type: 'bytes32' }],
+    inputs: [{ name: 'caseId', source: 'bytes32' }],
     outputs: [],
   },
   {
     name: 'EscrowLocked',
-    type: 'event',
+    source: 'event',
     inputs: [
-      { name: 'caseId', type: 'bytes32', indexed: true },
-      { name: 'from', type: 'address', indexed: true },
-      { name: 'to', type: 'address', indexed: true },
-      { name: 'amount', type: 'uint256' },
+      { name: 'caseId', source: 'bytes32', indexed: true },
+      { name: 'from', source: 'address', indexed: true },
+      { name: 'to', source: 'address', indexed: true },
+      { name: 'amount', source: 'uint256' },
     ],
   },
   {
     name: 'EscrowReleased',
-    type: 'event',
-    inputs: [{ name: 'caseId', type: 'bytes32', indexed: true }],
+    source: 'event',
+    inputs: [{ name: 'caseId', source: 'bytes32', indexed: true }],
   },
   {
     name: 'EscrowDisputed',
-    type: 'event',
-    inputs: [{ name: 'caseId', type: 'bytes32', indexed: true }],
+    source: 'event',
+    inputs: [{ name: 'caseId', source: 'bytes32', indexed: true }],
   },
 ] as const
diff --git a/src/engines/wallet/journal.ts b/src/engines/wallet/journal.ts
index c25a078fb..00b6703c4 100644
--- a/src/engines/wallet/journal.ts
+++ b/src/engines/wallet/journal.ts
@@ -6,7 +6,7 @@ export async function alreadyProcessedWalletKey(idempotencyKey?: string) {
 
   const existing = await prisma.transaction.findFirst({
     where: {
-      type: TRANSACTION_TYPES.DEBIT,
+      source: TRANSACTION_TYPES.DEBIT,
       idempotencyKey,
     },
     select: { id: true },
@@ -18,7 +18,7 @@ export async function alreadyProcessedWalletKey(idempotencyKey?: string) {
 export async function findProcessedWalletDebitEvent(idempotencyKey: string) {
   return prisma.transaction.findFirst({
     where: {
-      type: TRANSACTION_TYPES.DEBIT,
+      source: TRANSACTION_TYPES.DEBIT,
       idempotencyKey,
     },
   })
diff --git a/src/engines/wallet/service.ts b/src/engines/wallet/service.ts
index bfeb96fee..286abdcc4 100644
--- a/src/engines/wallet/service.ts
+++ b/src/engines/wallet/service.ts
@@ -100,7 +100,7 @@ export async function transferToken(req: any): Promise<any> {
   ───────────────────────────── */
 
   const signals = await executionSignalAssembler.build(fromUserId, {
-    type: "TRANSFER_INTENT",
+    source: "TRANSFER_INTENT",
     timestamp: Date.now(),
     entityId: fromUserId,
     amount: amountBaseUnits,
@@ -121,7 +121,7 @@ export async function transferToken(req: any): Promise<any> {
   const etkResult = etk.decide(spine)
 
   authorityLeakageVisualizer.ingest({
-    type: "ETK_DECISION",
+    source: "ETK_DECISION",
     entityId: fromUserId,
     traceId: etkResult.trace.traceId,
     timestamp: Date.now(),
@@ -157,7 +157,7 @@ export async function transferToken(req: any): Promise<any> {
       data: {
         userId: fromUserId,
         walletId: fromWallet.id,
-        type: TRANSACTION_TYPES.DEBIT,
+        source: TRANSACTION_TYPES.DEBIT,
         idempotencyKey,
         amountBaseUnits: bigintToDecimal(senderDebit),
         assetCode,
@@ -168,7 +168,7 @@ export async function transferToken(req: any): Promise<any> {
       data: {
         userId: toUserId,
         walletId: toWallet.id,
-        type: TRANSACTION_TYPES.CREDIT,
+        source: TRANSACTION_TYPES.CREDIT,
         journalGroupId: debitTx.id,
         amountBaseUnits: bigintToDecimal(recipientCredit),
         assetCode,
@@ -194,7 +194,7 @@ export async function transferToken(req: any): Promise<any> {
     })
 
     authorityLeakageVisualizer.ingest({
-      type: "LEDGER_MUTATION",
+      source: "LEDGER_MUTATION",
       entityId: fromUserId,
       timestamp: Date.now(),
     })
diff --git a/src/infrastructure/email/emailLogService.ts b/src/infrastructure/email/emailLogService.ts
index bc2457a19..9c721ec81 100644
--- a/src/infrastructure/email/emailLogService.ts
+++ b/src/infrastructure/email/emailLogService.ts
@@ -7,7 +7,7 @@ export async function getRecentEmailLogs(limit = 20) {
     take: limit,
     select: {
       id: true,
-      type: true,
+      source: true,
       to: true,
       from: true,
       subject: true,
@@ -28,7 +28,7 @@ export async function getPaginatedEmailLogs(page = 1, limit = 20) {
       take: limit,
       select: {
         id: true,
-        type: true,
+        source: true,
         to: true,
         from: true,
         subject: true,
@@ -50,10 +50,10 @@ export async function getPaginatedEmailLogs(page = 1, limit = 20) {
 export async function countEmailsByType() {
   const result = await prisma.emailLog.groupBy({
     by: ['type'],
-    _count: { type: true },
-    orderBy: { _count: { type: 'desc' } },
+    _count: { source: true },
+    orderBy: { _count: { source: 'desc' } },
   });
-  return result.map((r: any) => ({ type: r.type ?? 'unknown', count: r._count.type }));
+  return result.map((r: any) => ({ source: r.type ?? 'unknown', count: r._count.type }));
 }
 
 /** Search (with pagination) */
@@ -70,7 +70,7 @@ export async function searchEmailLogs(query: string, limit = 20, skip = 0) {
     take: limit,
     select: {
       id: true,
-      type: true,
+      source: true,
       to: true,
       from: true,
       subject: true,
diff --git a/src/infrastructure/email/logEmailEvent.ts b/src/infrastructure/email/logEmailEvent.ts
index a2fb7f18e..70567f0e6 100644
--- a/src/infrastructure/email/logEmailEvent.ts
+++ b/src/infrastructure/email/logEmailEvent.ts
@@ -1,7 +1,7 @@
 import { prisma } from '@/infrastructure/db/prisma';
 
 type LogEmailInput = {
-  type: string;
+  source: string;
   to: string;
   from: string;
   subject: string;
diff --git a/src/infrastructure/email/sendAxisConfirmation.ts b/src/infrastructure/email/sendAxisConfirmation.ts
index e57c5a78b..56f209125 100644
--- a/src/infrastructure/email/sendAxisConfirmation.ts
+++ b/src/infrastructure/email/sendAxisConfirmation.ts
@@ -19,7 +19,7 @@ export async function sendAxisConfirmation(email: string, joinedAtISO: string) {
 
     // Log the event manually (until webhook is active)
     await logEmailEvent({
-      type: 'email.sent',
+      source: 'email.sent',
       to: email,
       from: 'connect@axpt.io',
       subject: 'Welcome to the Axis Journey',
diff --git a/src/lib/auth/session.ts b/src/lib/auth/session.ts
index fee02dcc7..338ce33d5 100644
--- a/src/lib/auth/session.ts
+++ b/src/lib/auth/session.ts
@@ -28,7 +28,7 @@ function getCookieConfig() {
   }
 }
 
-function normalizePayload(payload: any): SessionPayload | null {
+function normalizePayload(// payload removed (moved to context) any): SessionPayload | null {
   if (
     !payload?.userId ||
     !payload?.tier ||
@@ -70,7 +70,7 @@ function normalizePayload(payload: any): SessionPayload | null {
  * Create signed session JWT
  */
 export async function createSessionToken(
-  payload: SessionTokenInput
+  // payload removed (moved to context) SessionTokenInput
 ): Promise<string> {
   const iat = Math.floor(Date.now() / 1000)
   const exp = iat + SESSION_MAX_AGE
diff --git a/src/lib/auth/switchToResidentSession.ts b/src/lib/auth/switchToResidentSession.ts
index 40e8f64aa..ae516417a 100644
--- a/src/lib/auth/switchToResidentSession.ts
+++ b/src/lib/auth/switchToResidentSession.ts
@@ -10,7 +10,7 @@ interface SessionPayload {
   tier?: string;
 }
 
-export async function switchToResidentSession(payload: SessionPayload) {
+export async function switchToResidentSession(// payload removed (moved to context) SessionPayload) {
   const token = await new SignJWT(payload as any)
     .setProtectedHeader({ alg: 'HS256' })
     .setIssuedAt()
diff --git a/src/lib/axpt/devIssueToken.ts b/src/lib/axpt/devIssueToken.ts
index 31f9c85e0..6ade8dd3b 100644
--- a/src/lib/axpt/devIssueToken.ts
+++ b/src/lib/axpt/devIssueToken.ts
@@ -1,7 +1,7 @@
 // src/lib/axpt/devIssueToken.ts
 import jwt from 'jsonwebtoken';
 
-export function devIssueToken(payload: {
+export function devIssueToken(// payload removed (moved to context) {
   caseId: string;
   role: 'SELLER' | 'BUYER';
 }) {
diff --git a/src/lib/cases/eventPhases.ts b/src/lib/cases/eventPhases.ts
index 4210b74b4..daa5a57f3 100644
--- a/src/lib/cases/eventPhases.ts
+++ b/src/lib/cases/eventPhases.ts
@@ -6,7 +6,7 @@ export type Phase =
   | "RISK"
   | "UNKNOWN"
 
-export function getPhase(type: string): Phase {
+export function getPhase(source: string): Phase {
   switch (type) {
     case "CASE_CREATED":
       return "CREATION"
diff --git a/src/lib/cases/phaseEngine.ts b/src/lib/cases/phaseEngine.ts
index eeee84a81..e5e1367a7 100644
--- a/src/lib/cases/phaseEngine.ts
+++ b/src/lib/cases/phaseEngine.ts
@@ -8,7 +8,7 @@ export type CaseStage =
   | "AT_RISK"
 
 type Event = {
-  type: string
+  source: string
 }
 
 export function deriveCaseStage(events: Event[]): CaseStage {
diff --git a/src/lib/commands/buildCommand.ts b/src/lib/commands/buildCommand.ts
index 0b6c9e65d..3b83fe4d6 100644
--- a/src/lib/commands/buildCommand.ts
+++ b/src/lib/commands/buildCommand.ts
@@ -3,7 +3,7 @@ export function buildCommand({
   entity,
   exclude,
 }: {
-  type: 'PAUSE_ASSETS' | 'RESUME_ASSETS'
+  source: 'PAUSE_ASSETS' | 'RESUME_ASSETS'
   entity: {
     assets: string[]
     wallets: string[]
diff --git a/src/lib/email/emailLogService.ts b/src/lib/email/emailLogService.ts
index bc2457a19..9c721ec81 100644
--- a/src/lib/email/emailLogService.ts
+++ b/src/lib/email/emailLogService.ts
@@ -7,7 +7,7 @@ export async function getRecentEmailLogs(limit = 20) {
     take: limit,
     select: {
       id: true,
-      type: true,
+      source: true,
       to: true,
       from: true,
       subject: true,
@@ -28,7 +28,7 @@ export async function getPaginatedEmailLogs(page = 1, limit = 20) {
       take: limit,
       select: {
         id: true,
-        type: true,
+        source: true,
         to: true,
         from: true,
         subject: true,
@@ -50,10 +50,10 @@ export async function getPaginatedEmailLogs(page = 1, limit = 20) {
 export async function countEmailsByType() {
   const result = await prisma.emailLog.groupBy({
     by: ['type'],
-    _count: { type: true },
-    orderBy: { _count: { type: 'desc' } },
+    _count: { source: true },
+    orderBy: { _count: { source: 'desc' } },
   });
-  return result.map((r: any) => ({ type: r.type ?? 'unknown', count: r._count.type }));
+  return result.map((r: any) => ({ source: r.type ?? 'unknown', count: r._count.type }));
 }
 
 /** Search (with pagination) */
@@ -70,7 +70,7 @@ export async function searchEmailLogs(query: string, limit = 20, skip = 0) {
     take: limit,
     select: {
       id: true,
-      type: true,
+      source: true,
       to: true,
       from: true,
       subject: true,
diff --git a/src/lib/live/LiveBridge.ts b/src/lib/live/LiveBridge.ts
index 35e623ad8..e358c4730 100644
--- a/src/lib/live/LiveBridge.ts
+++ b/src/lib/live/LiveBridge.ts
@@ -28,7 +28,7 @@ function connect() {
   socket.addEventListener('open', () => {
     eventBus.emit('live:ws:connected', { at: Date.now() });
     socket?.send(
-      JSON.stringify({ type: 'command', command: 'subscribe-status' }),
+      JSON.stringify({ source: 'command', command: 'subscribe-status' }),
     );
   });
 
@@ -90,7 +90,7 @@ export function useLiveBridge() {
 
     const unsub = eventBus.on('live:command', ({ data }) => {
       if (!socket || socket.readyState !== WebSocket.OPEN) return;
-      socket.send(JSON.stringify({ type: 'command', ...data }));
+      socket.send(JSON.stringify({ source: 'command', ...data }));
     });
 
     return () => {
diff --git a/src/lib/live/fetchOwncastStatus.ts b/src/lib/live/fetchOwncastStatus.ts
index 37140dfa9..f7f6812e9 100644
--- a/src/lib/live/fetchOwncastStatus.ts
+++ b/src/lib/live/fetchOwncastStatus.ts
@@ -30,7 +30,7 @@ export async function fetchOwncastStatus() {
 
     const json = await res.json();
 
-    const payload: OwncastStatus = {
+    const // payload removed (moved to context) OwncastStatus = {
       online: json?.online ?? false,
       viewerCount: json?.viewerCount ?? 0,
       lastConnectTime: json?.lastConnectTime ?? null,
diff --git a/src/lib/live/useLiveStatus.ts b/src/lib/live/useLiveStatus.ts
index 4ccdcdb1a..770b7dd0e 100644
--- a/src/lib/live/useLiveStatus.ts
+++ b/src/lib/live/useLiveStatus.ts
@@ -33,7 +33,7 @@ export function useLiveStatus(): LiveStatus {
 
   useEffect(() => {
     const unsubStatus = eventBus.on('live:status:update', ({ data }) => {
-      const payload: any = data ?? {};
+      const // payload removed (moved to context) any = data ?? {};
 
       const viewers =
         typeof payload.viewers === 'number'
diff --git a/src/lib/notify.ts b/src/lib/notify.ts
index 1740ce5cc..e8b87832b 100644
--- a/src/lib/notify.ts
+++ b/src/lib/notify.ts
@@ -87,7 +87,7 @@ export async function sendCouncilNotification(
  * Uses settings.slackWebhookUrl or env fallback.
  */
 export async function sendSlackNotification(
-  payload: string | Record<string, any>
+  // payload removed (moved to context) string | Record<string, any>
 ): Promise<SlackResult> {
   const settings = await getSettings();
   const webhook =
diff --git a/src/lib/predictive/predictiveEngine.ts b/src/lib/predictive/predictiveEngine.ts
index 76f10e9af..5c4cfea1c 100644
--- a/src/lib/predictive/predictiveEngine.ts
+++ b/src/lib/predictive/predictiveEngine.ts
@@ -21,11 +21,11 @@ export async function runPredictiveEngine() {
   for (const signal of signals) {
     await prisma.circuitEvent.create({
       data: {
-        type: 'PREDICTIVE_SIGNAL',
+        source: 'PREDICTIVE_SIGNAL',
         severity: signal.severity,
         message: signal.message,
         metadata: {
-          signalType: signal.type,
+          signalType: signal.source,
           assetCode: signal.assetCode ?? null,
           value: signal.value ?? null,
           ...signal.metadata,
@@ -37,7 +37,7 @@ export async function runPredictiveEngine() {
   for (const rec of recommendations) {
     await prisma.circuitEvent.create({
       data: {
-        type: 'PREDICTIVE_RECOMMENDATION',
+        source: 'PREDICTIVE_RECOMMENDATION',
         severity: rec.autoRunnable ? 'INFO' : 'WARN',
         message: `Recommended intent: ${rec.intent} → ${rec.reason}`,
         metadata: {
@@ -53,7 +53,7 @@ export async function runPredictiveEngine() {
     if (confidence < GOVERNOR_CONFIG.predictiveRecommendationConfidence) {
       await prisma.circuitEvent.create({
         data: {
-          type: 'PREDICTIVE_EXECUTION',
+          source: 'PREDICTIVE_EXECUTION',
           severity: 'INFO',
           message: `Skipped predictive intent: ${rec.intent} (confidence below threshold)`,
           metadata: {
@@ -83,7 +83,7 @@ export async function runPredictiveEngine() {
       if (sameSignal && elapsed < COOLDOWN_MS) {
         await prisma.circuitEvent.create({
           data: {
-            type: 'PREDICTIVE_EXECUTION',
+            source: 'PREDICTIVE_EXECUTION',
             severity: 'INFO',
             message: `Skipped predictive intent: ${rec.intent} (cooldown active)`,
             metadata: {
@@ -111,7 +111,7 @@ export async function runPredictiveEngine() {
       if (!['STABILIZE_SYSTEM', 'RESUME_SAFE'].includes(rec.intent)) {
         await prisma.circuitEvent.create({
           data: {
-            type: 'PREDICTIVE_EXECUTION',
+            source: 'PREDICTIVE_EXECUTION',
             severity: 'WARN',
             message: `Skipped predictive intent: ${rec.intent} (system paused, only safe intents allowed)`,
             metadata: {
@@ -130,7 +130,7 @@ export async function runPredictiveEngine() {
 
     await prisma.circuitEvent.create({
       data: {
-        type: 'PREDICTIVE_EXECUTION',
+        source: 'PREDICTIVE_EXECUTION',
         severity: 'INFO',
         message: `Executing predictive intent: ${rec.intent}`,
         metadata: {
diff --git a/src/lib/predictive/predictiveSignals.ts b/src/lib/predictive/predictiveSignals.ts
index 1124f7afc..c020c69e4 100644
--- a/src/lib/predictive/predictiveSignals.ts
+++ b/src/lib/predictive/predictiveSignals.ts
@@ -5,7 +5,7 @@ import { GOVERNOR_CONFIG } from '@/domains/predictive/governorConfig'
 
 export function hashSignals(signals: PredictiveSignal[]) {
   const base = signals
-    .map((signal) => `${signal.type}:${signal.assetCode ?? 'global'}:${signal.value ?? 0}`)
+    .map((signal) => `${signal.source}:${signal.assetCode ?? 'global'}:${signal.value ?? 0}`)
     .sort()
     .join('|')
 
@@ -37,12 +37,12 @@ export async function collectPredictiveSignals(): Promise<PredictiveSignal[]> {
     }),
     prisma.circuitEvent.count({
       where: {
-        type: 'RECON',
+        source: 'RECON',
         severity: 'WARN',
       },
     }),
     prisma.circuitEvent.findFirst({
-      where: { type: 'SYNC' },
+      where: { source: 'SYNC' },
       orderBy: { createdAt: 'desc' },
     }),
   ])
@@ -50,7 +50,7 @@ export async function collectPredictiveSignals(): Promise<PredictiveSignal[]> {
   const retryPressure = retryableCount + submittingCount + submittedCount
   if (retryPressure >= GOVERNOR_CONFIG.retryPressureWarn) {
     signals.push({
-      type: 'RETRY_PRESSURE',
+      source: 'RETRY_PRESSURE',
       severity:
         retryPressure >= GOVERNOR_CONFIG.retryPressureCritical
           ? 'CRITICAL'
@@ -62,7 +62,7 @@ export async function collectPredictiveSignals(): Promise<PredictiveSignal[]> {
 
   if (deadLetterCount > 0) {
     signals.push({
-      type: 'DEAD_LETTER_PRESENT',
+      source: 'DEAD_LETTER_PRESENT',
       severity: 'CRITICAL',
       message: `Dead-letter jobs present: ${deadLetterCount}`,
       value: deadLetterCount,
@@ -71,7 +71,7 @@ export async function collectPredictiveSignals(): Promise<PredictiveSignal[]> {
 
   if (recentReconWarns >= GOVERNOR_CONFIG.reconWarnRepeatThreshold) {
     signals.push({
-      type: 'RECON_INSTABILITY',
+      source: 'RECON_INSTABILITY',
       severity:
         recentReconWarns >= GOVERNOR_CONFIG.reconCriticalRepeatThreshold
           ? 'CRITICAL'
@@ -87,7 +87,7 @@ export async function collectPredictiveSignals(): Promise<PredictiveSignal[]> {
 
     if (lagSeconds > GOVERNOR_CONFIG.syncLagWarnSeconds) {
       signals.push({
-        type: 'SYNC_LAG_RISING',
+        source: 'SYNC_LAG_RISING',
         severity:
           lagSeconds > GOVERNOR_CONFIG.syncLagCriticalSeconds
             ? 'CRITICAL'
@@ -110,7 +110,7 @@ export async function collectPredictiveSignals(): Promise<PredictiveSignal[]> {
     if (!row.assetCode) continue
 
     signals.push({
-      type: 'ASSET_STRESS',
+      source: 'ASSET_STRESS',
       severity:
         row._count >= GOVERNOR_CONFIG.assetStressCritical ? 'CRITICAL' : 'WARN',
       assetCode: row.assetCode,
diff --git a/src/lib/realtime/useCommandCenterStream.ts b/src/lib/realtime/useCommandCenterStream.ts
index 45538d4a0..8c8f9f714 100644
--- a/src/lib/realtime/useCommandCenterStream.ts
+++ b/src/lib/realtime/useCommandCenterStream.ts
@@ -4,7 +4,7 @@
 import { useEffect, useState } from "react";
 
 export type DomainEvent = {
-  type: string;
+  source: string;
   createdAt: string;
   streamId?: string;
   payload?: any;
diff --git a/src/lib/system/alert.ts b/src/lib/system/alert.ts
index 966769f0a..e4f0a8ae7 100644
--- a/src/lib/system/alert.ts
+++ b/src/lib/system/alert.ts
@@ -40,7 +40,7 @@ export async function sendAlert(
 
   await prisma.circuitEvent.create({
     data: {
-      type: 'ALERT',
+      source: 'ALERT',
       severity: level,
       message,
       metadata: {
diff --git a/src/lib/system/pause.ts b/src/lib/system/pause.ts
index 5c98c564b..f191e32cb 100644
--- a/src/lib/system/pause.ts
+++ b/src/lib/system/pause.ts
@@ -32,7 +32,7 @@ export async function getSystemState() {
     )
   } catch (error) {
     // If table doesn't exist yet, return default state
-    console.warn('SystemState table not found, using default state:', error)
+    console.warn('SystemState table not found, using default // state removed (ETK-owned)', error)
     return {
       globalPaused: false,
       pausedAssets: [],
@@ -116,7 +116,7 @@ export async function setSystemPause(
 
       await prisma.circuitEvent.create({
         data: {
-          type: 'PAUSE',
+          source: 'PAUSE',
           severity: 'CRITICAL',
           message,
         },
diff --git a/src/lib/toasts/adminOutcome.ts b/src/lib/toasts/adminOutcome.ts
index 0f41e5209..09bfa04a4 100644
--- a/src/lib/toasts/adminOutcome.ts
+++ b/src/lib/toasts/adminOutcome.ts
@@ -21,7 +21,7 @@ function asStringArray(value: unknown): string[] {
     : []
 }
 
-export function toastPauseResult(state: PauseState) {
+export function toastPauseResult(// state removed (ETK-owned) PauseState) {
   const assets = asStringArray(state.pausedAssets)
   const layers = asStringArray(state.pausedLayers)
 
diff --git a/src/lib/token/saveTokenLog.ts b/src/lib/token/saveTokenLog.ts
index 11ac3eea4..7cb17918b 100644
--- a/src/lib/token/saveTokenLog.ts
+++ b/src/lib/token/saveTokenLog.ts
@@ -6,7 +6,7 @@ import { TokenPayload } from './tokenSchema';
 
 const logPath = path.resolve('app/scripts/partner-tokens/logs/token-log.json');
 
-export async function saveTokenLog(token: string, payload: TokenPayload): Promise<void> {
+export async function saveTokenLog(token: string, // payload removed (moved to context) TokenPayload): Promise<void> {
   const newEntry = {
     ...payload,
     token,
diff --git a/src/lib/token/signToken.ts b/src/lib/token/signToken.ts
index 4c0ee27d9..1c8b75fd0 100644
--- a/src/lib/token/signToken.ts
+++ b/src/lib/token/signToken.ts
@@ -4,7 +4,7 @@ import { env } from '@/infrastructure/env/readEnv';
 import { SignJWT } from 'jose';
 import type { TokenPayload } from '@/shared/types/token';
 
-export async function signToken(payload: TokenPayload): Promise<string> {
+export async function signToken(// payload removed (moved to context) TokenPayload): Promise<string> {
   const secret = new TextEncoder().encode(env.SIGNING_SECRET);
 
   const now = Math.floor(Date.now() / 1000);
diff --git a/src/lib/token/tokenService.ts b/src/lib/token/tokenService.ts
index 893cb63b0..d4e62fda1 100644
--- a/src/lib/token/tokenService.ts
+++ b/src/lib/token/tokenService.ts
@@ -31,7 +31,7 @@ export type TokenInput = {
 
 export type TokenEntry = {
   token: string;
-  payload: ReturnType<typeof generateTokenPayload> & { token: string };
+  // payload removed (moved to context) ReturnType<typeof generateTokenPayload> & { token: string };
   qrPath: string;
   createdAt: Date;
 };
@@ -88,7 +88,7 @@ export async function generateTokenFromInput(input: TokenInput): Promise<TokenEn
 
   return {
     token,
-    payload: { ...payload, token },
+    // payload removed (moved to context) { ...payload, token },
     qrPath,
     createdAt,
   };
@@ -117,25 +117,25 @@ export async function promptAndGenerateToken(): Promise<TokenEntry> {
   }));
 
   const answers = await inquirer.prompt([
-    { type: 'input', name: 'partner', message: '🧿 Partner slug (e.g., mansa-mussa):' },
-    { type: 'input', name: 'displayName', message: '🧠 Full name of partner:' },
-    { type: 'input', name: 'email', message: '📧 Partner email (optional):' },
+    { source: 'input', name: 'partner', message: '🧿 Partner slug (e.g., mansa-mussa):' },
+    { source: 'input', name: 'displayName', message: '🧠 Full name of partner:' },
+    { source: 'input', name: 'email', message: '📧 Partner email (optional):' },
     {
-      type: 'list',
+      source: 'list',
       name: 'tier',
       message: '🎖️ Select tier:',
       choices: ['Investor', 'Project Manager', 'Nomad', 'Creative Producer'],
     },
     {
-      type: 'checkbox',
+      source: 'checkbox',
       name: 'docs',
       message: '📄 Select documents:',
       choices: DOC_CHOICES,
     },
-    { type: 'input', name: 'popupMessage', message: '✨ Custom popup message (optional):' },
-    { type: 'input', name: 'greeting', message: '👋 Custom greeting (optional):' },
+    { source: 'input', name: 'popupMessage', message: '✨ Custom popup message (optional):' },
+    { source: 'input', name: 'greeting', message: '👋 Custom greeting (optional):' },
     {
-      type: 'list',
+      source: 'list',
       name: 'expChoice',
       message: '⏳ Choose token expiration:',
       choices: EXPIRATION_OPTIONS.map(opt => opt.name),
diff --git a/src/lib/token/validatePayload.ts b/src/lib/token/validatePayload.ts
index a9177c757..77f732f03 100644
--- a/src/lib/token/validatePayload.ts
+++ b/src/lib/token/validatePayload.ts
@@ -8,7 +8,7 @@ import type { TokenPayload } from './tokenSchema';
  * - Checks schema compliance
  * - Checks expiry (exp vs now)
  */
-export function validateTokenPayload(payload: unknown): {
+export function validateTokenPayload(// payload removed (moved to context) unknown): {
   valid: boolean;
   data?: TokenPayload;
   error?: string;
diff --git a/src/lib/token/verifyToken.ts b/src/lib/token/verifyToken.ts
index a62c5bd0c..114e5cb48 100644
--- a/src/lib/token/verifyToken.ts
+++ b/src/lib/token/verifyToken.ts
@@ -26,7 +26,7 @@ const secret = toUint8(secretStr.trim()); // ← trim whitespace
 
 export async function verifyToken(token: string): Promise<{
   valid: boolean;
-  payload: TokenPayload | null;
+  // payload removed (moved to context) TokenPayload | null;
   error?: string;
 }> {
   try {
@@ -43,7 +43,7 @@ export async function verifyToken(token: string): Promise<{
     const v = TokenPayloadSchema.safeParse(payload);
     if (!v.success) {
       console.log('[verifyToken] final valid?', false);
-      return { valid: false, payload: null, error: 'Token payload validation failed.' };
+      return { valid: false, // payload removed (moved to context) null, error: 'Token payload validation failed.' };
     }
 
     // 3) Revocation check — your Prisma model stores RAW token in `rawToken`
@@ -53,11 +53,11 @@ export async function verifyToken(token: string): Promise<{
     console.log('[verifyToken] revoked?', !!revoked);
     if (revoked) {
       console.log('[verifyToken] final valid?', false);
-      return { valid: false, payload: null, error: 'Token has been revoked.' };
+      return { valid: false, // payload removed (moved to context) null, error: 'Token has been revoked.' };
     }
 
     console.log('[verifyToken] final valid?', true);
-    return { valid: true, payload: v.data };
+    return { valid: true, // payload removed (moved to context) v.data };
   } catch (err: any) {
     // Helpful diagnostics
     console.warn('[verifyToken] jose error:', {
@@ -66,6 +66,6 @@ export async function verifyToken(token: string): Promise<{
       message: err?.message,
     });
     console.log('[verifyToken] final valid?', false);
-    return { valid: false, payload: null, error: 'Token verification failed' };
+    return { valid: false, // payload removed (moved to context) null, error: 'Token verification failed' };
   }
 }
diff --git a/src/lib/treasury/emitTreasuryEvents.ts b/src/lib/treasury/emitTreasuryEvents.ts
index ac548df8b..6402ba0b1 100644
--- a/src/lib/treasury/emitTreasuryEvents.ts
+++ b/src/lib/treasury/emitTreasuryEvents.ts
@@ -13,7 +13,7 @@ export async function emitTransferEvents(
         eventType: tx.direction === "in"
           ? "TREASURY_INFLOW"
           : "TREASURY_OUTFLOW",
-        payload: {
+        // payload removed (moved to context) {
           path: ["txHash"],
           equals: tx.txHash,
         },
@@ -30,7 +30,7 @@ export async function emitTransferEvents(
         tx.direction === "in"
           ? "TREASURY_INFLOW"
           : "TREASURY_OUTFLOW",
-      payload: {
+      // payload removed (moved to context) {
         txHash: tx.txHash,
         from: tx.from,
         to: tx.to,
diff --git a/src/lib/treasury/erc20Abi.ts b/src/lib/treasury/erc20Abi.ts
index 79f6e38b7..9f37bb775 100644
--- a/src/lib/treasury/erc20Abi.ts
+++ b/src/lib/treasury/erc20Abi.ts
@@ -1,9 +1,9 @@
 export const erc20Abi = [
   {
-    type: "function",
+    source: "function",
     name: "balanceOf",
     stateMutability: "view",
-    inputs: [{ name: "account", type: "address" }],
-    outputs: [{ name: "", type: "uint256" }],
+    inputs: [{ name: "account", source: "address" }],
+    outputs: [{ name: "", source: "uint256" }],
   },
 ] as const;
\ No newline at end of file
diff --git a/src/lib/treasury/erc20TransferAbi.ts b/src/lib/treasury/erc20TransferAbi.ts
index 116d4b894..7273b5ede 100644
--- a/src/lib/treasury/erc20TransferAbi.ts
+++ b/src/lib/treasury/erc20TransferAbi.ts
@@ -1,11 +1,11 @@
 export const erc20TransferAbi = [
   {
-    type: "event",
+    source: "event",
     name: "Transfer",
     inputs: [
-      { indexed: true, name: "from", type: "address" },
-      { indexed: true, name: "to", type: "address" },
-      { indexed: false, name: "value", type: "uint256" },
+      { indexed: true, name: "from", source: "address" },
+      { indexed: true, name: "to", source: "address" },
+      { indexed: false, name: "value", source: "uint256" },
     ],
   },
 ] as const;
\ No newline at end of file
diff --git a/src/lib/treasury/escrowMatcher.ts b/src/lib/treasury/escrowMatcher.ts
index 51fce43b3..1a3289b79 100644
--- a/src/lib/treasury/escrowMatcher.ts
+++ b/src/lib/treasury/escrowMatcher.ts
@@ -25,7 +25,7 @@ export async function matchTransferToEscrow(
         streamType: "CASE",
         streamId: c.id,
         eventType: EventTypes.ESCROW_FUNDED,
-        payload: {
+        // payload removed (moved to context) {
           txHash: tx.txHash,
           amount: tx.amount,
           walletId,
@@ -39,7 +39,7 @@ export async function matchTransferToEscrow(
         streamType: "CASE",
         streamId: c.id,
         eventType: EventTypes.ESCROW_PARTIALLY_FUNDED, // ✅ FIXED
-        payload: {
+        // payload removed (moved to context) {
           txHash: tx.txHash,
           amount: tx.amount,
           walletId,
@@ -53,7 +53,7 @@ export async function matchTransferToEscrow(
         streamType: "CASE",
         streamId: c.id,
         eventType: EventTypes.ESCROW_MISMATCH, // ✅ FIXED
-        payload: {
+        // payload removed (moved to context) {
           txHash: tx.txHash,
           amount: tx.amount,
           walletId,
diff --git a/src/server/warroom/warroom-stream.ts b/src/server/warroom/warroom-stream.ts
index 661d5c19b..70d266090 100644
--- a/src/server/warroom/warroom-stream.ts
+++ b/src/server/warroom/warroom-stream.ts
@@ -14,16 +14,16 @@ export function attachWarRoomStream(ws: any) {
 
     if (!decision.allowed) {
       ws.send(JSON.stringify({
-        type: "SNAPSHOT_BLOCKED",
+        source: "SNAPSHOT_BLOCKED",
         reason: decision.reason,
-        payload: null
+        // payload removed (moved to context) null
       }))
 
       executionTraceLedger.append({
         id: crypto.randomUUID(),
         timestamp: Date.now(),
-        type: "STREAM_BLOCKED",
-        payload: snapshot,
+        source: "STREAM_BLOCKED",
+        // payload removed (moved to context) snapshot,
         result: decision.reason
       })
 
@@ -31,22 +31,22 @@ export function attachWarRoomStream(ws: any) {
     }
 
     ws.send(JSON.stringify({
-      type: "SNAPSHOT",
+      source: "SNAPSHOT",
       version: EXECUTION_VERSION,
-      payload: snapshot,
+      // payload removed (moved to context) snapshot,
     }))
 
     ws.send(JSON.stringify({
-      type: "GOVERNANCE_DECISION",
+      source: "GOVERNANCE_DECISION",
       version: EXECUTION_VERSION,
-      payload: decision
+      // payload removed (moved to context) decision
     }))
 
     executionTraceLedger.append({
       id: crypto.randomUUID(),
       timestamp: Date.now(),
-      type: "WS_BROADCAST",
-      payload: snapshot
+      source: "WS_BROADCAST",
+      // payload removed (moved to context) snapshot
     })
   })
 }
